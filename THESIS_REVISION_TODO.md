# Thesis revision checklist

**Deadline update (14 Sep 2026):** Prof. Pires agreed to send Henrique feedback today
(14 Sep) so he can send the first half (through Ch3) to his second advisor, Prof.
Sampaio, for review/acceptance. Plan: second half follows about a week later
(~21 Sep). Hard deadline: full thesis to the school by **30 Sep 2026 at the latest**.
Ch4 work (this file's Task V equivalent, the antenna-family rewrite) is not on the
critical path for today's send but needs to be in good shape before the second-half
send in about a week.

Consolidated from three independent reviews of the current thesis draft on 17 August 2026.  This is a prioritised working list, not a claim that every item is an established physics error.  Items marked **verify** require comparison with the cited source or a trusted symbolic calculation before changing the thesis.

Known placeholder references to the appendices are deliberately not repeated here, as they are already tracked separately.

## How to read and update this file (protocol for any Claude instance, added 26 Aug 2026)

**This file is the single source of truth, edited by multiple Claude Code sessions over time, not just
whichever one you're in right now.** Treat it accordingly:

- **Always re-read this file fresh at the start of a session and before acting on it, even if you
  remember editing it recently.** Another instance may have advanced it further than your own memory
  of it — the entries below already show this happening in practice (a second session found and fixed
  the real footnote 17 after an earlier session's guess was wrong; re-lettered tasks after one was
  retired; pulled a "Mechanical Pile" out of the whole document, not just the section a prior session
  was focused on). If a memory note or your own recollection disagrees with what's actually in this
  file, **this file wins** — it reflects real edits to real source files, a memory note is a summary
  that can go stale within the same day.
- **If the file (or `memory/thesis-review-antcalc.md`) shows as changed since you last read it,
  that's expected, not an error.** Read the new content, take it as current state, and only push back
  if something in it looks actually wrong on its own merits (a physics claim you can check and
  disagree with, a classification that doesn't hold up) — not just because it differs from what you
  remembered. Don't silently revert another session's work.
- **Task-labelling conventions in use, keep them consistent:**
  - Items requiring Henrique's own composition/judgement (the "Yours" bucket) get single capital
    letters — **A, B, C, ...** — assigned once and kept stable; if an earlier letter gets retired
    (folded into a later task), leave a `~~struck~~` pointer rather than reusing the letter or leaving
    a gap.
  - Purely mechanical items (no physics judgement, no composition, a fix or relocation anyone could
    verify and execute) live in the **Mechanical Pile** section, numbered **Mech-1, Mech-2, ...** in
    roughly execution order. If you find a mechanical item still sitting in its original
    section elsewhere in this file, that's a sign no one's swept it into the pile yet, not that it
    doesn't belong there.
  - When an item is finished, resolved as already-satisfied, found stale, or retired, mark it `[x]`
    (or leave `[ ]` with a clear stale/deferred note — see Mech-6, Mech-9) and **say what actually
    happened**, not just "done" — the entries in this file that hold up best are the ones that record
    what was checked and why, so a later session (or Henrique) doesn't have to re-derive the
    reasoning. If an item duplicates one already resolved elsewhere in the file, mark it done with a
    pointer to where, rather than leaving two copies of the same open task.
  - Before marking anything "Mine"/mechanical and executing it, verify it's actually still mechanical
    against the *current* source — grep for cross-chapter references, check whether a cited equation
    or subsection still means what the task assumes. Several items in this file were reclassified
    from Mine to Yours mid-review after this check turned up a real dependency (`subsec:antFamilies`,
    `subsec:PaVe`, the §2.4 `\sim`-equation fixes) — that reclassify-and-flag discipline is the point,
    not a failure mode to avoid.
- **New Prof. Pires review emails get processed the same way every time**, in order: read the whole
  email before reacting to any single point (check for repeats of earlier feedback first); ground
  every point against the actual current source, including verifying his own technical claims where
  checkable, not just his paraphrase of the text; explain each point back to Henrique in plain terms
  *before* touching any file; classify into Mine/Mechanical vs. Yours and write the result into this
  file as the durable record; then execute Mine items directly (showing the real diff afterward) and
  build Yours items through grounding/worked examples rather than drafting the prose, reviewing
  Henrique's own draft afterward the same way you'd review anyone's. Full version of this protocol,
  plus the working history behind it, is in `memory/mechanical-vs-yours-workflow.md` and
  `memory/thesis-review-antcalc.md` — read those too, but this file's actual task state is authoritative
  over both if they ever disagree.

## 0. PRIORITY — Advisor feedback from Prof. Pires (Ch1 Introduction + Ch2 partial, received 24 August 2026)

Advisor feedback takes priority over everything below where they overlap. This is a **partial** pass — Prof. Pires said more corrections for the rest of Chapter 2 are coming later this week; fold those in here when they arrive. Where he supplied ready-made replacement text, go back to his email for the exact wording rather than re-deriving it.

---

### >>> YOURS — CONSOLIDATED REMINDER LIST (Henrique's own to-do; kept current; last updated 21 Sep 2026, late)

**STATUS UPDATE (batch 14–16, 21 Sep late): items 3, 4, 7, 8, 9 (diagrams drafted and inserted — please REVIEW them, item 9b) and 13 are DONE. No `% TODO[Yours]` markers are left in Ch4/Ch5; the only ones left are none in the source. What remains is the page budget.** *Only things that need YOUR decision, writing, run or communication. Everything Claude does is in the batch records below. Tick/strike items here when done.*

**A. Blocking / code**
1. **Diagnose the A22 Leading/Subleading regression on `c5df6d3`** (Lead ε⁻³: 25/12 vs literature 17/8; Sub ε⁻⁴: −3/8 vs −1/4; Breve and N_f fine). Until fixed, do not trust: Ch3 commit citation (l.10, l.871), Ch4 §4.2.1/§4.2.2 `Out[2]/[4]/[5]` + Table 4.1, Appendix A (A22, Ã22), Ch5 §5.4/5.5/5.7 ("poles cancel / BuildRRatio reproduces Eq. (5.17)"), Ch6 Conclusions, and the "Pass" entries of the §4.6 tables.
2. Release housekeeping: `version.wl` release-history entry for the N_f fix; decide which tag/commit the thesis cites (Ch3 l.10, l.871); Ch3 l.188 `DropSumOver` row is inaccurate (generation sums → N_f, now N_f/2 per class).

**B. Chapter 4 — decisions / writing (all have a `% TODO[Yours]` in the source unless noted)**
3. **§4.5.2 (massive A30):** Prof's sentence "Internally, this calculation performs the massive IBP reduction…" was NOT written — the default route returns the encoded literature closed form; the LiteRed J-route only runs when forced and calibrates the −1/4. Decide the wording (and the Table 4.2 massive row). *(The Conclusion's massive sentence follows Prof's text, per your decision.)*
4. **Q22:** what to say about the origin of the −1/4 (`MassiveA30IntegratedCutMeasureFactor`, calibrated cut-measure constant, not derived analytically).
5. **R8b:** the removed middle line had `½ Im S` that I could not reconcile with the printed series; and 0311276 (4.27) prints `Box(s13,s13,s12)` for V8 whereas the thesis uses `Box(s13,s23,s12)` — decide.
6. **V5a sign check** (printed V5a = −R3·Re B0 under the App.-B convention of B0 vs the `Re[−i∫…]` definition).
7. **Bateman citation** for the ₂F₁ quadratic transformation (marker in Appendix B.6; not in `references.bib`).
8. **dΦ4 prefactor** `(q²)^{3−d/2}` (verbatim paper, dimensionally inconsistent) — marker above `eq:phaseSpaces`; decide/check a 2nd source.
9b. **Review the five diagram figures (Figs. 4.2–4.6; `figures/diagrams-*.tex`), drawn by Claude with tikz-feynman:** A₂¹ (vertex + two vanishing external self-energies), A₂² (planar ladder, crossed ladder, quark self-energy insertion, N_f quark bubble), A₃¹ (vertex correction, box, three-gluon vertex, self-energy insertion), A₄⁰ (both gluons from the quark, one from each line, three-gluon vertex), B₄⁰/C₄⁰ side by side ((a),(b) gluon splitting from the quark/antiquark line; (c),(d) the exchange diagrams for identical flavours). They are representative, chosen from the standard topologies — please check they match the diagram classes FeynArts actually generates for your routes, and swap any you prefer.
9. **Feynman diagrams:** pick the ~6 representative diagrams for ONE family-map figure at the top of Ch4 (A₂¹ vertex, A₂² ladder/crossed, N_f bubble, A₃¹, A₄⁰, B₄⁰/C₄⁰); then re-add Prof's "shown in Fig. X" clauses. (Claude can build the tikz-feynman once you choose.)
10. ~~"PaVe" abbreviation~~ **DONE 22 Sep (Henrique: "change PaVe thesis-wide"):** every visible "PaVe" replaced by "Passarino-Veltman" — Ch2 (definition "(PaVe)" removed, equation-arrow label), Ch4 (§4.6 table cells; dead blocks too), Appendix B (section title "Passarino-Veltman Reduction", subsection "Passarino-Veltman Scalar Functions", 6 sentences), figures (Fig. 3.7 node and grey label, caption; the higher-level-functions figure, currently in a disabled part of Ch3). Label/ref names (`subsec:PaVe`, `eq:PaVeSchematic`, `figures/pave-reduction.tex`) unchanged — invisible. No "PaVe" left in the PDF; pagination unchanged (main text ends p.80).
11. **Review the Claude-drafted paragraphs before they reach Prof** (single-use drafts): §4.2.1 B₀/C₀-counterterm paragraph and the "three further steps" Out[4]→Out[5] paragraph (both now in the file); still to be re-applied from the backup: Ã₃¹ pole-pattern explanation (A1), real-part explanation (A3), R3-absence sentence (§4.3.1).
12. **§4.6 tables** say "Pass" for the A22 set while item 1 is open.
12a. ~~Make sure the "computed by hand" claim is exactly true~~ **RESOLVED 21 Sep (Henrique):** all massless master integrals were derived by him (A₆ to the ε-expansion — via Mellin–Barnes — and it matches the exact closed form of hep-ph/0507061); the **massive** ones (I₁, I₂, §4.5.1) are taken from Ref. [25]; he no longer has the complete derivations at hand, so Appendix C shows R₈ₐ in full as the worked example of the method. Wording chosen by Claude (Henrique: "I leave it to you"): the Ch4 note says the massless masters "were all derived explicitly, and not merely taken from a source", results reproduce the literature [5,13], Appendix C gives definitions and results and R₈ₐ step by step "as an example of the method"; the Appendix C intro says the same; the A₆ subsection was rewritten to the original Mellin–Barnes derivation + Laurent series **followed by** "agrees with the expansion of the exact expression in ε of Ref. [12]" (this also answers Prof's Q5). **Left for you:** if you find the other derivations, they could go into Appendix C (not counted in the 80 pages); for the viva be ready to reproduce at least R₄, A₃ and the A₆ MB steps.
12d. **§4.3.2 (Ã₃¹ starts at 1/ε²) — the explanation paragraph is MY earlier draft** (written before the mechanism was pinned down in hep-ph/0505111). It says: adjacent qg/gq̄ dipoles keep 1/ε² poles at leading colour; at subleading colour the projection removes them; the universal poles ∝ C_F so their 1/N parts cancel against the A₂¹A₃⁰ subtraction; what remains is ln(s_ij/q²)/ε, vanishing in the soft limit. The more precise, source-grounded version (see the "Mechanism identified" note under Q15 in the Ch4 second-batch block): 0505111 Eq. (5.12) for leading colour, 2[I_qg(s13)+I_qg(s23)−I_qq̄(s123)]A₃⁰, vs Eq. (5.14) for subleading colour, 2[I_qq̄(s12)−I_qq̄(s123)]A₃⁰, where I ∝ ε⁻²(−s)^{−ε} so the scale-independent double pole cancels and only log(s12/s123)/ε remains (→ log z → 0 in the soft limit); integrated result agrees exactly with Eq. (5.19). **Decide whether to have it swapped in**, and in any case read it: it is a physics claim with your name on it (Prof's question Q15).
12c. **§4.2.4 (Breve) source note:** Prof's replacement text was used, so the old quantitative sentence ("an uncancelled $28C_F^2\zeta_3/(3\epsilon)$ residue", = 4C_F²·7ζ₃/(3ε)) is no longer in the thesis. It's a nice concrete detail for the viva; re-add it as one sentence in the second paragraph if you want it.
12b. **Review my wording of the master-integral relocation (my composition, requested by you):** the note at the start of Ch4 (after the Table 4.1 intro, "These master integrals are defined and evaluated in Appendix C, organised by antenna family; in this chapter only R₃ … and the massive master integrals of §4.5 are treated in the main text"), the edits to Prof's PV/IBP second paragraph, the three one-line pointers at the end of §4.2/§4.3/§4.4, and the intro paragraph of Appendix C. Also decide: the massive masters I₁, I₂ (§4.5.1) were left in the main text with the massive antenna — say if you want them in Appendix C too.

**C. Chapter 5**
13. **5.Y1:** one bridging sentence before "Upon integration … replaced by their integrated counterparts" (true only up to the ½ on Ã₄⁰ and 2 on C₄⁰ of Eq. (5.15); marker in `05-validation-r-ratio.tex`).
14. Ledger justification: the *reason* for the inclusive-level ½ ("identical-gluon phase-space counting") is deliberately not asserted — add only if you can point to a passage in a paper.

**D. Chapter 6**
15. Check Conclusion §3 claim "the unintegrated antenna functions were compared with established results in the literature" against the §4.6 validation table / Appendix A local validation, so you can defend it at the viva. (Massive-antenna "genuinely new ingredients" and the Conclusion wording follow Prof, your decision on 21 Sep; note the published massive NNLO antenna literature — Abelof/Gehrmann-De Ridder et al. — in case of viva questions.)

**E. Communication**
16. **Tell Prof (deviations from his text):** A₆ exact expression (hep-ph/0507061 Eq. 5) instead of Mellin–Barnes and A₆ is not a leading-colour master; Eq. (5.15) keeps ½Ã₄⁰ / 2C₄⁰ (his copy has unit coefficients); `R⁽⁰⁾=N Σ e_q²` (N, not N_c); In/Out box kept in 5.5; `m_q` kept (he wrote `m_Q`); + whatever gets trimmed in the Ch4 compression pass; and the status of item 1.
17. **Tell Sampaio** everything that postdates his copy: Ch2 §2.1.2–2.5 batch (15 Sep), Ch4 (rewritten), Ch5 (restructured), Ch6/7 (merged).

**G. FCUL formatting norms (checked 21 Sep 2026 against the official PDF, `normas_escrita_trabalho_final.pdf`, "Normas para elaboração da Dissertação de Mestrado")** — key rules: *main text ≤ 80 pages* (bibliography and annexes are listed separately, so not counted); body **Times New Roman 11 pt, line spacing 1.15**; **footnotes, figure captions and tables: 9 pt, single spacing**; margins 2.5 cm; page numbers Roman from the title page to the front matter, then Arabic from the first page of the main text to the very end (bibliography and appendices included); equations numbered chapter.number and centred; **figure captions BELOW figures, table captions ABOVE tables**; figure/table numbering chapter.number, tables numbered independently of other elements; if the thesis is in English the **Portuguese Resumo must be 1200–1500 words** and the English Abstract ≤ **300 words**; ≤ 5 keywords; "Versão Provisória" on the cover of a provisional version. *Note in the PDF: missing elements or elements "em moldes diferentes" prevent admission to the defence.*
19. ~~Abstract ≈ 332 words, max 300~~ **DONE 21 Sep (night): English Abstract trimmed to 290 words (texcount; 288 rendered)** by tightening six sentences (no content removed except two redundancies: 'the accuracy of these predictions', 'demonstrating the applicability of the developed framework…'); pages unchanged. **Resumo trimmed to 1484 words (texcount; 1477 rendered; limits 1200–1500) — DONE:** removed the closing clause of the R-ratio paragraph ("verificando o cancelamento dos polos quando as diferentes contribuições são combinadas num observável físico") and "desenvolvido nesta tese" in the last paragraph; fixed "raíz" → "raiz". Pages unchanged. ~~(old text:)~~ **Abstract was ≈ 332 words (texcount) — max 300.** Trim ≈ 35 words (your writing). The Resumo is ≈ 1501 words (max 1500; right at the edge) — a few words of margin would be safer. (This also answers Prof's 15-Sep question: Resumo ≫ Abstract in length is REQUIRED for an English thesis.)
20. ~~Captions 10 pt → 9 pt; tables 11 pt → 9 pt~~ **DONE 21 Sep (batch 12):** `preamble.tex` now `\captionsetup{font={footnotesize,singlespacing},labelfont=bf}` (+ `[table]`/`[longtable]` `position=top`); every live table float in Ch2, Ch3, Ch4 and Appendix B got `\footnotesize` (the two `\small`/`\footnotesize` cases normalised), and the two longtables (Ch2 `tab:limits`, Ch4 `tab:masterValidation`) are `\footnotesize` too. (The front-matter list-of-symbols longtables were left alone.)
21. ~~Table captions above~~ **DONE 21 Sep (batch 12):** all 14 tables (2.1–2.3, 3.1–3.7, 4.1–4.2, B.1–B.2) have the caption ABOVE the table (spot-checked pp. 44, 51, 69 visually). Figure captions were already below. *Cosmetic leftover:* in the narrow columns of Table 4.2 the justified text leaves gaps ("Born-normalised"); add `\raggedright` if it bothers you.
22. The **font-leak bug** (batch 11) is therefore confirmed as a real violation (Ch5–Ch7, appendices and bibliography were 9 pt): keep the fix.

**F. Housekeeping**
18. Delete/archive when no longer needed: `backup_ch4_2026-09-21_pre-revert.tex` (only AFTER the Ch4 re-application is finished), the now-unused `chapters/06-future-work.tex` and `chapters/07-conclusion.tex`, and the "conflicted copy" files in the repo root.

**BATCH 4 — Ch6/Ch7 merged (21 Sep, late).** New file `chapters/06-conclusions-future-work.tex` (`\chapter{Conclusions and Future Work}\label{ch:conclusion}`): Prof's five Conclusion paragraphs, then bold unnumbered `\section*{Future Work}` with his five paragraphs, Fig. 6.1 (`\input{figures/next_architecture.tex}`, `fig:next`, now referenced as "Fig. 6.1") after paragraph 4. Transcribed literally (per Henrique's rulings: follow Prof on the massive-antenna claim and the Conclusion; the Ward-identity, MadGraph/NNLOJet and FeynCalc-analogy paragraphs are dropped). `main.tex` now includes the new file instead of `06-future-work` + `07-conclusion` (the two old files are untouched but unused — item 18). Ch1 roadmap: the two sentences on Ch6/Ch7 replaced by one ("Finally, Chapter 6 presents the conclusions … and discusses future directions … further antenna families and kinematic configurations …"; "general model support" no longer listed, as in Prof's text). Restored the `% TODO[Yours]` dΦ4 marker above `eq:phaseSpaces` (item 8; it had only lived in the backup). `ch:futurework` no longer exists: its only other reference is inside a dead `\iffalse` block of Ch2 (~l.1572) — build reports nothing undefined.
**Build 136 pp, 0 undefined refs. Page position: the merged chapter runs printed pp. 78–80; the Conclusions end on p.80 with only 7 of ~40 lines used (≈ 33 lines of slack), Appendix A starts p.81.** ⇒ We are at the limit but inside it. The Ch4 compression pass (family figure/table, boilerplate, §4.6 table merge) is what buys room for everything still to be re-applied in Ch4 (≈ +8 pp of Prof's prose).

**BATCH 5 — ALL master integrals except R3 moved to the new Appendix C (21 Sep, late; Henrique's decision after the compression discussion: "diagrams at the start, MI to their own appendix separated by family, R8a reinstated among the other MI, note at the start of Ch4, do NOT do the diagrams yet").**
New `appendices/appendixC.tex` (`\chapter{Master Integrals}\label{ap:masterIntegrals}`, included in `main.tex` after Appendix B): C.1 "$A$-Type Integrals of the $A_2^2$ Set" (`sec:MIA22`: A₂₂,LO, A₃, A₄, A₆), C.2 "$V$-Type Integrals of the $A_3^1$ Set" (`sec:MIA31`: V₅ₐ, V₅b, V₈), C.3 "$R$-Type Integrals of the Four-Parton Antennae" (`sec:MIfourParton`: R₄, R₆, **R₈ₐ with its full derivation restored inline** (`subsec:R8aMI`), R₈b). Text moved verbatim (the ex-`\subsubsection`s are now `\subsection`s; four internal references re-worded "Subsubsection"→"Subsection"; V₅ₐ's pointer now goes to R₈ₐ in C.3). The old Appendix B.6 (R8a derivation) removed — Appendix B is byte-identical to its pre-R8a-move version again. Ch4: the three "Master Integrals" subsections (§4.2.5, §4.3.4, §4.4.5) are gone; each family section ends with one line pointing to its appendix section; Table 4.1 intro carries the note; Prof's PV/IBP second paragraph re-pointed to Appendix C; §4.2.1's "three further steps" paragraph now cites Appendix C.1; Ch2 forward-reference ("MI … discussed in further detail in …") now points to Appendix C. **Kept in Ch4:** R₃ (inside the A₃⁰ worked example), the phase-space/normalisation front matter (needed by R₃ and §4.5) and the massive masters I₁, I₂ (§4.5.1). No results table was added to the main text (optional; costs ≈ ½ page).
**Build 137 pp, 0 undefined refs (0 reference/citation warnings). Page position: main text now ENDS printed p.75 (Conclusions' last page 7/40 lines), Appendix A starts p.76 ⇒ 5 pages + 33 lines below the 80-page limit** (Ch4 lost ≈ 5 pp; Appendix C is 9 pp, not counted). This is the room for re-applying Prof's antenna prose (measured at ≈ +6–7 pp for the full text, ≈ 4 pp for the "additive-minimal" version). Not yet done: Ch4 title/section retitling, unnumbering of subsubsections, chapter intro P1–P5, per-component origin sentences + interpretation comments, §4.5/§4.6 text, family-map figure (waiting for item 9).

**BATCH 6 — Ch4 step 1: Henrique's addition to the master-integral note + retitling + unnumbering (21 Sep, late).**
*Note (Henrique's request):* added to the Ch4 note (after the Table 4.1 intro): "Evaluating these master integrals analytically was a crucial part of this work: they were computed explicitly, with the derivations presented step by step in Appendix C, and were not merely taken from a source; the resulting expressions reproduce the analytic results known from the literature [5,13]." **Accuracy caveat → Yours item 12a.**
*Retitling per Prof's ToC (now matches line for line, checked in `main.toc`):* chapter "Calculation and Integration of QCD Antenna Functions"; 4.1 NLO Antenna Functions (4.1.1 Tree-Level Three-Parton Antenna $A_3^0$; 4.1.2 Local Infrared Validation of $A_3^0$ [new label `subsec:a30LocalValidation`]; 4.1.3 Virtual Two-Parton Antenna $A_2^1$); 4.2 NNLO Two-Parton Antennae: The $A_2^2$ Set (four components titled by symbol only); 4.3 NNLO Three-Parton Antennae: The $A_3^1$ Set (new label `sec:A31set`); 4.4 NNLO Four-Parton Antennae; 4.5 Exploratory Massive Extension (4.5.1 Massive $A_3^0$: Master Integrals; 4.5.2 Construction and Integration of the Massive $A_3^0$ — thesis wording kept, Prof's is "Build and Integrate of the Massive A30"); 4.6 Summary and Validation of the Calculated Antennae.
*Unnumbering:* all 8 remaining Ch4 `\subsubsection` → `\subsubsection*` (bold, no number, no ToC entry). The 5 labels on them were dropped and every reference repointed: Ch4 ("Every step of the analytic integration above…", "apart from the exception discussed below", massive build text → Subsection 4.1.1), Appendix B l.9 → `sec:matDerivation`, Appendix B l.705 → `subsec:a30LocalValidation`, Ch2 l.1760 footnote → `sec:matDerivation`. "A Note on Sources" (Breve) is only unnumbered so far — Prof wants it dissolved into the §4.2.4 text with his replacement paragraphs (step 2).
**Build 137 pp, 0 undefined refs / 0 reference warnings. Page position unchanged: main text ends printed p.75 (Ch4 = pp. 43–67, Ch5 starts p.68).** Room ≈ 5 pp + 33 lines.
**NEXT (step 2, Ch4 prose, additive):** chapter intro P1–P5; Δ4/Θ/Ω and normalisation wording; per-component origin sentence + interpretation comment (Prof's texts); §4.1 additions not yet applied (A₃⁰ description/Fig. 4.1 caption text, Born paragraph, "Implementation in AntCalc" set, local validation "(AP)"); §4.2.4 Breve "source" text; §4.5 and §4.6 context; family figure last (needs item 9).

**BATCH 7 — Ch4 step 2a (Prof's chapter/§4.1/§4.2-opener prose, additive; 21 Sep, late).** Applied verbatim from Prof's text (via the backup): chapter intro P1–P5 (P4 adapted: master integrals → Appendix C + the "derived explicitly" note + R₈ₐ example; P5 after Table 4.1); phase-space wording (Δ₄/Θ/Ω), normalisation paragraphs (S_Γ, S_Γ,2) and the LiteRed2 `j[…]` paragraph; §4.1 opener; A₃⁰ description + Fig. 4.1 caption + "Applying the QCD Feynman rules…" + "Summing the squared matrix element…" + Born-normalisation paragraph + "The electric charge cancels…" paragraph; "Implementation in AntCalc" set (IntegrableForm, "As with BuildAntenna…", BuildAndIntegrateAntenna, closing validation paragraph; HEAD's older duplicate closing paragraph removed); "(AP)"; §4.1.3 A₂¹ text (Passarino-Veltman → B₀/C₀, integrated-antenna lead-in, pole-cancellation comment; the old "PaVe scalar functions appear in both…" paragraph deleted as Prof said; `% TODO[Yours]` marker for "Fig. X" kept); §4.2 opener (two paragraphs). Also this turn: note + A₆ rewrite (item 12a).
**Build 138 pp, 0 undefined refs / 0 reference warnings. Page position: main text ends printed p.76 (Ch4 = pp. 43–68, Ch5 starts p.69), Appendix A starts p.77 ⇒ room ≈ 4 pp + 33 lines.** Cost of this step ≈ +1 page.
**NEXT (step 2b):** component-by-component text (origin sentence + IBP/master pointer + interpretation) for §4.2 (A₂², Ã₂², Â₂², Ă₂² incl. Breve source text), §4.3 (+ intro, Ã₃¹ pole-pattern explanation, Â₃¹ rewrite), §4.4 (+ intro, four components), §4.5 and §4.6 text; family figure last (item 9).

**BATCH 8 — §4.2 (the $A_2^2$ set) complete (21 Sep, late).** The four component subsections replaced by the approved text (Prof's leading/subleading/N_f/Breve lead-ins, IBP/master pointers, interpretation comments, the Breve "source" paragraphs dissolved into the text; the B₀/C₀-counterterm and Out[4]→Out[5] paragraphs kept), with every master-integral pointer redirected to Appendix C.1 (`sec:MIA22`). Two `% TODO[Yours]` markers for the "Fig. X" diagram clauses (A₂², Â₂²). **Build 139 pp, 0 undefined refs. §4.2 now = printed pp. 55–59 (≈ +1 page vs. the HEAD-structure version); main text ends p.77, Appendix A starts p.78 ⇒ room ≈ 3 pp + 33 lines.** Rough forecast for what is left in Ch4 (§4.3 ≈ +1.5, §4.4 ≈ +1, §4.5 ≈ +0.5, §4.6 ≈ +1, figure ≈ +0.5) ≈ +4.5 pp vs ≈ 3.8 pp of room ⇒ likely ~1 page short: fallbacks = merge/condense the two §4.6 tables (≈ −1), smaller/no results box repeats, or shorter interpretation comments. Preview PDF of §4.2 (pp. 55–60) sent to Henrique.

**BATCH 9 — §4.3 (the $A_3^1$ set) complete (21 Sep, late).** Section intro (three paragraphs, label `sec:A31set`), §4.3.1 leading colour (UV-renormalisation paragraph, post-reduction counterterm/R₃ sentence, interpretation of the poles up to 1/ε⁴), §4.3.2 subleading colour (+ Ã₃¹ pole-pattern paragraph = item 12d), §4.3.3 $\hat A_3^1$ rewritten per Prof (bare interference has no N_f term; non-zero antenna = N_f coupling counterterm ∝ A₃⁰; the In[3]/In[4] zero block removed; A2 finding recorded) — all from the approved text, master pointers → Appendix C.2 (`sec:MIA31`), "Section 4.1.1" wording → "Subsection". One `% TODO[Yours]` for the "Fig. X" clause (A₃¹). **Build 140 pp, 0 undefined refs. §4.3 = printed pp. 60–63 (≈ +1 page); main text ends p.78, Appendix A starts p.79 ⇒ room ≈ 2 pp + 33 lines.** Left in Ch4: §4.4 (≈ +1), §4.5 (≈ +0.5), §4.6 (≈ +1), figure (≈ +0.5) ≈ +3 vs ≈ 2.8 pp of room — borderline; §4.6 tables merge is the first fallback.
**Also owed (my promise on the MI move): Prof's EXPANDED master-integral texts** (A-type, V-type, R-type: factorisation explanations, V₅b propagator definition, real-part explanation Q17, R4/R6 rewrites, optical-theorem wording) currently exist only in the backup; Appendix C still holds the un-expanded HEAD texts. To be swapped into Appendix C after §4.4 (appendix pages are free).

**BATCH 10 — §4.4 (four-parton antennae) complete (21 Sep, late).** Section intro (four paragraphs: double-real context, process → A₄⁰/Ã₄⁰ and B₄⁰/C₄⁰, contrast with the loop antennae, how AntCalc builds them), §4.4.1 A₄⁰ (masters R₄, R₆ only — Q19/A4 answered), §4.4.2 Ã₄⁰ (adds R₈ₐ, R₈b), §4.4.3 B₄⁰ (R₄, R₆), §4.4.4 C₄⁰ (R₄, R₆, R₈b; interpretation: only the triple-collinear limit survives — Prof's claim; consistent with the Table 4.3 local-validation entry "single-collinear region regular; triple-collinear reproduced"), all with Prof's interpretation comments; masters → Appendix C.3 (`sec:MIfourParton`); normalisation reference → `eq:normN`; three `% TODO[Yours]` "Fig. X" clauses (A₄⁰, B₄⁰ … ). **Build 142 pp, 0 undefined refs. §4.4 = printed pp. 63–67 (≈ +1.5 pp). Main text now ends printed p.80 (Conclusions' last page 7/40 lines), Appendix A starts p.81 ⇒ AT the limit (33 lines of room).** Remaining Ch4 work costs ≈ +2 pp (§4.5 ≈ +0.5, §4.6 ≈ +1, figure ≈ +0.5) ⇒ ≈ 2 pp must be recovered first (candidates: move the four-particle phase-space/normalisation front matter — dΦ₄, Δ₄, Θ, Ω, S_Γ, S_Γ,2 — into Appendix C ≈ −0.7; merge the two §4.6 tables ≈ −1; shrink Ch5 figures ≈ −0.3; shorten interpretation comments).

**BATCH 11 — recovery steps 1 and 2 done, and a PRE-EXISTING FORMATTING BUG DISCOVERED (21 Sep, late). READ THIS.**
*Step 1 (done):* ALL phase-space front matter (shorthands dS_n, dΦ₂/₃/₄, Δ₄, Θ, Ω, S_Γ, S_Γ,2, P₂; incl. the dΦ₄ `TODO[Yours]`) moved from the start of Ch4 into a new **Appendix C.1 "Phase-Space Notation and Normalisation"** (`sec:MInotation`; the A/V/R sections are now C.2–C.4). At R₃ (Ch4 §4.1.1) the two- and three-particle measures dΦ₂, dΦ₃ are quoted right after the R₃ definition with the pointer "the shorthands … are defined, together with the four-particle measure, in Appendix C"; the S_Γ and massive-section references now say "of Appendix C.1". Saved ≈ 1 page.
*Step 2 (done):* the two §4.6 longtables merged into ONE (`tab:masterValidation`; the old labels `tab:masterValidationConstruction` and `tab:masterValidationStatus` are kept as aliases so the §4.5 reference still works): columns Route (+process +API call) | Construction and integration | Literature (order) | Validation (local/global) | Status; every cell of both tables preserved; both captions merged. Prof's §4.6 intro (3 paragraphs) applied and adapted to one table ("Table 4.2 summarises, for each antenna, the construction and integration method together with the corresponding validation evidence").
*THE BUG:* in the original `chapters/04-worked-example.tex` the §4.6 table block started with a bare `ootnotesize` (and `
enewcommand{rraystretch}` ) that was never closed. Because `\include` does not group, **everything after it — Ch5, Ch6, Ch7, the appendices and the bibliography — was typeset in ootnotesize (≈ 9 pt instead of 11 pt)** in every PDF up to now, including the version Prof read. Verified from the original `main.pdf` (git HEAD): median word height 8.71 pt in Ch3/Ch4 vs 7.16 pt in Ch5–Ch7. I fixed it by wrapping the table in `{ootnotesize … }`.
*Consequence:* **every page count reported before this point for Ch5–Ch7 was flattered by ≈ 3–4 pages.** Correct fonts: Ch5 ≈ 6.2 pp (p.72–78), Ch6 = pp. 79–81; **main text now ends printed p.81 (31/42 lines), Appendix A starts p.82, total 155 pp ⇒ ≈ 1.7 pp OVER the 80-page limit**, and §4.5 text (≈ +0.5) and the family figure (≈ +0.5) are not in yet ⇒ ≈ 2.8 pp to recover. The original thesis (79 pp) was really ≈ 83 pp at a uniform font. Appendices are also now 11 pt (they were tiny).
*Decision pending (Henrique):* keep the correct fonts (recommended) and trim ≈ 3 pp, or restore the leak (not recommended: inconsistent body font in Ch5–Ch7, likely against FCUL formatting norms). Trim candidates: Ch5 figures 0.8→0.6 width, drop the In/Out box in §5.5, condense Prof's §5.7 summary and T-object equations (≈ −1.4 total); Ch6 figure scale (≈ −0.2); Ch4: Table 4.1 → Appendix C (≈ −0.4), shorter interpretation comments; more relocations to appendices.

**BATCH 12 — FCUL compliance: captions 9 pt single-spaced, tables 9 pt, table captions above (21 Sep, late; Henrique: "go ahead with steps 1–4").** Details in items 20–21. Backup of the pre-compliance sources: scratchpad `pre_compliance/` (may vanish; `git diff` shows everything). **Build 155 pp, 0 undefined refs. Measured page effect: none visible at page granularity** (chapter starts unchanged: Ch4 p.43, Ch5 p.72, Ch6 p.79, Appendix A p.82; main text ends p.81 with 31/42 lines). **Chapter-end slack is what matters, measured:** Ch5's LAST page (p.78) holds only ≈ 8 lines ⇒ trimming ≈ 8–10 lines from Ch5 (e.g. the approved "shrink the two Ch5 figures 0.8 → 0.6", or dropping the In/Out box) saves a whole page ⇒ the main text would then end on **p.80 (≈ 31/42 lines) = inside the limit**. Ch4's last page (p.71) is only 17/42 used ⇒ ≈ 25 free lines, enough for the §4.5 text (≈ +20 lines). The family figure (≈ 17–20 lines) would push Ch4 onto a new page and the end back to p.81 ⇒ it needs an offset of ≈ 31+ lines (e.g. Ch6's last page has 31 lines: tightening Ch6 by that much drops a page).
**NEXT (waiting for Henrique's go):** (a) shrink Ch5 figures / trim ≥ 8 lines of Ch5 (the still-approved step 3 of the recovery list, interrupted by "stop"); (b) §4.5 text; (c) figure decision.

**BATCH 13 — ALL of Prof's notes now incorporated except what needs Henrique (21 Sep, late; Henrique: "before any compressing, first incorporate all of the prof's notes").**
Applied this turn: (a) **§4.5** from the approved text (framing paragraph, kinematics and threshold variable r, cut propagators D₁–D₃, the LiteRed2 J₁,J₂ family, the J→I basis change with the −1/4 factor (TODO), §4.5.1 with the massive phase-space measure dΦ_X and I₁, I₂, §4.5.2 build/integrate text and the "single 1/ε pole" comment; `m_q` kept); (b) **Prof's EXPANDED master-integral texts in Appendix C** (A-type intros, the V-type intro incl. the real-part explanation Q17 [Claude-drafted, item 11], V₅ₐ factorisation / V₅b propagator definition / V₈ box, the R-type intro, R₄ (five tripole variables, Beta functions), R₆ and R₈b (optical theorem), R₈ₐ (his step-by-step version, Bateman marker)); A₆ kept as the Mellin–Barnes + exact-form version. So every Prof review point (first + second Ch4 batch, Ch5, Ch6/7) is now in the source, EXCEPT: the "Fig. X" diagram clauses (6 markers: A₂¹, A₂², Â₂², A₃¹, A₄⁰, B₄⁰), Q22/C1 in §4.5, the 5.Y1 bridging sentence, the Bateman citation, the dΦ₄ prefactor — all `% TODO[Yours]` in the source (11 markers: 8 in Ch4, 2 in Appendix C, 1 in Ch5).
**Build 157 pp, 0 undefined refs (Appendix C grew by 2 pp — uncounted). Main text unchanged: ends printed p.81 with 31/42 lines ⇒ still ≈ 1 page and 31 lines over 80; Ch4 = pp. 43–71 (last page now full), Ch5 p.72–78 (last page ≈ 8 lines), Ch6 p.79–81.** So the true remaining problem, with all notes in: get from p.81 (31 lines) to ≤ p.80, i.e. ≈ 31 lines + whatever the family figure adds (≈ 17–20 lines, needs Henrique's diagram choice).

**BATCH 14 — three of the remaining Yours points closed (21 Sep, late; Henrique's explicit requests).**
*Q22 (−1/4):* Henrique's explanation ("what I found differed between my convention and theirs; my J did not match their I by −1/4; presumably basis choice") turned into one sentence in §4.5 (before the transformation equation): "This factor multiplies both of the relations below and reflects the different normalisation conventions of the two sets of masters rather than any physical effect; it was found by comparing J₁ with I₁." **Claude's nuance to confirm:** it is a *normalisation (measure) convention*, not a basis change — the basis change is the matrix (a, b) in the equation; the code calls it `MassiveA30IntegratedCutMeasureFactor` (I_paper = C_cut·j_MX30) and it multiplies both I₁ and I₂ relations. TODO marker removed.
*Bateman citation:* `references.bib` + `\cite{Erdelyi:1953HTF}` (Erdélyi–Magnus–Oberhettinger–Tricomi, *Higher Transcendental Functions*, vol. 1, Bateman Manuscript Project, McGraw-Hill 1953) at the quadratic transformation in Appendix C.3 (R₈ₐ). The identity ₂F₁(1,b;2b;4Z/(1+Z)²)=(1+Z)²₂F₁(1,3/2−b;b+½;Z²) was verified numerically to 30 digits at three points. Section/equation number in Erdélyi NOT checked (believed §2.11, "quadratic transformations") — none printed. Marker removed.
*Bridging sentence (5.Y1), drafted by Claude at Henrique's explicit request (single-use):* "The only exceptions are Ã₄⁰ and C₄⁰: their integrated counterparts are defined in the literature with an explicit overall factor of 2 and ½, respectively, and the corresponding terms of Eq. (5.15) therefore carry the compensating factors ½ and 2, as discussed in Section 5.4." inserted before "We denote the resulting integrated combinations by 𝒯-objects". Marker removed.
*Pending Henrique's yes/no:* (C1) §4.5.2 — proposed wording "The massive IBP reduction described above was carried out with LiteRed2, and its result, converted to the literature masters through Eq. (4.84), reproduces the analytic expression of Ref. [25]. In the default configuration, AntCalc returns this result directly as a stored closed form, evaluated and expanded in ε." Reason: in the release code the default massive call returns the encoded closed form (`MassiveA30IntegratedRuntimeClosedExpression`, bridge status "InvariantRenamePlusDerivedMX30CutMeasure"); the LiteRed2/J route runs only when `$MassiveA30ForceIBPMasterRoute = True` (used to obtain and cross-check the J₁,J₂ coefficients). (dΦ₄) recommendation: change the printed exponent (q²)^{3−d/2} to (q²)^{(2−d)/2}: dimensional analysis — [Φ₄] = mass^{3d−8}, the remaining factors of the measure carry mass^{4d−10}, so the prefactor must be (q²)^{(2−d)/2}, the same as dΦ₃; the paper's Eq. (A.5) exponent is inconsistent with Δ₄ as defined; R₄…R₈b are unaffected (they carry their own q²-powers, consistent with mass^{3d−8}).
*Diagrams:* Prof (21 Sep) insists — "5 figuras com 3 ou 4 diagramas na horizontal cada" for A₂¹, A₂², A₃¹, A₄⁰, B₄⁰, in the chapter rather than the appendix. Decision: include them FIRST, then trim (Henrique). Page state unchanged: main text ends p.81 (31/42 lines).

**BATCH 15 — C1 and dΦ₄ applied (Henrique: "yes apply both").** *C1 (§4.5.2):* Prof's sentence replaced by the accurate version — "The massive IBP reduction described above was carried out with LiteRed2, and its result, converted to the literature masters through Eq. (4.84) [label `eq:massiveMasterMap`], reproduces the analytic expression of Ref. [25]. In the default configuration, AntCalc returns this result directly as a stored closed form, evaluated and expanded in ε." Table 4.2 massive row now says "(default call returns the resulting stored closed form)". *dΦ₄:* exponent corrected to (q²)^{(2−d)/2} in all four places in Appendix C (eq:phaseSpaces, R₄, R₆, R₈ₐ) plus a 3-line remark after the Δ₄ equation (mass dimension 3d−8; Ref. [13] Eq. (A.5) prints 3−d/2, inconsistent with Δ₄ as defined). Checked: with (q²)^{(2−d)/2} the R₄ definition reproduces its stated power (q²)^{2−3ε}; the printed exponent would give (q²)^{4−3ε}. **Build 157 pp, 0 undefined refs; page state unchanged (main text ends p.81, 31/42 lines).**
*Diagram plan (Prof: 5 figures × 3–4 diagrams: A₂¹, A₂², A₃¹, A₄⁰, B₄⁰, in the chapter): proposal sent to Henrique; ≈ 0.19 page per figure ⇒ ≈ 1 page total. Arithmetic: end p.81 (31 lines) + ≈ 1 page ⇒ p.82; to end ≤ p.80 need ≈ −75 lines: Ch5 −8 lines (drops a page) and Ch6 −31 lines (drops a page) would do it.*

**BATCH 16 — the five diagram figures are IN (21 Sep, late; Prof: "5 figuras com 3 ou 4 diagramas"; Henrique: B₄⁰ and C₄⁰ side by side, at the start of each section).** New files `figures/diagrams-a21.tex`, `-a22`, `-a31`, `-a40`, `-bc40` (tikz-feynman, absolute coordinates, `\scriptsize` labels, single caption each, 9 pt). Placement: Fig. 4.2 (A₂¹) at the start of §4.1.3; Fig. 4.3 (A₂²) after the §4.2 opener (referenced from §4.2.1 and, panel (d), from §4.2.3); Fig. 4.4 (A₃¹) after the §4.3 intro; Figs. 4.5 (A₄⁰) and 4.6 (B₄⁰ | C₄⁰, dashed separator) after the §4.4 intro. All six "Fig. X" clauses now cite the real figures; **no `TODO[Yours]` left in Ch4/Ch5**. **Cost ≈ 1.6 pages (Ch4 now pp. 43–73, was 43–71).** **Build 159 pp, 0 undefined refs. Main text now ENDS printed p.83 (Ch5 pp. 74–80, Ch6 pp. 81–83, 31/42 lines on the last page) ⇒ 3 pages over the limit.** Chapter-end fills (what each chapter must lose to drop a whole page): Ch4 last page 25 lines, Ch5 last page ≈ 8 lines, Ch6 last page ≈ 31 lines ⇒ ≈ 64 lines in total would bring the end to p.80.

**BATCH 17 — diagram check against the code; Fig. 4.2 (A₂¹) corrected (21 Sep, late; Henrique: "are the diagrams good representations? A₂¹ and self-absorbed gluons — WF corrections/tadpoles?").** Henrique was right: the two external-leg self-energy panels are *wave-function corrections* (FeynArts `WFCorrections`), not tadpoles, and **AntCalc excludes both by design**: `src/engines/amplitudes_loop.wl:139` (one loop) and `:251` (two loop) call `InsertFields[CreateTopologies[…, ExcludeTopologies -> {WFCorrections, Tadpoles}], …]`; physically they are scaleless for massless on-shell legs (UV and IR poles cancel, LSZ factor 1). So A₂¹ = the vertex diagram only ⇒ **Fig. 4.2 is now a single diagram** (`figures/diagrams-a21.tex`) whose caption states that the external-leg corrections are scaleless and vanish. (I have not looked for a diagram-level statement in hep-ph/0505111; it defines A₂¹ through the one-loop matrix element.) Cross-check of the other figures against the profiles/generators: loop routes exclude scalars S[_], electroweak vectors V[1..3] (internal) and EW ghosts U[1..4] but keep the gluon ghost; tree-level B/C routes exclude V[1], V[2], S[_] (only gluon exchange) — consistent with Figs. 4.3–4.6; internal self-energy insertions (A₂² panel (c), A₃¹ panel (d)) are non-scaleless and are generated; the photon→closed-quark-loop→gg singlet class is not drawn (vanishes by Furry's theorem). **Build 159 pp, 0 undefined refs; page state unchanged (main text ends p.83).**

**BATCH 18 — diagrams verified against the ACTUAL FeynArts output (21 Sep, late).** Generated the real sets with AntCalc's own settings (`wolframscript`, `Get[AntennaPipeline.wl]`, `InsertFields[CreateTopologies[L,1->n, ExcludeTopologies->{WFCorrections,Tadpoles}], {V[1]}->…, Model->"SMQCD", ExcludeParticles->{S[_],V[1],V[2],V[3],U[1..4]}]`; scratchpad `fa/`). Counts: **A₂¹** 1 topology (2 class-level diagrams; 6 topologies/12 diagrams without the exclusions) = the vertex diagram only; **A₃¹** 10 topologies/20 diagrams (29/58 without); **A₂²** 8 topologies/16 diagrams. Findings: (i) every panel in Figs. 4.3–4.4 has a counterpart in the generated sets — A₂²: planar ladder (#5), crossed ladder (#6), internal quark self-energy insertion (#12, #17), N_f quark bubble on the gluon (#13–14; also ghost (#15) and gluon (#16) loops); A₃¹: internal-propagator self-energy (#14, mirror of my panel (d)). (ii) **Henrique's suspicion about panels (c)/(d):** they are *internal-propagator* self-energies, NOT wave-function corrections (those are on external legs) — FeynArts keeps them and they are non-scaleless (the propagator carries off-shell momentum), so they do contribute; the excluded ones are the external-leg WF corrections and tadpoles (scaleless ⇒ 0). (iii) The generated sets also contain the "singlet" diagrams (photon → closed quark loop → two gluons; A₂² #1–4, A₃¹ #5–8), which cancel pairwise between the two loop orientations (Furry's theorem) and are not drawn; and A₂² #10 is a gluon self-energy with a four-gluon-vertex tadpole loop, scaleless ⇒ 0. (iv) **Note added to Figs. 4.3 and 4.4 captions** (Henrique's request; my wording): "…which contributes. Self-energy corrections on the external legs and tadpoles are not generated: they are scaleless for massless on-shell partons and vanish in dimensional regularisation." Fig. 4.2 (A₂¹) already had the note. **Build 159 pp, 0 undefined refs; page state unchanged (main text ends p.83).**

**BATCH 19 — first trim applied: Ch5 In[2]/Out[2] box removed (21 Sep, late; Henrique: "go ahead with the removal").** §5.5 now says "…the calculation is performed with `BuildRRatio[SMQCD]`." inline (Prof's text showed only the In line; the Out duplicated Eq. (5.17)). **Ch5 = pp. 74–79 (6 pages, was 7); Ch6 = pp. 80–82; main text now ENDS printed p.82 (Appendix A p.83) ⇒ 2 pages over the limit.** **Build 158 pp, 0 undefined refs.** Remaining chapter-end slack: Ch4's last page (p.73) holds 25 lines, Ch6's last page ≈ 31 lines ⇒ cutting ≥ 25 lines from Ch4 and ≥ 31 lines from Ch6 brings the end to p.80 exactly. Candidates: Ch4 — Table 4.1 → Appendix C (≈ −14) + shorten a few interpretation comments (≈ −11); Ch6 — Fig. 6.1 smaller (≈ −5) + tighten two of Prof's paragraphs (≈ −26). Ch5 is now full (its last page ≈ 48 lines incl. figure).

**BATCH 20 — PAGE BUDGET MET: main text ends printed p.80 (21 Sep, late).** (1) *Table 4.1 (master-integral overview) moved to Appendix C* (now **Table C.1**, right after the Appendix C intro; label `tab:MI` kept; the Ch4 intro says "Table C.1 of Appendix C summarises the master integrals…"; the §4.6 table's A₃¹ row still cites it). Henrique agreed. (2) The move alone did NOT drop a page: measured whitespace at the bottom of Ch4 pages showed ≈ 45 lines of gaps (tall unbreakable equation blocks at pp. 45–46, the §4.6 longtable not fitting at p.71, …) that absorbed the saved lines. (3) The §4.6 table got `rraystretch{1.0}` and ragged-right cells (layout only). (4) **Global layout tweak in `preamble.tex`: display-equation spacing 11 pt → 8 pt (`\abovedisplayskip`/`\belowdisplayskip`, and the short skips), hooked into `\normalsize`; the FCUL norms do not fix this spacing.** Effect: Ch2 −1 page (Ch3 now starts p.28), Ch4 −1 page (Ch5 starts p.72), Ch6 = pp. 78–80. **Main text now ends printed p.80 (last page ≈ 31/42 lines; ≈ 13 free lines), Appendix A starts p.81; total 155 pp; 0 undefined refs.** Fig. 6.1 and all of Prof's Ch6 text stay untouched (Henrique: the figure is needed). Visual check of equation-heavy pages: fine. **Slack is only ≈ 13 lines: any further addition needs an offset.** Old plan items (Ch6 cuts, Ch4 comment trimming, Ch5 Fig. 5.1 removal) are no longer needed. *Open cosmetic issue:* the List of Tables prints the full long captions (e.g. Table 4.1, A.?) — add `\caption[short title]{…}` to the long ones (front matter, no cost to the 80 pages).

**BUILD NOTE (22 Sep): in-place builds inside Dropbox keep failing (locked/0-byte `main.aux`). Use: `OUT=/private/tmp/claude-501/thesis_build; mkdir -p $OUT/{chapters,appendices,frontmatter,figures}; latexmk -pdf -interaction=nonstopmode -outdir=$OUT main.tex; cp $OUT/main.pdf main.pdf`. Last good build: 156 pp, main text ends p.80.**

**BATCH 21 — front matter finished (21 Sep, night).** English Abstract 290 words, Resumo 1484 words (see item 19). **Agradecimentos written by Henrique (front matter, pp. i–ii, ≈ 990 words) and typo-checked by Claude:** applied only unambiguous corrections (desenvolvemos junto → desenvolvamos juntos; de dia para dia → no dia a dia; grade → grande; assídua → assiduamente; missing commas around relative/parenthetical clauses; "a Raquel, e Filipa, o Edu, e o camarada Neves" → "a Raquel, a Filipa, o Edu e o camarada Neves"; "a quem … não foram mencionados" → "a todos os que, por puro esquecimento, não foram mencionados"; "jardim de Éden / em outros" → "jardim do Éden / noutros"; "aonde … a na qual" → "em que / aquele em que / onde"). **Deliberately untouched (voice):** ganda tropinha, memo, becas, besi na beh, jolas, grind no MM, look up to, Susana(s), gangue do tremoço. **UPDATE (Henrique's decisions, same night): all applied — added a concrete line on Prof. Pires's detailed revision rounds and swapped the 2nd "prazer" for "privilégio"; added "pelo tempo dedicado à leitura desta dissertação" for Prof. Sampaio (Claude's neutral wording — assumes he read the thesis); cut only the "nem sequer do nome me lembro" part of the mentee line; fixed the Raquel sentence ("a Raquel que, ainda que não seja minha mentoranda, é como se fosse"); "Professor João Pires" capitalised; "nos 10.º e 11.º anos"; "fresquinhas". Bia duplication: ignored on purpose. Build hiccup twice (transient corrupted .aux / PDF while Dropbox syncs) — clean rebuild fixes it; consider building outside Dropbox.** ~~Left for Henrique to decide:~~ (i) "Bia" appears twice (Miguéis paragraph and "à Bia e à Joana") — same person?; (ii) the mentee "que nunca apareceu e nem sequer do nome me lembro" (suggest cutting); (iii) garbled sentence about Raquel ("já ainda que não minha mentoranda") — suggested "a Raquel que, ainda que não seja minha mentoranda, é como se fosse"; (iv) "prazer" ×3 in the first paragraph; (v) professor/Professor capitalisation; (vi) "nos décimo e décimo-primeiro anos" → "nos 10.º e 11.º anos"; (vii) "frequinhas" (typo for "fresquinhas" or intended?). A corrupted `.aux` (0-byte `main.aux`) broke one build; fixed by deleting generated files. **Build 156 pp (the +1 is the acknowledgements in the Roman-numbered front matter); main text unchanged: ends printed p.80.**

**BATCH 22 — appendix structural review + 4 fixes applied (22 Sep 2026, not from Prof; Henrique asked for a general read-through of the appendices).** Read all of Appendix A/B/C for structure, cross-reference placement and readability. **Applied [Mine]:** (1) `appendixA.tex` l.37, $A_2^1$ subsection: `BuildAntenna[A,2,0]`→`BuildAntenna[A,2,1]` (copy-paste leftover from the $A_2^0$ template). (2) `appendixB.tex` l.801, garbled sentence ("defined the arg. quantity here was found to be" — duplicated "here", and `arg.` didn't match the `ang.` name used two paragraphs earlier for the same spin-correlation term) — rewritten, renamed back to `ang.` throughout. (3) `appendixB.tex` §"The Massive Extension" (l.426) had no `\label{}` and was referenced from nowhere in the thesis, despite covering the general massive-kinematics/quasi-collinear background that Ch4 §4.5 draws on — added `\label{sec:massiveExtension}` and a forward-pointer from §4.5's opening paragraph (`chapters/04-worked-example.tex`). (4) `appendixB.tex` §4.5.2-analogue (quasi-collinear AP kernel, l.1163): the printed $P_{Qg\to Q}(z)=\frac{1+z^2}{1-z}$ omitted a $-2\mu_{Qg}^2$ mass term that the Mathematica check right below it actually verifies against — replaced with a gathered two-line equation showing the $\mu_{Qg}^2$-corrected kernel and its $\mu_{Qg}^2=0$ (strict massless) reduction, per Henrique's exact instruction on the equation layout. **Not applied / flagged only:** Appendix B ("Supporting Derivations") has no chapter-level intro paragraph unlike A and C (it's a grab-bag of relocated content from several earlier tasks) — a one-paragraph intro would help but wasn't written (no explicit ask). **Build 156 pp, 0 undefined refs** (stale-`.aux` fatal error hit again on the first rebuild — same known failure mode as the 14/21 Sep notes; `find . -name "*.aux" -delete` + removing `main.toc/bbl/bcf/blg` fixed it). Uncommitted as of this entry.

### Prof. Pires — FULL Ch4 + Ch5 + Ch6/7 review email re-pasted, 21 Sep 2026 (late evening, AFTER the Ch4 revert) — INTAKE ONLY (batch records 1–21 above)

**Repeat check (done first).** The Ch4 part of this email (chapter title → §4.6, both "halves") is a **verbatim repeat** of the two Ch4 batches already logged below
(Q1–Q23, applied/open lists B/C, Y1–Y12). Nothing new in Ch4. **New in this email: Ch5 and Ch6/Ch7.** State of the tree: Ch4/AppB/Ch2 = HEAD `4133d64` (none of Prof's Ch4 text is in the
file; everything is in `backup_ch4_2026-09-21_pre-revert.tex`); Ch3 + Ch5 edits of 21 Sep kept (Ch5: ½Ã40 / 2C40 in the r_NNLO equation + justification paragraph).

**PAGE BUDGET (hard limit 80 main-text pages; Resumo/Abstract/appendices excluded).** Measured on the current `main.pdf` (134 pp): Ch1 starts PDF p.16 = printed p.1; Conclusion is PDF p.94 = printed p.79
(28 of ~40 lines used); Appendix A = PDF p.95. **Slack ≈ 12 lines + 1 page ≈ 1.3 pp.** Per-chapter now: Ch4 PDF 58–87 (30 pp), Ch5 88–91 (4 pp, last page full), Ch6 92–93 (p.93 only 14 lines), Ch7 p.94.
Cost of applying everything Prof asked (**Ch4 measured +10 pp** — 4.1 +1, 4.2 +3, 4.3 +3, 4.4 +3, 4.6 +1; **Ch5 estimated +1 pp**, Prof's text ≈ +650 words vs. live, 2 figures unchanged; **Ch6+7 estimated +1 pp**, Prof's text ≈ 1350 words vs. ≈ 800 live, merge saves only the ≈0.6-page gap left by the page break) ≈ **+12 pp against 1.3 pp of slack ⇒ ≈ 11 pp must be moved out of the main text.**
Candidate offsets (from the earlier measurement, still valid): R4–R8b derivations → appendix (−3/−4); A30 IBP example + J-steps → appendix (−2/−3); §4.6 tables → condense/appendix (−1.5/−2); compact In/Out boxes (−1/−2); Ch5 smaller figures / tighter summary (−0.5/−1). Sum ≈ −8 to −12: barely enough, no room for extra Feynman-diagram figures unless one compact multi-panel figure (≈ ⅛–⅓ page). **Rule stays: corrections first (page-cheap), prose only against a measured offset; re-measure after every batch.**
**DECISION NEEDED (Henrique): which of the offsets above, or negotiate with Prof that some "expand" requests go to an appendix.** Nothing below is applied until this is settled.

**Ch5 — new points (Prof's Ch5 notes), grounded against the live `05-validation-r-ratio.tex` and `main.pdf`:**
- *Prof's only question in Ch5:* "(I think you should have an expansion in α_s/2π, as in AntCalc?)" — **Q24.** Found: live Ch5 is internally inconsistent. Eq. for `R_hadr` (l.29) expands in α_s/π with c_LO/c_NLO/c_NNLO, while the assembly (l.228) and `BuildRRatio[SMQCD]` `Out[2]` use α_s/2π (the antenna normalisation). Prof is right; his replacement (R_had = R⁽⁰⁾R(α_s), R = 1 + (α_s/2π) r_NLO + (α_s/2π)² r_NNLO) removes the mismatch. Viva answer: α_s/2π is the natural expansion parameter of the antenna normalisation (Ch2); NLO coefficient (3/2)C_F ↔ α_s/π for N=3 (checked: (α_s/2π)(3/2)(4/3) = α_s/π).
- **Numbers checked:** Eq. (5.16) coefficient reproduces the known 1.9857 − 0.1153 N_f for N=3 (recomputed: 730/48 − 11ζ₃ = 1.9857…, N_f term −11/12 + 2ζ₃/3 = −0.1153). Prof's Fig. 5.1/5.2 captions match the real figures (`figures/rratio-full-domain.pdf`: shaded low-energy/threshold/Z regions; `figures/rratio_talk.pdf`: grey c c̄ and b b̄ bands + lower R/R_LO panel). His [24] = Gehrmann-De Ridder:2004ttg (0403057), [31] = Dine:1979qh, [32] = Adler1974, [33] = Tanabashi:2018oca (PDG), [13] = 0311276 — use the bib keys, not his numbers.
- **CONFLICT 1 (important): Prof's Eq. (5.15) has unit coefficients on Ã40 and C40** (from the old PDF). The live, verified-against-0505111/0403057 version is `−(1/N)(… + ½Ã40 + 2C40 − …)` + the justification paragraph. **Keep ours** (same kind of deviation as A6/Q5); tell Prof. Consequence: his 5.2 closing sentence "the antenna functions … are replaced by their integrated counterparts" is no longer literally true → needs one bridging sentence pointing to the ½/2 paragraph in 5.4 [Yours, 1 sentence].
- **CAVEAT 2:** Ch5's central claim (poles of the sum cancel; `BuildRRatio[SMQCD]` reproduces Eq. 5.16) is only as good as the unresolved A22 Leading/Subleading regression on `c5df6d3` (first-batch block, item 5). Text is Prof's and stays; do not send Ch5 as "validated" until Henrique closes it.
- **Mechanics:** Prof's LaTeX is corrupted in pasting (lost `_`, `\\`, `{`) — transcribe equations from the live file, not from his email. His 5.2 is a `\section`, so `\ref{subsec:Tobjects}` now renders "Section 5.2": fix the word "Subsection" in the one live Ch3 reference (`03-package-framework.tex:702`; the l.728 one is inside `\iffalse`, Ch2 l.1489 too). New order 5.1 Definition → 5.2 Assembly (T-objects) → 5.3 NLO → 5.4 NNLO → 5.5 Implementation → 5.6 Data → 5.7 Summary is a relocation of existing content; dependency grep done: inbound refs only `subsec:Tobjects` (Ch3 l.702 live) and `ch:validation` (Ch1, Ch3, Ch4 — chapter-level, unaffected); labels `sec:rratioData`, `fig:rratioFullDomain`, `fig:rratioContinuumOrders` only used inside Ch5. The dead `\iffalse` T-object block (l.123–219, holds `eq:integratedT`) stays untouched.

**Ch6/Ch7 — new points:**
- Merge into ONE chapter "Conclusions and Future Work" (= Chapter 6; Chapter 7 disappears). Inbound refs checked: `ch:futurework`/`ch:conclusion` only in **Ch1 roadmap l.85–89** (live, must be rewritten: also mentions "general model support", which Prof's Future-Work text no longer lists) — Ch2 l.1572 is inside `\iffalse`. `main.tex` `\include` list needs the edit. Fig. 6.1 (`fig:next`) keeps its number (still Chapter 6). "Future Work" becomes a bold, unnumbered heading.
- **CAVEAT 3 (Future Work, "some of the required massive antenna functions remain to be calculated… genuinely new ingredients"):** a quick literature search found contradicting-looking work — Abelof & Gehrmann-De Ridder JHEP 04 (2011) 063 (massive NLO/NNLO framework); Abelof, Dekkers, Gehrmann-De Ridder on massive double-real antennae incl. JHEP 04 (2012) 076 and arXiv:1210.5059; the S→Q Q̄ gg real-radiation antennae JHEP 10 (2013) 161; and the NNLO q q̄→t t̄ subtraction (arXiv:1404.6493, 1506.04037) with integrated real-virtual/double-virtual massive antennae. **I have NOT read these; I cannot say which final-final massive antennae are actually missing.** The sentence as Prof wrote it is a viva risk (an examiner from the field will know these) → [Yours]: check the papers / ask Prof exactly what he means, then soften ("some … are not yet implemented in an automated framework", or similar).
- **CAVEAT 4 (Conclusion, massive A30 sentence):** "required … a transformation between the master-integral basis produced by the IBP reduction and that used for the known analytic results" is the same issue as open item C1 of the Ch4 second batch: the *default* massive integrated route returns the encoded literature closed form; the LiteRed J-route runs only when forced (it calibrates the −1/4). Whatever wording Henrique chooses for Ch4 §4.5.2 must be repeated here [Yours, follows C1].
- **CAVEAT 5:** "unintegrated antenna functions were compared with established results in the literature" — check against the Ch4 validation table before sending (repo's own provenance audit calls the NNLO A31/A22 comparison regression evidence, not independent validation). Verify, don't assert.
- **What Prof's text drops (editorial, [Yours]):** the FeynCalc-analogy motivation paragraph of Ch7; and in Ch6 the "user-defined routes + Ward-identity certificate" paragraph and the MadGraph/NNLOJet paragraph. Ch3 l.781 already marks the Ward-identity material as overlapping `subsec:wardAndRelease`; NNLOJet numerical validation was judged circular on 28 Aug. Recommendation: drop as Prof did (saves ≈ ½ page), but it is Henrique's call. Old text goes into a trailing `\iffalse` block (house convention), not deleted.

**Classification.**
*[Mine] (literal Prof text / relocations / mechanical — executed only after the page-budget decision, page-neutral first):*
- **5.M1** chapter title `$R$`-ratio italic (use `\texorpdfstring`); **5.M2** intro paragraph; **5.M3** reorder to 5.1–5.7 + labels; **5.M4** 5.1 text + α_s/2π expansion (Q24); **5.M5** 5.2 text (T-object equations from live file); **5.M6** 5.3 NLO text; **5.M7** 5.4 text **with our ½Ã40/2C40 equation kept** and the justification paragraph moved under it; **5.M8** 5.5 text; **5.M9** 5.6 text + the two new captions; **5.M10** 5.7 Summary; **5.M11** Ch3 l.702 "Subsection" → "Section".
- **6.M1** merge into one chapter, remove `07-conclusion` from `main.tex`, old text → `\iffalse`; **6.M2** Conclusions text (Prof's 5 paragraphs, minus the massive-A30 sentence until 6.Y2 is settled — verify against Table 4.3, CAVEAT 5); **6.M3** "Future Work" bold-unnumbered + Prof's 5 paragraphs (minus the massive-antenna "genuinely new" sentence until 6.Y1); **6.M4** Ch1 roadmap rewrite (2 sentences).
- **4.M\*** the whole Ch4 re-application = Mine per the earlier classification correction, in this order: (a) page-cheap corrections (list in the "REVERTED" block: Eq. 4.7, colour indices, dΦ3, R3 d-dim line, J-steps, Γ̄, A6 exact, R8a 6/ε⁴, R6 (−q²−i0), S_Γ,2 in R4/R6/R8a/R8b, N_f Out[4] halved, Table 4.1 †/caption, Table 4.2 rows, PV/IBP paragraph consistent with kept Ch3); (b) length-neutral Prof paragraphs (title, chapter intro, notation, section titles/ToC, unnumbered subsubsections + repoints); (c) expansions/interpretation comments **only against measured offsets**.
*[Yours]:*
- **5.Y1** one bridging sentence (integrated T-objects vs ½/2 factors, CONFLICT 1); **6.Y1** massive-antenna "genuinely new" claim (CAVEAT 3); **6.Y2** massive-A30 wording in Conclusion = Ch4 C1; **6.Y3** what to keep of the dropped Ch6/Ch7 paragraphs; **P0** the page-budget strategy above; plus all still-open items of the Ch4 block (C1–C7, Y3/Y4/Y8/Y9, the Lead/Sub regression, the Feynman-diagram decision) — unchanged.

**BATCH 1 — Ch4 page-cheap corrections APPLIED (21 Sep, late; `chapters/04-worked-example.tex` only; approved plan: corrections → Ch5 → Ch6/7 → compressed Ch4 prose).**
Applied (all transcribed from `backup_ch4_2026-09-21_pre-revert.tex` or Prof's own text; nothing new composed except the two Claude-drafted paragraphs already approved single-use on 21 Sep, re-inserted verbatim):
Eq. (4.7) + `M_B`: ε↔ε* swap fixed (Q8); colour contractions `(T)_{i1i2}(T)_{i2i1}` at all three places (Q9); sentence describing colour indices replaced (Prof; points to `subsec:colourAlg`); Γ̄ definition added after Eq. (4.8) (Q9); dΦ3 exponent `(q²)^{(2-d)/2}` (Q7); the "R3" subsection restructured per Prof/Q10 — dedicated subsection dissolved, duplicate analytic-R3 equation removed, `eq:R3` label on the analytic result, **d-dimensional 3rd line of R3 restored**, "For A30 the integration can be done directly…" and "As discussed in Chapter 2…" paragraphs (this SAVES lines); IBP example: heading "…a Representative Term", J-steps added (Q11: completeness relation + `J(1-ε,-ε,1-ε)=…` line); Q12 sentence ("At this intermediate stage…"); Q13 "usual" dropped; `+...`→`+\cdots` in 14 Out blocks; chapter-intro false PV claim replaced by Prof's two PV/IBP paragraphs (Q1/Q2; R8a pointer kept as `subsubsec:R8a` until R8a moves); Table 4.1 † marks + caption note; B0/C0-counterterm paragraph + truncation note in §4.2.1 (Q3/Y2) and the "three further steps" Out[4]→Out[5] paragraph (Q4); labels `subsec:A21`, `eq:SGamma`; N_f `Out[4]` coefficient halved (c5df6d3 fix); A6 = exact expression + Laurent series (Q5; not Mellin–Barnes); R4 last line `S_Γ,2` (Q21); R6 middle line `S_Γ,2` + `(−q²−i0)^{−ε}` typo; R8a `6/ε⁴` (was 6/ε²) + `S_Γ,2`; R8b last line `S_Γ,2`; Table 4.2 MI lists (A40 R4,R6 / Ã40 all four / B40 R4,R6 / C40 R4,R6,R8b).
**Build:** `latexmk` clean, **135 pp (was 134), 0 undefined refs**, rendered pp. of Eqs. 4.7–4.9 and A6 checked. **Page effect: Ch4 +1 page** (Ch5 now starts PDF p.89, Conclusion is PDF p.95 = printed p.80, 28/40 lines used ⇒ **remaining slack ≈ 12 lines**). Most of it is the two explanatory paragraphs (Q3 ≈ 15 lines, Q4 ≈ 15 lines), J-steps (+9), A6 exact (+5), PV/IBP intro (+9); to be shrunk in the compression pass (e.g. fold the Q4 paragraph to ≈ 6 lines).
**Not done in this batch (deliberately):** unnumbering of `\subsubsection`s and the repoints that go with it (Ch2 l.1760 still points to `subsubsec:a30IBPExample`, valid while numbered); section/subsection retitling to Prof's ToC; chapter-title change; chapter-intro P1–P5; Δ4/Θ/Ω and normalisation wording (4.5)/(4.6); LiteRed2 `j[…]` paragraph; all set-level intros, component lead-ins and interpretation comments; V-integral/R-integral prose; §4.5/§4.6 text; R8a → appendix. Still `[Yours]`: dΦ4 prefactor marker (not re-inserted — it lived in the backup only), Feynman-diagram figure(s), C1–C7.

**BATCH 2 — R8a derivation moved to Appendix B (21 Sep, late; Henrique: "retain the results … as for all other masters; point to the appendix").**
Ch4 §R8a now = definition + 3-sentence lead-in (Henrique's/HEAD's own sentences re-joined, no new physics) + **the normalised result equation unchanged** (closed form with ₄F₃/₃F₂ and Laurent series, new label `eq:R8a`) + pointer. New **Appendix B.6 "Derivation of the $R_{8,a}$ Master Integral"** (`sec:R8aDerivation`, inserted after the Laporta example; the old B.6/B.7/B.8 shift by one — all referenced by `\ref`, checked) holds, verbatim from the old text: tripole form, integration order, χ, quadratic transformation, v, Euler integral, pre-normalisation result; ends "after the final normalisation … gives Eq. (4.66)". Pointers repointed: Ch4 intro ("detailed derivation … Appendix B.6"), V5a ("methods used in Appendix B.6"); new label `subsec:fourPartonMI` on the four-parton MI subsection (used by the appendix). `% TODO[Yours]` Bateman citation re-inserted at the quadratic transformation. Interpreted "those two masters" as R8a only; R6/R8b left inline — move them too if that was meant.
**Build 135 pp, 0 undefined refs; main text back to ends printed p.79 (Conclusion 28/40 lines), Appendix A starts p.80 ⇒ batch 1's extra page is recovered.** Chapter-local spare lines: Ch4 last page 16/40 used (≈ 24 free), Ch6 last page 14/40. Later fix-ups needed when the `\subsubsection`s are unnumbered: the appendix intro says "Subsubsection 4.4.5.3" (→ Subsection pointer).

**BATCH 3 — Ch5 applied (21 Sep, late; `chapters/05-validation-r-ratio.tex`, one-word fix in Ch3 l.702).** Direction note added first: Ch4 §4.4.5.3 (R8a) now ends with "The detailed derivation of this result … is given in Appendix B.6" (in addition to the lead-in pointer); the appendix intro points back to §4.4.5 (`subsec:fourPartonMI`).
Ch5 = Prof's structure: 5.1 The Hadronic $R$-Ratio → 5.2 Assembly from Integrated Antenna Functions (label `subsec:Tobjects` kept, now a `\section`) → 5.3 NLO Validation → 5.4 NNLO Validation and the Hadronic $R$-Ratio → 5.5 Implementation in AntCalc → 5.6 Comparison with Experimental Data → 5.7 Summary. Title with italic $R$ (`\texorpdfstring`). Q24 fixed: expansion in α_s/2π throughout (`R_had = R⁽⁰⁾R(α_s)`, `R(α_s)=1+(α_s/2π)r_NLO+…`); the old α_s/π `c_LO…c_NNLO` equation is gone. Prof's texts transcribed (his pasted LaTeX was corrupted; equations taken from the live file). **Deviations, tell Prof:** (1) Eq. (5.15)/`eq:rNNLO` keeps **½Ã40 and 2C40** + the justification paragraph, placed right under it; (2) `R⁽⁰⁾=N Σ e_q²` (thesis colour symbol N; live text had N_c); (3) In[2]/Out[2] box kept in 5.5 (Prof shows In only); (4) the NLO equation keeps the explicit pole series (shows the cancellation Prof states); (5) his `[31]`/`[24]` mapped to `Dine:1979qh` / `Gehrmann-DeRidder:2004ttg`. Dead `\iffalse` T-object block untouched. `% TODO[Yours] 5.Y1` marker before Prof's "Upon integration … replaced by their integrated counterparts" sentence (bridging sentence for ½/2).
Ch3 l.702: "Subsection~\ref{subsec:Tobjects}" → "Section~…" (T-objects is now §5.2).
**Build 136 pp, 0 undefined refs. Page effect: Ch5 grew from 4 to 5 full pages (+1); Conclusion is again printed p.80 (28/40 lines), Appendix A starts p.81 ⇒ slack ≈ 12 lines.** Ch5's last page is completely full, so Ch5 cannot be tightened to 4 pages without cutting ≈ 40 lines. Ch6/Ch7 merge (recovers the page-break gaps) is what must now absorb Prof's longer Conclusions/Future-Work text; if that overshoots, next levers are Ch4 compression (family figure/table, boilerplate).

### !! 21 Sep 2026 (evening) — Chapter 4 REVERTED to commit 4133d64 because of the 80-page limit

Main text (Ch1–7, excl. Resumo/Abstract and appendices) had grown from 79 pp (commit 4133d64) to 89 pp; the limit is 80 (see the note near line 628).
All 10 pages were in Ch4 (4.1 +1, 4.2 +3, 4.3 +3, 4.4 +3 (of which 4.4.5 ≈ +2), 4.6 ≈ +1). Henrique ordered the revert.
Done: `chapters/04-worked-example.tex`, `appendices/appendixB.tex` and `chapters/02-physics-background.tex` restored to HEAD (the last two differed from HEAD
only by `\ref` repoints that depended on the new Ch4 labels). Ch3 and Ch5 edits of 21 Sep were KEPT. Build: 134 pp, main text ends p. 79, 0 undefined refs.
**Nothing is lost:** the full edited Ch4 (first batch + ChatGPT §4.3–4.4 intro + Claude's second-batch edits) is in `backup_ch4_2026-09-21_pre-revert.tex` (repo root, untracked;
delete it once no longer needed). Scratchpad snapshots (may vanish): `ch4_before_step1.tex`, `ch4_HEAD.tex`.
**Consequences to remember (the blocks below describe edits that are NOT in the current file):**
- HEAD Ch4 again contains the errors fixed in the first batch (Eq. 4.7 `\slashed` conjugate/colour factors, R3 last line vs P3 in 0311276, PV/PaVe claim for A22 and Table 4.1 without †,
  Nf `Out[4]` with the doubled coefficient, missing J-steps, A6 exact expression) and, from the second batch, the R8a `6/ε²` typo (must be 6/ε⁴), the R6 `(q²)^{-ε}` typo, S_Γ/S_Γ,2 notation.
  Ch3 (kept) now says the two-loop source has no PV step, which contradicts HEAD Ch4 until Ch4 is corrected again.
- Ch5 justification paragraph after Eq. (5.14) (kept) refers to "the integrated antennae computed in Chapter 4"; still fine.
- Any re-application must be page-neutral: Pires asked to expand almost every section. Options measured/estimated: move R4–R8b derivations to an appendix (−3/−4 pp),
  condense/move the two §4.6 tables (−1.5/−2), move the A30 IBP example + J-steps to an appendix (−2/−3), compact In/Out boxes (−1/−2).
  The important CORRECTIONS (above list) are page-cheap and should be re-applied first; the "expand" prose is what costs pages.

### Discoveries made while applying the first half of Ch4 (chapter intro → §4.2.5.4), 21 Sep 2026 — keep even though the Ch4 edits were reverted

The edited text is only in `backup_ch4_2026-09-21_pre-revert.tex`; the facts below are what must survive into the next application.

**1. The PV/PaVe claim for the A22 set was wrong (Ch3, Ch4 text, Table 4.1).**
- The two-loop source is reduced by IBP only: `A22PublicBuildComponents` → `A22InvariantOnlyReduction` → `A22LoopOnlyIBPReduction` (LiteRed2) + `IBPMasterValues`. No PV/TID step.
- The PV/TID reducer is used only in the one-loop route (`interference_loop.wl:147`) and in the A21 integration (`integration_pave.wl:48`).
- B0 and C0 reach the A22 set only through the A21 UV counterterm attached at the build boundary (Lead −11/(6ε)·A21, QuarkLoop +1/(3ε)·A21). Hence the † marks in Table 4.1 and the corrected Ch3 "Two-Loop Routes" paragraph (Ch3 edit is KEPT in the tree).

**2. Master integrals of the A22 set: how the integrated result is actually assembled.**
- Route-local per-label master values (`A22TwoLoopTreeMasterCoefficientRules`); `IBPNormalization = 1`; `IBPConventionBridgeFactor = 1` for A22; timelike continuation (−q²)^{−2ε} → q^{−2ε}cos(2πε) (NOT applied to Breve); the UV counterterm is added in `IntegratedAntennaTTerms`.
- Verified recipe: literature masters × π⁴e^{2γε}/Γ(1−ε)²·cos(2πε) + counterterm reproduces the Lead/Sub `Out[5]` exactly (old code). Breve uses −π⁴e^{2γε}/Γ² with no cos.
- The hard-coded factors give F_v·F_tree = cos(2πε)e^{2γε}/Γ² exactly through ε⁴. It is NOT a fit. My earlier suspicion of a "convention bridge" / fitted `A22TwoLoopTreeVirtualConventionFactor` was wrong and is retracted (bridge factor = 1).
- A6: exact expression (Γ + ₃F₂ + ₃F₂ + ₄F₃) verified and used with the Laurent series cited from hep-ph/0507061 (`GehrmannDeRidder0507061`). Note the A6 series with the S_Γ prefactor (…+27ζ₃/ε+23π⁴/36) differs from the R8b-section one (…+23ζ₃/ε+103π⁴/180) only because of the prefactor (Γ-ratio²), not because of an error.
- PV-reduction "canonical form" (Prof's Q1) = invariants-only (Mandelstam) form as in 0505111, not loop momenta.

**3. The N_f term: a genuine double count in AntCalc, now fixed (commit c5df6d3).**
- SMQCD has two quark classes, F[3] (up-type) and F[4] (down-type). Each vacuum-polarisation generation sum had been replaced by the TOTAL N_f, i.e. 2N_f overall, and a compensating ½ sat in the runtime A4 prefactor. The results agreed with the literature only because of that compensation (a "historical fit").
- Fix: `TwoLoopSMQCDGenerationMultiplicity[] := Nf/2`; A4 prefactor −1/4 → −1/2; cache semantic versions bumped (BuildAntenna 6, IntegrateAntenna 5). The source now documents that the scalar Nf topology is exactly the four-propagator A4 integral with no factor ½.
- Printed effect in the thesis: the N_f `Out[4]` coefficient halves, to −(ε−1)(6ε³−5ε²+3ε−2)/(2π⁴ε(2ε−3)(3ε−2))·A4; `Out[5]` is unchanged (verified in a fresh kernel).
- Pitfall: `<< AntCalc`` loads the INSTALLED paclet (was 0.3.1-beta.2), not the repo. For checks use `Get["…/AntennaPipeline.wl"]` or reinstall the paclet (`CreatePacletArchive`, `PacletInstall`, `PacletDataRebuild[]`, restart kernel). Henrique's first "fix did not work" reading was the stale paclet.

**4. R-ratio ledger multipliers (½ on Ã40, 2 on C40) are legitimate, literature-derived.**
- Ledger (`rratio_driver.wl:327`): A40Leading 1, A40Subleading ½, B40 1, C40 2, A22OneLoopSelfPoleShift 0. Sources: 0505111 (5.27), (5.28), (5.32), (5.44); 0403057 (4.51), (4.53). AntCalc's printed integrated Ã40/C40 already carry these explicit 2 and ½.
- Ch5 Eq. (5.14) was corrected and a justification paragraph inserted right after it (this Ch5 edit is KEPT).

**5. UNRESOLVED regression introduced by c5df6d3 (Henrique diagnosing).** Integrated A22 Leading and Subleading now disagree with the thesis and hep-ph/0403057 Eq. (4.9):
- Leading: 1/(4ε⁴)+25/(12ε³)+… (literature: 17/(8ε³)+…).
- Subleading: −3/(8ε⁴)−21/(16ε³)+… (literature: −1/(4ε⁴)−3/(4ε³)+…). Nf and Breve agree.
- Observation only (unverified hypothesis): the integration side changed only through the A4 runtime prefactor.
- Until resolved, do not trust: the Ch3 citation "0.3.1β3, commit c5df6d3" (Ch3 l.10 and l.871, kept in tree), Ch4 §4.2.1/§4.2.2 `Out[2]/Out[4]/Out[5]`, Table 4.1, Appendix A A22/Ã22, Eq. (5.15).

**6. Other verified facts from this half (all in the backup file, not in the current Ch4).**
- R3 last line was wrong; correct form equals P3 of 0311276 (verified).
- Eq. 4.7: `\slashed{\varepsilon}(p)…\slashed{\varepsilon}^*(k_3)` (also M_B) and colour factors (T^{a_3})_{i1i2}(T^{a_3})_{i2i1}; the Γ̄ ≡ γ⁰Γ†γ⁰ sentence was Prof's instruction.
- The J-step identity J(1−ε,−ε,1−ε) = ((3ε−2)/ε)·J(1−ε,1−ε,1−ε) was added because Prof asked.
- dΦ4 prefactor (q²)^{3−d/2} is verbatim 0311276 (A.5), not from Prof; left as `TODO[Yours]`.
- Ch3 l.188: the `DropSumOver` row ("replaces every SumOver[] with 1") is inaccurate (kept, still open).
- Repo hazards: `thesis_docs/codexAlley/antenna_pipeline` is an older divergent line (port hunks, never whole files); stale `.aux` corruption (`Extra }` at l.402) is fixed by deleting the `.aux` files and `main.toc/lof/lot/bbl/bcf/blg/out/run.xml`, then `latexmk -g -pdf …`.

### Prof. Pires review of Chapter 4 (second batch: §4.3 → §4.6) — 21 September 2026 (email, later the same day)

Henrique had ChatGPT apply part of this batch; it was audited by Claude (read-only), then Henrique authorised
("apply your points") the fixes below. Applied to `chapters/04-worked-example.tex`; build clean, 144 pp, 0 undefined refs.

**State found at audit:** applied = §4.3 intro, §4.3.1–4.3.3, §4.4 intro, R4 `S_Γ,2` fix. NOT applied = §4.3.4 text and V-integrals,
§4.4.1–4.4.5 text, §4.5, §4.6 intro. All of these are now applied (see B).

**A. Prof's questions, in context, with what was found / changed (for Henrique to answer in person)**

- **A1 (§4.3.2, after `Out[5]` of Ã31):** "why is the most singular pole only 1/ε²? add the explanation." Found: ChatGPT's paragraph was
  qualitatively right but omitted the key ingredient, the subtraction of A_2^1·A_3^0 in the definition of the one-loop antenna
  (Ch2 eq. after "This procedure defines…", and Eq. (3.3) of 0505111). Claude's derivation (Catani pole structure + that subtraction;
  NOT checked numerically): the universal double and single poles are ∝ C_F = ½(N−1/N), so their 1/N parts cancel against the
  subtraction; what remains at subleading colour is ln(s_ij/q²)/ε, which vanishes in the soft limit, so the integrated result starts at 1/ε².
  Changed: the last two sentences of the paragraph now say this (Claude-drafted, single-use permission "apply your points").
- **A2 (§4.3.3):** "shouldn't ReturnMasterCombination give R3 (A31hat = (b/ε)·A30)? remove or correct; confirm." Confirmed from source
  (`integrated_antenna_extraction.wl:193–230`): the raw IBP part of the Nf component is 0; +1/(3ε)·𝒜_3^0 is added AFTER the reduction, so
  R3 enters through IntegratedLowerAntenna[{A,3,0}], not through the master combination. The In[3]/In[4] block stays removed (Prof's text).
  Same mechanism for Lead: −11/(6ε)·𝒜_3^0 is added after the reduction, so R3 is absent from Lead `Out[4]` (sentence added in §4.3.1).
- **A3 (§4.3.4):** "why only the real part of the master integrals?" Answer written into the §4.3.4 intro (Claude-drafted sentence): the one-loop
  antennae are built from 2Re(M0†M1), so imaginary parts from the time-like loop do not contribute. Open: sign check, see C4.
- **A4 (§4.4.1):** "is this expression complete? only two masters appear." Yes: `Out[4]` printed is complete; A40 Lead reduces to R4,R6 only;
  Ã40 additionally has R8a,R8b (Prof's own §4.4.2 statement); B40 has R4,R6 only; C40 has R4,R6,R8b (no R8a).
- **A5 (§4.4.5.1, Eq. 4.56):** "S_Γ,2 became S_Γ in the last equation, check." Right: HEAD had S_Γ; already S_Γ,2 in the file. Verified with mpmath
  that R4 middle line = S_Γ,2 · Γ⁵(1−ε)Γ(2−2ε)/(Γ(3−3ε)Γ(4−4ε)) exactly (A = S_Γ·B), and B = 1/12 + 59/72 ε.
- **A6 (§4.5, Eq. 4.84):** "where does the factor −1/4 come from?" In AntCalc: `MassiveA30IntegratedCutMeasureFactor[] := -1/4`
  (`src/routes/massive_a30_integrated.wl:387`), documented as "I_paper = C_cut·j_MX30", fixed by two independent coefficient
  determinations in `MassiveA30IntegratedCutMeasureConsistencyReport[]`; it is a calibrated cut-measure constant, not derived analytically.
  Text now says "an overall constant factor of −1/4 between the cut-integral normalisation of LiteRed2 and the phase-space normalisation
  of the literature masters" and carries a `% TODO[Yours]`. **Henrique must decide what to say about its origin.**

**B. Applied (Mine / verified fixes)**
- §4.3.1: dangling "using" fixed; Prof's closing sentence restored, corrected (Lead has V5a,V5b only; V8 does not appear); sentence on the
  post-reduction counterterm −11/(6ε)𝒜_3^0 / R3 (Claude-drafted). `\label{sec:A31set}` on §4.3; §4.4 intro now points to it (was rendering "Section 4.3.4"). Labels on 4.3.x/4.4.x subsections.
- §4.3.4 intro + V5a/V5b/V8 rewritten per Prof (V5b gets the "definition in terms of propagators" equation, with Re added for consistency with its cos(πε)).
- §4.4.1–4.4.4 texts, §4.4.5 intro, R4/R6/R8a/R8b texts per Prof. "Section X" (normalisation) → Eq. `eq:normN`; "[references]" for I6, A4, I8 → `GehrmannDeRidder0311276` (which itself cites [28,43]).
  Removed the now-redundant "Finally, the γ*→qq̄qq̄ process…" paragraph. Table 4.2 rows: B40 → R4,R6; C40 → R4,R6,R8b; A40/Ã40 split.
- Verified fixes beyond Prof's text: **R8a closed form had 6/ε² where 0311276 Eq. (4.17) and the series need 6/ε⁴** (numerically verified, remainder O(ε));
  middle-line `S_Γ` → `S_Γ,2` in R6 and R8a (numerically verified same series as the last line; (4.72) = (4.73) to 40 digits);
  R6 `(q²)^{-ε}=(q²)^{-ε}e^{iπε}` typo fixed to `(−q²−i0)^{-ε}`; R8b last line `S_Γ`→`S_Γ,2` (ε⁻⁴ coefficient 3/4 only follows in S_Γ,2).
- §4.5, §4.6 texts per Prof. **Deviation:** kept the thesis symbol `m_q` (Prof writes `m_Q`), since Ch2, Ch3 and the appendices use `m_q`; `Q` kept for the particle labels.

**C. Yours / open**
1. **Massive A30 wording (`% TODO[Yours]` in §4.5.2).** Prof's sentence "Internally, this calculation performs the massive IBP reduction…" was NOT written:
   in AntCalc the default massive integrated route returns the encoded literature closed form after an invariant renaming
   (`MassiveA30IntegratedRuntimeSeries`, `massive_a30_integrated.wl:269`; notice at `integration_router.wl:788` "follows the derived MX30 master closure");
   the LiteRed J-route runs only when forced and is used to calibrate −1/4. So "agrees with Ref. [25]" is not an independent check for the default route.
   Also affects Table 4.2 row "IBP / LiteRed2, massive family". Decide wording (or ask-codex / run the forced route to state precisely what is validated).
2. R8b: the removed middle line contained `½ Im S` which I could not reconcile with the printed series (the three-term combination already reproduces
   −61π⁴/60 without it). The `I_8 = S + O(ε⁰), Im S = −13π⁴/90` line is kept as before; the Γ²(1−ε) in the removed line looked like a typo for Γ²(1+ε).
   Note also 0311276 (4.27) prints `Box(s13,s13,s12)` for V8; the thesis R8b now uses `Box(s13,s23,s12)` as in §4.3.4 (deviates from the paper's print, probably a paper typo).
3. Feynman-diagram clauses omitted, `TODO[Yours]` markers now at: §4.2 A21, A22 two-loop, Nf loop, plus new §4.3.1, §4.4.1, §4.4.3 ("Fig.~X").
4. **Sign check (V5a):** the printed V5a is −R3·Re B0 under the Appendix-B convention of B0 (B0 ≈ +1/ε), while the definition line `Re[−i∫dΦ3∫d^dp/(2π)^d …]` would give +. Pre-existing; not touched. Check the convention of the source (0505111).
5. Bateman citation for the quadratic transformation (`% TODO[Yours]` in R8a); Prof asked "[referencia ao Bateman ?]". Not in `references.bib`.
6. §4.6 tables say "Pass" for the A_2^2 set while the A22 Lead/Sub regression in AntCalc c5df6d3 is unresolved (see the first-batch block).
7. Prof's ToC/titles: 4.5.2 title in his note is "Build and Integrate of the Massive A30"; the thesis keeps "Construction and Integration of the Massive $A_3^0$".

### Prof. Pires review of Chapter 4 (first batch: chapter intro → §4.2.5.4) — 21 September 2026 (email)

Prof's section/equation numbers follow the compiled draft he read; the `.tex` may have shifted since. His first notes stopped at §4.2.5.4 (the $A_6$ integral). **The second batch, received 21 September, covers §§4.3–4.6 and is recorded below.** If Ch4 has already gone to Prof. Sampaio, everything in this block postdates his copy (same rule as the 15 Sep batch).

**Henrique's standing request for this batch: remember every question in the context it was asked, and what changed, so he can answer Prof properly.** Section A below is that record.

#### A. Prof's questions — in context, what we found, what changed

#### Second batch intake — §§4.3–4.6 (21 September 2026; no thesis edit yet)

**Intake and repeat check.** The pasted feedback was read in full before this entry. It is not a repeat: the preceding durable record explicitly said that Prof's first Ch4 batch stopped at §4.2.5.4. The live source is `chapters/04-worked-example.tex`; the technical claims below were checked against the release branch at `~/Desktop/AntCalc` and the primary sources hep-ph/0403057 and hep-ph/0311276 (read as rendered pages). No thesis source has changed during this intake.

**Classification.** The requested contextual expansions, final-result interpretation, diagram choices, and the explanation of the subleading-colour pole pattern are **[Yours]**: they require physics prose and viva-defensible judgement. The only currently verified mechanical correction is the $R_4$ normalisation notation (Q21 below), now applied. The necessary re-description of $\widehat A_3^1$ is **[Yours]**, despite Prof supplying example wording, because it changes the conceptual account of what the zero master-combination output means.

**Classification correction (Henrique, 21 Sep).** The preceding paragraph misapplied the agreed ROE labels and is superseded by this entry. **Prof's literal supplied replacement prose is [Mine]**: I verify it, insert it, rebuild, render, and show the diff. This covers the supplied expansion/replacement blocks in Q14, Q16, Q18--Q20, Q22 (except the bracketed question), and Q23. **[Yours]** is limited to material Prof asked Henrique to supply but did not draft: the Feynman-diagram decision and its references (Q14); the derivation and explanation of why $\widetilde{\mathcal A}_3^1$ begins at $1/\epsilon^2$ (Q15); the real-part explanation in Q17 if Henrique elects to write it himself; and the explanatory account of the massive paper-to-cut-basis $-\tfrac14$ factor (Q22). The old per-item labels below are superseded where they conflict with this correction.

- **Q14 — §4.3 opening / leading $A_3^1$: expand the set-level context, renormalisation, and final-pole discussion; add a representative diagram reference.** *Prof's request:* supplied the proposed opening plus a leading-colour narrative, including the tree--one-loop interference origin, separate Leading/Subleading/$N_f$ components, UV renormalisation, and a $1/\epsilon^4$ interpretation. *Found:* the live §4.3 is only two sentences, then jumps directly to notebook output. Its statements that there is no one-loop-squared contribution and that the integrated one-loop three-parton term combines explicit loop and unresolved phase-space poles agree with the perturbative structure in hep-ph/0403057 §4.2. The `Fig. 4.X` clause cannot be added until Henrique decides on a diagram. *Status:* **[Yours], open; no draft authorised.**

- **Q15 — why does $\widetilde{\mathcal A}_3^1$ start at $1/\epsilon^2$?** *Prof's question:* asks for a physical explanation in §4.3.2. *Found:* the live output indeed begins at $1/\epsilon^2$. However, hep-ph/0403057 Eq. (4.34) is an inclusive colour-decomposed three-parton contribution, not a one-to-one definition of the thesis's isolated subleading antenna; its $1/N$ bracket has higher poles. Therefore it cannot by itself prove a cancellation mechanism for the thesis convention. Do **not** claim generically that subleading colour suppresses soft/collinear poles. A defensible explanation requires deriving the antenna's colour-stripped unresolved limits or locating the exact $\widetilde{\mathcal A}_3^1$ definition in the primary antenna literature. *Status:* **[Yours], blocking verification open.**

  **Correction / direct verification (21 Sep).** hep-ph/0505111 gives the isolated integrated result directly in Eq. (5.19), and it agrees exactly with the thesis output through $\mathcal O(\epsilon^0)$: its $1/\epsilon^2$, $1/\epsilon$, and finite coefficients are algebraically identical. Equation (5.14) also gives the unintegrated pole structure as $2[\mathbf I_{q\bar q}^{(1)}(\epsilon,s_{12})-\mathbf I_{q\bar q}^{(1)}(\epsilon,s_{123})]A_3^0$. The earlier statement was limited to hep-ph/0403057 and is superseded. Henrique's remaining task is the explanatory derivation of the cancellations, not verification of the result itself.

  **Mechanism identified (21 Sep; explanation only, no thesis prose drafted).** The pole operators in hep-ph/0505111 make the hierarchy precise. The leading-colour antenna has $2[\mathbf I_{qg}^{(1)}(\epsilon,s_{13})+\mathbf I_{qg}^{(1)}(\epsilon,s_{23})-\mathbf I_{q\bar q}^{(1)}(\epsilon,s_{123})]A_3^0$ (Eq. (5.12)): its two colour-adjacent $qg$/$g\bar q$ dipoles retain $1/\epsilon^2$ poles depending on the unresolved invariants $s_{13}$ and $s_{23}$. Multiplication by the tree antenna and integration of its unresolved phase space can therefore reach $1/\epsilon^4$. In the subleading coefficient, Eq. (5.14) instead has $2[\mathbf I_{q\bar q}^{(1)}(\epsilon,s_{12})-\mathbf I_{q\bar q}^{(1)}(\epsilon,s_{123})]A_3^0$. Since $\mathbf I_{q\bar q}^{(1)}\propto \epsilon^{-2}(-s)^{-\epsilon}$, the scale-independent double-pole term cancels in the difference: it starts as $\log(s_{12}/s_{123})/\epsilon$. In the $q\parallel g$ limit $s_{12}/s_{123}\to z$, and in the soft limit $z\to1$, so this factor removes the overlapping soft--collinear enhancement. The remaining single-unresolved integration produces at most $1/\epsilon^2$, agreeing with Eq. (5.19). This is a colour-coherence/cancellation statement after the gauge-invariant colour projection, not a generic consequence of a non-planar loop topology; individual diagrams need not have the reduced pole count.

  **Diagram decision and implementation (Henrique, 21 Sep).** §4.3 now begins with one compact representative one-loop $\gamma^*\to qg\bar q$ diagram (`fig:A31Representative`), rather than repeating a diagram in each of the Leading/Subleading/$N_f$ subsections. Its caption explicitly states that the antenna components are defined by the complete tree--one-loop interference, not by the pictured graph. The adjacent opening prose names the three components and correctly distinguishes the $N_f$ UV-renormalisation contribution from the leading/subleading bare-interference components. Source: `chapters/04-worked-example.tex`; rebuild and render required.

  **Diagram reverted (Henrique, 21 Sep).** Henrique requested removal of the new A31 representative diagram. `fig:A31Representative` and its figure-specific reference were removed; the corrected component-description paragraph remains. The broader diagram strategy is therefore undecided again.

- **Q16 — §4.3.3 $\widehat A_3^1$: why does `ReturnMasterCombination -> True` give zero; should $R_3$ appear?** *Prof's answer/proposed replacement:* the bare tree--one-loop interference has no $N_f$ term; the nonzero antenna is the $N_f$ UV coupling counterterm proportional to $A_3^0$, and its phase-space integration inherits $R_3$. *Found:* confirmed in the live source and release code. The zero is the unreplaced **bare IBP diagnostic payload**, while AntCalc applies A31 component-specific lower-antenna counterterms after backend reduction (`integration_workflows.wl`, `integrated_antenna_extraction.wl`). The current text already partly says this, but the displayed zero and the phrase "the result in terms of the antenna's master integrals vanishes" invite the wrong reading. The proposed replacement is physically consistent; it must make clear that no $V_{5,a},V_{5,b},V_8$ contribution survives, not that the integrated antenna is zero. *Status:* **[Yours], open.**

- **Q17 — §4.3.4: why only real parts of the $V$ masters?** *Prof's question.* *Found:* the relevant NNLO term is the physical tree--one-loop interference $\langle M^{(0)}|M^{(1)}\rangle+\langle M^{(1)}|M^{(0)}\rangle=2\operatorname{Re}\langle M^{(0)}|M^{(1)}\rangle$ (hep-ph/0403057 Eq. (4.27), and its integrated form Eq. (4.33)). Thus only the real part contributes to the cross section; absorptive imaginary parts cancel between conjugates. *Status:* **[Yours], open; this explanation is grounded.**

- **Q18 — expand the $V_{5,a}$, $V_{5,b}$, and $V_8$ discussions.** *Prof's request:* explain factorisation for $V_{5,a}$, non-factorisation for $V_{5,b}$, and the box/$D_0$ form for $V_8$. *Found:* all three descriptions are consistent with the live definitions. $V_{5,a}$ depends only on fixed $q^2$ and factors against the phase-space volume; $V_{5,b}$ depends on variable $s_{13}$; $V_8$ has a phase-space-dependent box. *Status:* **[Yours], open.**

- **Q19 — §4.4 set-level expansion and $A_4^0$ master completeness.** *Prof's request:* add physical double-real context and check whether the leading $A_4^0$ master expression is complete because it shows only two masters. *Found:* the live `Out[4]` contains exactly $R_4$ and $R_6$, and no $R_{8,a}$/$R_{8,b}$; that is complete for this reduction, not truncated. The standard four-parton basis has four possible masters (hep-ph/0311276 Eqs. (3.2)–(3.5)); which ones appear is antenna-specific. *Status:* expansion **[Yours], open**; completeness question **resolved, no source change needed.**

- **Q20 — §§4.4.2–4.4.5: explain the remaining four-parton antennae and master basis.** *Prof's request:* contextual prose for $\widetilde A_4^0$, $B_4^0$, $C_4^0$, and the $R_4,R_6,R_{8,a},R_{8,b}$ section. *Found:* the live reduction outputs support the stated sets: $\widetilde A_4^0$ uses all four, $B_4^0$ uses $R_4,R_6$, and $C_4^0$ uses $R_4,R_6,R_{8,b}$. The existing $C_4^0$ statement that only its triple-collinear limit survives is a physics claim that should be retained only after direct source-level checking when drafting. *Status:* **[Yours], open.**

- **Q21 — $R_4$: is the change from $S_{\Gamma,2}$ to $S_\Gamma$ in the last displayed equation correct?** *Prof's question.* *Found:* **no.** hep-ph/0311276 Eq. (4.13) defines its $S_\Gamma$ to include $P_2$. In this thesis, Eq. `eq:SGamma` instead defines bare two-loop $S_\Gamma$, while $S_{\Gamma,2}=P_2S_\Gamma$. The final $R_4$ line must therefore carry $S_{\Gamma,2}$, matching its preceding exact line and the source paper under the thesis's notation. *Changed:* **[Mine], applied** in the Chapter 4 source; full rebuild clean (139 pp; no undefined or multiply-defined references) and rendered PDF page 69 checked.

- **Own finding — audit adjacent four-parton normalisations.** The same source/thesis naming mismatch appears potentially in the displayed $R_{8,b}$ result (and the prose calling the source prefactor "usual $S_\Gamma$"); it does not follow merely from Q21. Treat this as my finding, not Prof's. *Status:* **verify before change; no edit.**

- **Q22 — §4.5 massive $A_3^0$: conceptual framing and origin of the $-\tfrac14$ conversion.** *Prof's request/question.* *Found:* the release implementation now derives the common cut-measure relation directly: its provenance notes state $I_{\rm paper}=-j_{\rm MX30}/4$, fixed independently from the undotted and numerator-master relations, rather than an arbitrary display factor. The physical prose needs to distinguish the literature phase-space masters $I_1,I_2$ from LiteRed2's reverse-unitarity cut basis $J_1,J_2$; the minus and quarter belong to that cut-measure convention. *Status:* conceptual expansion **[Yours], open**; factor explanation **grounded but requires careful prose**.

- **Q23 — §4.6: explain why the summary/validation section exists.** *Prof's request.* *Found:* the live section already centralises construction and validation tables, but lacks the framing proposed by Prof: it should explain that the section distinguishes the fully validated massless set from the exploratory massive route and gives the reader a single audit point. *Status:* **[Yours], open.**

Grounding sources: the live `.tex`; `~/Desktop/AntCalc` (branch `release/0.3.x`, HEAD `2181fff`; the A22 build architecture below is identical at the manuscript's commit `4ceb991`) read directly, **no `ask-codex` used**; hep-ph/0311276 App. A; hep-ph/0507061; numeric checks with mpmath (scratchpad).

**Marked "Pergunta"/"PERGUNTA" by Prof:**

- **Q1 — "What does *canonical form* mean?"** *Context:* the old Ch4 intro sentence "PaVe … used only for reducing unintegrated one- and two-loop antennae to their canonical form" (the phrase appears nowhere else in the thesis). *Henrique's answer:* the form with no loop momenta, only Mandelstam invariants, as hep-ph/0505111 does. *Grounded:* matches the code (`A22PublicBuildComponents` docstring: "loop-integrated, Mandelstam-only"; `InvariantOnlyQ`). *Changed:* Prof's replacement paragraph drops the phrase — applied; nothing else needed in the text, only the viva answer.
- **Q2 — "PV is a one-loop tensor reduction. Why did it enter the $A_2^2$ calculation?"** *Context:* the same intro sentence, and Ch3 §"Two-Loop Routes" (`03-package-framework.tex` ~l.335): "Passarino-Veltman reduction is used on the build side to remove the two loop momenta $\ell_1,\ell_2$, while IBP … " *Henrique's answer:* PV is there to keep the pattern with the other loop antennae. *Grounded (this contradicts that answer):* `AntennaReductionProfile` exists only for `{A,2,1}` and `{A,3,1}` — none for `{A,2,2}`; `BuildTwoLoopAntennaData` never calls the PV reducer; `A22PublicBuildComponents` → `A22InvariantOnlyReduction` → `A22LoopOnlyIBPReduction` (LiteRed2 IBP) + `IBPMasterValues` (literature master substitution) → an invariant-only expression; then it attaches the counterterm skin $-\tfrac{11}{6\epsilon}A_2^1$ (Lead) / $+\tfrac1{3\epsilon}A_2^1$ (QuarkLoop), with $A_2^1$ itself built via PV. **PV reaches $A_2^2$ only through that counterterm.** Prof's implicit point is right. *Changed:* the Ch4 sentence is fixed by his paragraph (applied). **Ch3 ~l.335 fixed 21 Sep (see Y1).**
- **Q3 — "Why do only one-loop $B_0, C_0$ appear if you are computing $A_2^2$?"** *Context:* §4.2.1, right after `BuildAntenna[A,2,2,Component->Leading]` whose Out[2] is $\frac{11[(3+2\epsilon)B_0+2s_{12}C_0]}{12\epsilon}+\ldots$ *Grounded:* $A_2^1=-\tfrac12[(3+2\epsilon)B_0+2q^2C_0]$, so the displayed term is exactly $-\tfrac{11}{6\epsilon}A_2^1$ = the coupling-renormalisation counterterm; the "$\ldots$" is the genuine two-loop part. Same for $N_f$ ($-\tfrac1{6\epsilon}[\ldots]=+\tfrac1{3\epsilon}A_2^1$). Consistent with Table 4.1 (build-side $B_0,C_0$ only for Lead and $N_f$). Henrique floated dropping PaVe altogether; recommendation given: keep it in the package ($A_2^1$/$A_3^1$ need it; deadline), fix the *narrative*. *Changed:* 21 Sep — explanatory paragraph in §4.2.1 + Table 4.1 caption note (see Y2).
- **Q4 — "Is this the final result or are there more terms you are not showing?"** *Context:* §4.2.1, `A22Lead` Out[4] (three MI terms, no "$+\ldots$") after `ReturnMasterCombination->True`, whereas the unintegrated Out[2] had "$+\ldots$". *Grounded (revised 21 Sep after Henrique pasted his own `Out[28]` — identical to the printed Out[4] for Lead, so the transcription is faithful):* **Out[4] is a symbolic combination, not the input to Out[5] in the form the thesis writes it.** `ReturnMasterCombination` returns the loop-only IBP combination in terms of `j[...]` labels; `A22MISubs` then collapses every $A_4$-type label onto one symbol `A4` (and every $A_3$-type onto `A3`). The code does **not** do that: `A22TwoLoopTreeMasterCoefficientRules` assigns each exact-topology label its own *route-local* value (`A22TwoLoopTreeValueForExactTopology`, e.g. `A22TwoLoopTreeMasterValueA4Basis7Like/Basis8Like/NfLike`), in the package convention (timelike continuation with $\cos 2\pi\epsilon$, plus a two-parton convention factor), then multiplies by `IBPNormalization`, and the $A_2^1$ counterterm is added separately. A code comment (`integration_ibp.wl` ~l.2236) says the relation of the Basis7/8-like $A_4$ representatives to the canonical $A_4$ "has not yet been closed at the master-value level" and that the validated route-local values are preserved. **Correction to my earlier note:** the A22 `IBPConventionBridgeFactor` is explicitly `1` ("would double-count"), so the "convention bridge" hypothesis was wrong — the difference comes from the route-local values, `IBPNormalization`, and the counterterm. Numeric check: $-\pi^4e^{2\gamma\epsilon}/\Gamma(1-\epsilon)^2\times$ the printed MI combination reproduces the printed $\breve A_2^2$ Out[5] to $10^{-60}$ (single master, so no collapse issue) but not Lead/Sub/$N_f$. **$\breve A_2^2$ / JMS sign claim is safe:** `A22OneLoopSelfVirtualConventionFactor` $=1-\pi^2\epsilon^2/6-\tfrac23\zeta_3\epsilon^3+\pi^4\epsilon^4/120$ is *exactly* the expansion of $e^{2\gamma\epsilon}/\Gamma(1-\epsilon)^2$ (checked to $\epsilon^4$), i.e. a standard normalisation, not a fit; the older notebook factor had a wrong $+26\zeta_3/3$. **Open worry:** `A22TwoLoopTreeVirtualConventionFactor` ($=1-2\pi^2\epsilon^2-\tfrac{28}3\zeta_3\epsilon^3+\tfrac23(\pi^4+42\zeta_3)\epsilon^4$, contains $\cos 2\pi\epsilon$ plus $\zeta_3$ terms of mixed weight) is described in its comment as "fixed by matching" the paper's bare $T_{qq}^{(6,[2\times0])}$ bracket — whether that is derived or fitted decides how the $1/\epsilon$ and finite parts of $A_2^2,\tilde A_2^2,\hat A_2^2$ can be described (independent construction vs. matched to hep-ph/0403057). Unresolved. *Status:* **MOSTLY RESOLVED — see the UPDATE indented below; the $N_f$ factor $\tfrac12$ is open.** Cosmetic: the `Collect[#,{A3,A4,A6}]` in the Lead In[4] lists $A_6$ (absent) and omits $A_{22,\mathrm{LO}}$.
  **UPDATE 21 Sep (later) — `ask-codex` run once (1 call, 3 bundled questions: full chain Out[4]→Out[5]; provenance of `A22TwoLoopTreeVirtualConventionFactor`; are the route-local Basis7/8-like $A_4$ values canonical). Raw output: scratchpad `codex_a22.out`. Then my own numeric reproduction (mpmath, scratchpad `chk3.py`/`chk4.py`).**
  - **Chain (code: `integration_ibp.wl`, `integrated_antenna_extraction.wl`):** `ReturnMasterCombination` = unreplaced backend `j[...]` combination; substitution via `A22TwoLoopTreeMasterCoefficientRules` (per exact-topology label); timelike continuation $(-q^2)^{-2\epsilon}\to q^{-2\epsilon}\cos 2\pi\epsilon$ (`A22TwoLoopTreePaperConventionRules`, l.2187; **not** applied on the Breve route); `IBPNormalization`=1 for A22; `IntegratedAntennaTTerms` then adds $-\tfrac{11}{6\epsilon}A_2^1$ (Lead), $0$ (Sub), $+\tfrac1{3\epsilon}A_2^1$ ($N_f$) (l.234). Route-local values: Basis7Like/Basis8Like $A_4$ are hard-coded and differ from the runtime canonical $A_4$ already at $1/\epsilon$; runtime $A_4$ carries a documented extra overall $1/2$ (`masterIntegrals/A4.wl:53`); $A_3$ uses an inherited "R8a package bridge"; $A_{22,\mathrm{LO}}$'s bridge check is `Missing["NotYetEncoded"]`. **Correction to my earlier note:** the A22 `IBPConventionBridgeFactor` is `1`; the "convention bridge" hypothesis was wrong.
  - **Numeric reproduction (mine, independent of the code's internals):** the thesis's Out[4] with the *literature* masters of §4.2.5 substituted (all $A_4$-type labels $\to$ one $A_4$), times the standard factor $\pi^4e^{2\gamma\epsilon}/\Gamma(1-\epsilon)^2\cdot\cos 2\pi\epsilon$ (the $\pi^4$ cancels the $1/\pi^4$ in Out[4]'s coefficients): **Leading (plus $-\tfrac{11}{6\epsilon}A_2^1$) and Subleading reproduce the printed Out[5] exactly at all five orders, residual $\sim10^{-46}$.** **$N_f$:** the same recipe gives exactly **twice** the master part (ratio $=\tfrac12$ through finite order, residual $\sim10^{-38}$): $\hat A_2^2=\tfrac12[\text{recipe}]+\tfrac1{3\epsilon}A_2^1$. The $\tfrac12$ matches the code's runtime-$A_4$ backend adjustment; its origin is **not explained in the code** — a real gap between the thesis's stated procedure and what is computed for $N_f$. **Breve:** exact with $-\pi^4e^{2\gamma\epsilon}/\Gamma^2$ and no $\cos$ (its convention factor is exactly the expansion of $e^{2\gamma\epsilon}/\Gamma(1-\epsilon)^2$, confirmed by codex).
  - **Provenance verdict (codex, from code):** `A22TwoLoopTreeVirtualConventionFactor` is a hard-coded "matching" result — no derivation, fit routine or target-equation citation in the repo; the repo's own `docs/development/a31-a22-provenance-audit.md` classifies the A31/A22 literature comparison as internal consistency / regression evidence, not independent validation. **But** my recomputation shows the public Lead/Sub numbers follow from canonical literature masters + standard normalisation alone, so for those two the results do not depend on that factor's provenance. R-ratio pole cancellation ($\epsilon^{-4}\ldots\epsilon^{-1}$) is an end-to-end internal check; its finite-term comparison uses an encoded reference.
  - **Consequence for the thesis text (nothing edited yet):** Out[4] is not the input to Out[5] as printed. Honest statement: the master combination becomes Out[5] after (i) substituting the analytic masters of §4.2.5, (ii) converting the $S_\Gamma$ normalisation to the $e^{2\gamma\epsilon}/\Gamma(1-\epsilon)^2$ convention, (iii) the real part of the timelike continuation ($\cos2\pi\epsilon$; none for $\breve A_2^2$), (iv) the $A_2^1$ counterterm for Lead/$N_f$. **Open [Yours]:** the $N_f$ factor $\tfrac12$ (look at `masterIntegrals/A4.wl:53` and the true $N_f$-topology master before claiming the thesis's $A_4$ substitution reproduces $\hat A_2^2$).
  - **RETRACTION / refinement (21 Sep, same session):** the two hard-coded ζ₃-laden convention factors are *not* a free fit: `A22VirtualTwoPartonConventionFactor`×`A22TwoLoopTreeVirtualConventionFactor` (also duplicated as `A4VirtualConventionFactor`×`A4TwoLoopTreeVirtualConventionFactor` in `masterIntegrals/A4.wl`) multiply to **exactly** $\cos(2\pi\epsilon)\,e^{2\gamma\epsilon}/\Gamma(1-\epsilon)^2$ through $\epsilon^4$ (verified symbolically, difference $=0$; the ζ₃ pieces cancel in the product). So the tree/two-loop chain is the standard normalisation written as a truncated series — my earlier worry about that factor is withdrawn. **The only unexplained factor is the $N_f$ ½.** `A4.wl:57` remark: "The validated backend package value includes an additional overall 1/2 normalization adjustment relative to the straightforward appendix-to-package lift." Argument that it compensates something upstream: $A_4$ *is* the bubble-on-gluon-line vertex (the $N_f$ topology itself) and Lead/Sub reproduce the literature with the unmodified literature $A_4$, so the master cannot legitimately carry a ½ only in $N_f$ → the raw $N_f$ coefficient (or $N_f/T_R$ normalisation) is presumably 2× too large and the ½ hides it. **Unproven; needs an independent bare-$N_f$ derivation or a check of the package's $T_R$/quark-loop factor.** **RESOLVED by Henrique (21 Sep, reported, NOT yet verified by Claude):** cause = the code treated the $N_f$ quark variables of the up and down quarks as different and then summed them → spurious factor 2 in the raw $N_f$ coefficient (confirms the hypothesis; the ½ in `A4.wl` was hiding it). **Verification gap:** `~/Desktop/AntCalc` (`release/0.3.x`, HEAD `2181fff`) shows no $N_f$ change — `masterIntegrals/A4.wl` still has the "1/2 normalization adjustment"; the only uncommitted diffs are the A22↔A31 LiteRed-kinematics isolation and a version bump to 0.3.1-beta.3 (dated 14 Sep). So the fix lives elsewhere (other clone/branch/unsaved) — ask. **Checklist once located:** (1) the ½ in `A4.wl` must be removed *with* the upstream 2, otherwise $N_f$ is now half the right answer; (2) new $N_f$ `Out[4]` A4-coefficient should be half the printed one, and the literature-master recipe ($\pi^4e^{2\gamma\epsilon}/\Gamma^2\cdot\cos2\pi\epsilon$ + $\tfrac1{3\epsilon}A_2^1$) should then reproduce `Out[5]` directly with no ½ (Claude can check numerically from pasted outputs); (3) $N_f$ `Out[5]` unchanged; (4) re-run the R-ratio (poles cancel; $1.9857-0.1153N_f$); (5) look for the same bug class elsewhere (flavour sums in $B_4^0$/$C_4^0$, the R-ratio ledger entries ×½ $\tilde A_4^0$ / ×2 $C_4^0$ — confirm they are genuine symmetry factors, not compensations). **UPDATE 21 Sep (later): fix located and reviewed.** It is an uncommitted working-tree change in `thesis_docs/codexAlley/antenna_pipeline` (the older, *divergent* line, HEAD `7e2289c`), being ported by Henrique to `~/Desktop/AntCalc` (release). Content, reviewed by Claude: (a) `src/engines/amplitudes_loop.wl`: SMQCD (from SM.mod) has two quark classes F[3] (up-type) and F[4] (down-type); FeynArts emits one vacuum-polarisation insertion per class, each with its own `SumOver[Generation,3]`, and `CleanTwoLoopAmplitude` had replaced *each* by total Nf → 2Nf. New `TwoLoopSMQCDGenerationMultiplicity[] := Nf/2` per class. (b) `masterIntegrals/A4.wl` + `master_values_runtime.wl`: the compensating ½ removed (`A4` prefactor −1/4 → −1/2), `A4BackendPackageLiftRatio=1`. (c) `result_cache.wl`: BuildAntenna cache semantic version bumped (release now 6; IntegrateAntenna 5) so stale doubled results are not replayed. (d) new regression check `dev/check_a22_nf_flavour_counting.wl`. Release copy confirmed to contain (a)-(d). **Not ported / not to be ported:** `src/next/*` (python orchestrator prototypes), `runcards/runcard.json`, `results/*.m`, notebooks. **Two things flagged to Henrique:** (1) the release working tree ALSO changes the $A_3^1$ master `qkMI` in `masterIntegrals/master_values_runtime.wl` (from `Cos[eps Pi]Gamma[1-2eps]Gamma[1-eps]^4Gamma[1+eps]/(eps Gamma[3-4eps]Gamma[2-2eps]^2)` to `Gamma[1-2eps]Gamma[1-eps]^4/Gamma[2-2eps]`, ratio ≈ 2ε) — this is **not** in the Dropbox fix, matches neither Dropbox nor release HEAD, and the thesis's own $V_{5,b}$ (Ch4, hep-ph/0403057 form with $\cos\pi\epsilon$, $\Gamma(3-4\epsilon)$, $-1/\epsilon$) agrees with the OLD form → looks like an accidental/erroneous edit that would change $A_3^1$ and the R-ratio; must be reverted or justified. (2) `CleanOneLoopAmplitude` (release l.90–95, three-final-particle one-loop route) still maps generation sums to Nf (same pattern); harmless if the surviving closed-quark-loop terms vanish (scaleless external-gluon self-energy; Furry), which is consistent with the bare $\hat A_3^1=0$, but worth applying the same multiplicity for consistency. Also: add a release-history entry (version.wl) for the fix and settle which tag/commit the thesis cites.

**21 Sep (later still): release ported and verified by Claude** — `~/Desktop/AntCalc` `master_values_runtime.wl` now differs from HEAD only in the $A_4$ prefactor (the stray `qkMI` edit was reverted); fix present in `amplitudes_loop.wl`/`A4.wl`/cache version; `version.wl` has **no release-history entry for the fix** yet. **Design consequence for the thesis:** coefficient ×½ and $A_4$ ×2 cancel, so all *final* numbers should be unchanged (Out[5] for $N_f$, Appendix A $\hat A_2^2$ built+integrated, R-ratio) — **to be confirmed by a re-run**; the only printed object expected to change is §4.2.3 `Out[4]` (predicted: the $A_4$ coefficient halves to $-(\epsilon-1)(6\epsilon^3-5\epsilon^2+3\epsilon-2)/(2\pi^4\epsilon(2\epsilon-3)(3\epsilon-2))$ — do NOT edit until confirmed from a real run). After the fix the four-step recipe (literature masters × $\pi^4e^{2\gamma\epsilon}/\Gamma^2\cdot\cos2\pi\epsilon$ [none for Breve] + $A_2^1$ counterterm for Lead/$N_f$) should hold uniformly for Lead/Sub/$N_f$, so the thesis's stated procedure becomes literally true. **New small items:** Ch3 l.188 (`DropSumOver` row says "Replaces every `SumOver[]` with 1"; actually external sums → 1, generation sums → $N_f$ (now $N_f/2$ per SMQCD quark class in the two-loop route), others → 1). **Same-bug-class candidates:** `SMQCDRRatioObservableConventionLedger` (`rratio_driver.wl:327`) still applies $\tilde A_4^0\times\tfrac12$ and $C_4^0\times2$ — undocumented in Ch5; investigate/document (validation chapter depends on it).

**21 Sep, 14:1x — fix VERIFIED in a fresh kernel by Claude (`wolframscript`, `Get[AntennaPipeline.wl]` from `~/Desktop/AntCalc`, commit `c5df6d3` "patched discrepancy with Nf in A22", 14:06):** $N_f$ `Out[4]` coefficient of `A4` is now exactly **half** the old one, and `Out[5]` is **unchanged** ($-\tfrac1{4\epsilon^3}-\tfrac1{9\epsilon^2}+\tfrac{65+9\pi^2}{216\epsilon}+\tfrac{4085-546\pi^2+72\zeta_3}{1296}$). Henrique's own 14:04 notebook run showed the OLD coefficient (unchanged) because his kernel loaded the *installed paclet* `AntCalc 0.3.1-beta.2` (`PacletFind["AntCalc"]`), not the repo — README: `<< AntCalc`` uses the installed copy; after editing the repo either `Get[FileNameJoin[{repoRoot,"AntennaPipeline.wl"}]]` or rebuild+reinstall the paclet (`CreatePacletArchive`, `PacletInstall`, `PacletDataRebuild[]`, restart kernel). **Implication:** the thesis's printed outputs may have been generated with an installed paclet older than the repo — regression run of the other components on `c5df6d3` started (A22 Leading/Subleading/Breve; A31/A40 sets and R-ratio still to do). **Applied to the thesis (Mine, factual):** (1) §4.2.3 $N_f$ `Out[4]` coefficient halved; (2) Ch3 l.10 and l.871 now cite AntCalc 0.3.1β3, commit `c5df6d3` (update if re-committed; `version.wl` still has no release-history entry describing the $N_f$ fix); (3) label `eq:SGamma` added. **Drafted by Claude at Henrique's explicit request (single-use): the Q4 "three further steps" paragraph in §4.2.1 right after the Lead `Out[4]` block** (S_Γ→S_ε conversion $e^{2\gamma_E\epsilon}/\Gamma^2(1-\epsilon)$ + constant loop factors cancelling $1/\pi^4$; timelike continuation $(-q^2)^{-2\epsilon}\to(q^2)^{-2\epsilon}\cos2\pi\epsilon$, none for $\breve A_2^2$; integrated $A_2^1$ counterterm for Lead/$N_f$). Q4 is now closed. **Ledger question (Henrique: "weren't ×½ Ã40 and ×2 C40 discussed?"):** discussed 7–8 Sep as council finding F1 ("undocumented", "plausibly real symmetry factors") and left open in Claude's notes; never resolved and **not in the thesis**: Ch3 l.713 mentions an "observable convention bridge" in one sentence, Ch5 never mentions it, Appendix B only notes $C_4^0$'s collinear limit carries an explicit identical-particle $\tfrac12$. Code (`rratio_driver.wl:327`): `A40Leading 1, A40Subleading 1/2, B40 1, C40 2, A22OneLoopSelfPoleShift 0`. Still open [Yours]: derive/justify each multiplier from the subtraction formula (0505111 §9) and document in Ch5 (one paragraph + small table).

**21 Sep (latest) — Q10 and Q11 DONE, and a SERIOUS OPEN ISSUE from the regression run.** *Q10 ($R_3$):* verified numerically that (a) the thesis's analytic $R_3$ equals $P_3$ of hep-ph/0311276 (A.8) exactly (ratio 1), and (b) the corrected $d$-dimensional parametrisation $(2\pi)^{3-2d}2^{-1-d}(q^2)^{\frac{2-d}2}\Omega_{d-1}\Omega_{d-2}\int d\mathcal S_3\,(s_{12}s_{13}s_{23})^{\frac{d-4}2}\delta(q^2-s_{123})$ also equals $P_3$ at $q^2=1,2$ (the OLD exponent $(q^2)^{2-d/2}$ gives ratio 2 at $q^2=2$, confirming the $d\Phi_3$ fix) → the last line of the $R_3$ equation replaced accordingly, marker removed (the middle, energy-variable line from TM_JoanaReis was not checked and is untouched). *Q11 ($J$-steps):* added the completeness relation $J(a,b,c)=J(a{+}1,b,c)+J(a,b{+}1,c)+J(a,b,c{+}1)$ and the line $J(1-\epsilon,-\epsilon,1-\epsilon)=2J(1-\epsilon,-\epsilon,2-\epsilon)+J(1-\epsilon,1-\epsilon,1-\epsilon)=\tfrac{3\epsilon-2}{\epsilon}J(1-\epsilon,1-\epsilon,1-\epsilon)$; all relations (and the thesis's two IBP lines and final result) verified via the Dirichlet closed form to $10^{-29}$; Henrique explicitly authorised adding them (Prof instruction). Build 139 pp clean. *Clarified with Henrique:* $\bar\Gamma$ definition = Prof instruction ("Sugiro colocares depois da equação (4.8)"), not yet added (marker stays); $d\Phi_4$ is NOT from Prof (he only said to check $d\Phi_2$'s $q^2$ dependence) — my own finding, source-verbatim, left as is with marker; Feynman diagrams ARE from Prof (general note + his texts with "Fig. X" for $A_2^1$, $A_2^2$, $\hat A_2^2$). **OPEN — REGRESSION:** fresh-kernel run on `c5df6d3` (`Get[AntennaPipeline.wl]`): Breve and $N_f$ agree with the thesis/literature, but **Leading and Subleading do not**: Leading $\tfrac1{4\epsilon^4}+\tfrac{25}{12\epsilon^3}+\tfrac{67-12\pi^2}{24\epsilon^2}+\ldots$ (thesis & hep-ph/0403057 Eq. 4.9: $\tfrac{17}{8\epsilon^3}$, $\tfrac{433-72\pi^2}{144\epsilon^2}$), Subleading $-\tfrac{3}{8\epsilon^4}-\tfrac{21}{16\epsilon^3}+\ldots$ (thesis/literature: $-\tfrac14$, $-\tfrac34$). Cause not yet identified; the only numeric change in the commit for integration is the $A_4$ runtime prefactor ($-1/4\to-1/2$), so Lead/Sub presumably contain labels using the runtime $A_4$. **Ch3 now cites `c5df6d3` — do not rely on that citation until Lead/Sub are resolved.** Needs Henrique's own run of Lead/Sub in a session that really loads the repo.

**21 Sep (later) — $\Gamma$-bar added; ledger multipliers VERIFIED from the primary sources.** *$\bar\Gamma$:* definition added after Eq. (4.8) at Prof's instruction (drafted by Claude on Henrique's explicit request, single-use); "which in total becomes" changed to "In total, the squared amplitude then becomes" for grammar; marker removed; 4 markers remain ($d\Phi_4$, three "Fig. X"). *Table 4.1:* only the caption changed (the $B_0,C_0$ note); entries untouched and consistent with the four printed master combinations; held until the Lead/Sub regression is resolved. *Ledger ($\tilde A_4^0\times\tfrac12$, $C_4^0\times2$) — Henrique's justification checked against the papers (pages read as images):* hep-ph/0505111 Eq. (5.27) $A_4^0$ = 2 ordered sub-antennae; (5.28) $\tilde A_4^0$ = 4 sub-antennae ("all four $\tilde a_4^0$ yield identical integrals"); (5.32) $\tilde{\mathcal A}_4^0=2\,(s_{1234})^{-2\epsilon}[\tfrac1{2\epsilon^4}+\tfrac3{2\epsilon^3}+\ldots]$; (5.44) $\mathcal C_4^0=\tfrac12(s_{1234})^{-2\epsilon}[\ldots]$. hep-ph/0403057 Eq. (4.53): the $\tfrac1N$ bracket of $T_{q\bar qgg}^{(6)}$ is *exactly the negative* of the bracket inside (5.32) (all five coefficients) $\Rightarrow$ $-\tfrac1N\cdot\tfrac12\tilde{\mathcal A}_4^0$; Eq. (4.51): the identical-flavour term is exactly the negative of the bracket inside (5.44) $\Rightarrow$ $-\tfrac2N\mathcal C_4^0$. **AntCalc's printed public integrated series (Ch4 §4.4.2, §4.4.4) equal the paper's $\tilde{\mathcal A}_4^0$ (with its explicit 2) and $\mathcal C_4^0$ (with its explicit ½) exactly**, so the ledger multipliers are literature-derived conversions, not fits. Not re-checked by Claude: Eqs. (5.42) and (9.12). **Remaining gap [Yours]:** Ch5's printed assembly (`05-validation-r-ratio.tex` l.107, l.202, l.238) uses unit coefficients, $-\tfrac1N\tilde A_4^0$ and $-\tfrac1N C_4^0$, whereas `BuildRRatio` applies ×½ and ×2 (`rratio_driver.wl:327`) — the printed formulas are not what is computed until this is stated (state the factors and their origin: colour ordering / photon-like unordered gluons / identical-gluon phase-space counting for $\tilde A_4^0$; identical-flavour-only interference for $C_4^0$; cite the equations above). Closes council finding F1's second half.

**21 Sep (latest) — Henrique's own run CONFIRMS the regression** (new version, his kernel): Leading $\tfrac1{4\epsilon^4}+\tfrac{25}{12\epsilon^3}+\tfrac{67-12\pi^2}{24\epsilon^2}+\ldots$, Subleading $-\tfrac3{8\epsilon^4}-\tfrac{21}{16\epsilon^3}+\ldots$ (disagree with thesis/literature); $N_f$ and Breve agree; his `Out[8]` for $N_f$ shows the halved $A_4$ coefficient. **Henrique is doing the diagnosis himself; Claude must not advance the analysis.** (Claude had only computed, offline, $\Delta$Lead and $\Delta$Sub relative to the printed series and to their $A_4$ terms; no conclusion drawn.) **Thesis changes made this turn, per Henrique:** (1) `05-validation-r-ratio.tex` Eq. (5.14): $r_\text{NNLO}$ now has $-\tfrac1N(\ldots+\tfrac12\tilde{\mathcal A}_4^0+2\,\mathcal C_4^0-\ldots)$ — only the live integrated assembly changed; the unintegrated $T$-objects at l.95–108 are integrand-level identities and stay (the ½ and 2 arise on passing to the paper's integrated antennae); the dead `\iffalse` block l.123–219 untouched. (2) Table 4.1: the four $B_0,C_0$ cells of the $A_2^2$ and $\hat A_2^2$ rows now carry $^\dagger$, caption reworded to "entries marked $^\dagger$ arise only from the $A_2^1$ ultraviolet counterterm…" (Claude's reading of "change the table itself"; may need redirecting). **Open question to Henrique:** add 1–2 sentences of justification after Eq. (5.14) citing hep-ph/0505111 (5.27),(5.28),(5.32),(5.44) and hep-ph/0403057 (4.51),(4.53)? Claude recommends yes, briefly; not drafted. **Consequences to re-check once Lead/Sub are resolved:** Ch3 cites `c5df6d3`; Ch4 §4.2.1/§4.2.2 Out[2]/Out[4]/Out[5] and Table 4.1; Appendix A $A_2^2$/$\tilde A_2^2$; Eq. (5.15) claims `BuildRRatio[SMQCD]` reproduces the exact coefficient. **Feynman diagrams:** Henrique not sold; Claude proposed ONE compact 4-panel figure (A21 vertex; planar ladder; crossed ladder; closed-quark-bubble on gluon) via tikz-feynman, ≈1/8 page — decision pending.

**21 Sep (latest) — ledger justification ADDED to Ch5** (drafted by Claude on Henrique's explicit request, single-use): one paragraph after Eq. (5.14) in `05-validation-r-ratio.tex` stating that the $\tfrac12$ and $2$ follow from the literature definitions: $\tilde A_4^0$ photon-like unordered gluons → four sub-antennae with identical tripole integrals vs two ordered for $A_4^0$ (0505111 (5.27),(5.28)), explicit overall 2 (5.32), inclusive $q\bar qgg$ contains exactly half (0403057 (4.53)) → $\tfrac12\tilde{\mathcal A}_4^0$; $C_4^0$ identical-flavour-only interference, explicit $\tfrac12$ (5.44), inclusive identical-flavour term contains twice (4.51) → $2\mathcal C_4^0$; AntCalc's integrated antennae follow these definitions. Citations render as [5] (0505111) and [24] (0403057). **Deliberately not asserted:** the *reason* for the inclusive-level ½ ("identical-gluon phase-space counting" in Henrique's note) — not verified in the papers; add only if a passage can be pointed to. Build 139 pp clean.

**Thesis items that go stale after the fix:** §4.2.3 `Out[2]`/`Out[4]` ($N_f$), Appendix A $\hat A_2^2$ built expression, and the version/commit citation (Ch3 l.10 and l.871 say 0.3.0β2 / commit `4ceb991`, but the printed outputs come from 0.3.1β…); pick the final tag/commit to cite once the fix is in.
  - `ask-codex` usage logged in memory (`ask-codex-antcalc-oracle`).
- **Q5 — "Is there no valid $d$-dimensional expression for $A_6$, only the Laurent expansion in $\epsilon$?"** *Context:* §4.2.5.4, end of the MI section. *Henrique's answer:* none known; check. *Found:* Gehrmann–Huber–Maître (hep-ph/0507061, Eq. 5) give it **exactly in $\epsilon$** ($\Gamma$'s + two ${}_3F_2$ + one ${}_4F_3$ at unit argument); Gonsalves (PRD 28, 1983) had only $\epsilon^0$. Verified numerically: reproduces $-1,0,5\pi^2/6,27\zeta_3,23\pi^4/36$ and their $\epsilon^1$ term $117\zeta_5-8\pi^2\zeta_3$. *Changed:* DONE (already in the source) — MB block replaced by `eq:A6exact` + the Laurent series, both cited `GehrmannDeRidder0507061`. **Deviates from Prof's suggested wording (he wrote Mellin–Barnes) — tell him.**

**Embedded "are you sure / check this" questions:**

- **Q6 — "Is the section number 4.4.5.3 correct? (…means the text is too fragmented)."** *Context:* intro paragraph pointing to the $R_{8,a}$ derivation. *Grounded:* the number *is* correct in the compiled draft (`main.toc`: 4.4.5.3 = $R_{8,a}$); his point is the fragmentation. *Changed:* his ToC has no numbered subsubsections → applied (see B); the pointer now goes to Subsection 4.4.5 (label `subsec:fourPartonMI`).
- **Q7 — "Check the $d\Phi_2$ equation, there seems to be a typo in the $q^2$ dependence."** *Context:* Eq. (4.2) (phase-space measures, cited [13,16]). *Grounded vs hep-ph/0311276 (A.3)–(A.5):* the $d\Phi_2$ line is **correct** (integrates exactly to $P_2$). The real $q^2$ typo is in **$d\Phi_3$**: thesis $(q^2)^{2-d/2}$, source $(q^2)^{\frac{2-d}2}$; confirmed by dimensional analysis and by Ch4's own $\mathcal A_3^0$ integral, which uses $(q^2)^{1-d/2}$. **$d\Phi_4$:** thesis $(q^2)^{3-d/2}$ is verbatim the source's (A.5), but by dimensional analysis the prefactor must be $(q^2)^{\frac{2-d}2}$ for $\Phi_4\propto(q^2)^{(3d-8)/2}$ — probably a typo in the paper too; **not checked against a second source.** *Changed:* $d\Phi_3$ fixed (applied); $d\Phi_4$ left as is — **[Yours], decision.**
- **Q8 — "Eq. (4.7): why is the gluon polarisation vector not conjugated in the second diagram but conjugated in the first (and the opposite for the off-shell photon)?"** *Grounded:* Prof is right — typo. Outgoing gluon → $\varepsilon^*(k_3)$, incoming photon → $\varepsilon(p)$ in **both** diagrams; AntCalc's own Out[3] "Amplitude" has it right. The relative minus sign is correct (antiquark propagator carries $-(k_2+k_3)$). *Changed:* fixed in Eq. (4.7) and in the $M_B$ definition (applied).
- **Q9 — "You never define $\bar\Gamma$? And when conjugating, the quark colour indices swap because $T^a$ is Hermitian."** *Grounded:* both confirmed. $\bar\Gamma=\gamma^0\Gamma^\dagger\gamma^0$ is defined nowhere; the colour contraction $(T^{a_3})_{i_1i_2}(T^{a_3})_{i_1i_2}$ (three places: before (4.9), the sum line, and (4.14)) must be $(T^{a_3})_{i_1i_2}(T^{a_3})_{i_2i_1}=\mathrm{Tr}(T^aT^a)=\tfrac12(N^2-1)$ (value already right). *Changed:* colour indices fixed (applied). **$\bar\Gamma$ definition [Yours]** (a sentence after (4.8); marked in source).
- **Q10 — "Are you sure Eq. (4.24) is correct? You lost $(s_{12}s_{13}s_{23})^{\frac{d-4}2}$."** *Grounded:* right — the line `R3 = 1/(128 q²π³)∫dS3 δ(...)` is the $d=4$ form. *Changed:* the surrounding restructure applied (see B); the equation itself **[Yours]**, left with a source marker. It follows from the corrected $d\Phi_3$.
- **Q11 — "Can you review (4.31)–(4.33)? Nowhere do you say how you go from $J(1-\epsilon,-\epsilon,1-\epsilon)$ to $J(1-\epsilon,1-\epsilon,1-\epsilon)$."** *Grounded:* real gap; the final result is right. The first IBP produces $J(1-\epsilon,-\epsilon,1-\epsilon)$, but the second is applied to $J(1-\epsilon,-\epsilon,2-\epsilon)$ (different $c$), and the symmetry line doesn't connect them. Missing relation: insert $y_{13}+y_{23}+y_{12}=1$, i.e. $J(a,b,c)=J(a{+}1,b,c)+J(a,b{+}1,c)+J(a,b,c{+}1)$. At $(1-\epsilon,-\epsilon,1-\epsilon)$, with $J(2-\epsilon,-\epsilon,1-\epsilon)=J(1-\epsilon,-\epsilon,2-\epsilon)$: $J(1-\epsilon,-\epsilon,1-\epsilon)=2J(1-\epsilon,-\epsilon,2-\epsilon)+J(1-\epsilon,1-\epsilon,1-\epsilon)=\frac{3\epsilon-2}{\epsilon}J(1-\epsilon,1-\epsilon,1-\epsilon)$, hence $J(-\epsilon,-\epsilon,2-\epsilon)=\frac{(1-\epsilon)(2-3\epsilon)}{\epsilon^2}J(1-\epsilon,1-\epsilon,1-\epsilon)$ ✓ (also confirmed by the Dirichlet closed form $J=\Gamma(a)\Gamma(b)\Gamma(c)/\Gamma(a{+}b{+}c)$). *Changed:* nothing — **[Yours]**, add the step (marked in source).
- **Q12 — "'they show the expressions before dimension regularisation is applied' — how so? It is applied from the start."** *Grounded:* Prof is right, the sentence was misleading. In Out[7] the coefficient $4+d(\epsilon-2)-4\epsilon$ at $d=4-2\epsilon$ equals $-2(2-2\epsilon+\epsilon^2)$, i.e. the factor in Eq. (4.36): $d$ comes from the IBP coefficients (LiteRed2 keeps $d$), $\epsilon$ from the trace algebra. His replacement wording is consistent with this → applied.
- **Q13 — "'usual' polygamma identities — why usual?"** *Changed:* "usual" dropped (applied).
- **Not a question but a conflict:** Prof's suggested §4.2.1 sentence lists $A_6$ among the leading-colour masters; the thesis's own Table 4.1 and Out[4] have none — $A_6$ appears only for $\tilde A_2^2$. Applied **without** $A_6$ for Lead.

#### B. [Mine] — applied 21 Sep 2026 (`chapters/04-worked-example.tex`, plus two one-line repoints in Ch2 and Appendix B)

Build afterward: 138 pp, **0 undefined refs, 0 multiply-defined labels** (needed the usual stale-`.aux` cleanup first: a corrupted aux file made the first rebuild fail with `Extra }` at `l.402 138}`; `find . -maxdepth 2 -name "*.aux" -not -path "./build/*" -delete` + removing `main.toc/lof/lot/bbl/bcf/blg/out/run.xml` fixed it — not a content bug). Rendered pages checked (chapter opener, Fig. 4.1 + Eqs. 4.7–4.9, colour sum 4.14, $A_2^1$ poles + §4.2 opener, $A_{22,\mathrm{LO}}$/$A_3$/$A_4$ page). New Ch4 overfull boxes: none (the remaining ones are the pre-existing `In[3]:=` code lines). The ToC for Ch4 now matches Prof's proposed structure line for line (4.1 … 4.6).

*Prof's literal text pasted in (transcription):* chapter title → "Calculation and Integration of QCD Antenna Functions"; the five-paragraph chapter intro (his P1–P5; P5 after Table 4.1); the PV/IBP paragraph, the "Since Passarino-Veltman reduction …" paragraph (now points to Subsection 4.4.5 via new label `subsec:fourPartonMI`), the phase-space-multiplicity paragraph; "Using this notation, …"; the $\Delta_4$/$\Theta$/$d\Omega_n$ wording; the normalisation-factor wording (4.5)/(4.6); the LiteRed2 `j[…]` paragraph (→ Appendix B.7); §4.1 opener; the $A_3^0$ description + Fig. 4.1 caption + "Applying the QCD Feynman rules …"; the colour-index sentence (→ §2.4.3, `subsec:colourAlg`); "Summing the squared matrix element over the final-state colour indices gives," and "and the tree-level squared matrix element is obtained in $d$ dimensions …"; the Born-normalisation paragraph; the "electric charge cancels …" paragraph; §4.1.1.2 opener (with $d\Phi_{X_{ijk}}$ restored in the equation and in "with $d\Phi_{X_{ijk}}=d\Phi_3/\Phi_2$"); "For $A_3^0$, the phase-space integration can be performed directly …"; the $R_3$ restructure (old subsection "The $R_3$ Integral" dissolved, duplicate analytic $R_3$ equation removed, `eq:R3` label moved to the analytic result, closing sentence added); IBP-example lead-ins, "polygamma identities … have been used", and the physics comment after the Laurent series (new label `eq:A30Laurent`); the whole "Implementation in *AntCalc*" set (IntegrableForm paragraph, "As with BuildAntenna …", "At this intermediate stage …", "The complete construction and integration procedure …", closing validation paragraph); "Altarelli-Parisi (AP)"; the $A_2^1$ paragraph, integrated-result lead-in, and pole-cancellation comment (old "PaVe scalar functions appear in both …" paragraph deleted, as he said); the §4.2 opener (two paragraphs); the four $A_2^2$-component texts + their interpretation comments; the $\breve A_2^2$ source-note text (subsection dissolved into the text); the master-integral intros for $A_{22,\mathrm{LO}}$, $A_3$ (now four separate equations), $A_4$ (now five equations), $A_6$; all section/subsection titles per his proposed ToC.

*Verified typo fixes (Mine, mechanical):* $d\Phi_3$ exponent $(q^2)^{2-d/2}\to(q^2)^{\frac{2-d}2}$ (Q7); Eq. (4.7) and the $M_B$ definition: $\varepsilon^*(p)\leftrightarrow\varepsilon(k_3)$ swap (Q8); colour contraction $(T^{a_3})_{i_1i_2}(T^{a_3})_{i_1i_2}\to(\ldots)_{i_2i_1}$ at all three places (Q9); `+...` → `+\cdots` in the four $A_2^2$ `Out[2]` blocks (his text says "$\cdots$ denoting terms omitted").

*Structural / cross-reference work (checked for dependencies before doing it — this is what would have made it non-mechanical):* all 18 remaining Ch4 `\subsubsection` → `\subsubsection*` (unnumbered, bold, no ToC entry). **Scope extension, flagged:** Prof named only 4.1.1.2/.4/.6 and the 4.2.5.x master-integral ones, but his proposed ToC has no subsubsection level at all, so applied to all for consistency. 8 stale labels dropped; every reference that used them repointed: Ch4 (intro paragraph → `subsec:fourPartonMI`; "Every step in Subsubsection …" → "Every step of the analytic integration above"; $V_{5,b}$ section → `subsec:fourPartonMI`; $A_4$ note → `subsec:A22mi`; $R_{8,b}$ note → `subsec:A22mi` + new `subsec:A31mi`; massive-$A_3^0$ build text → `sec:matDerivation`), **Ch2 l.1760** footnote → `sec:matDerivation`, **Appendix B l.9** → `sec:matDerivation`, **Appendix B l.705** → new `subsec:a30LocalValidation` (label added on the renamed "Local Infrared Validation" subsection). One reference (`subsec:masslessA30`, Ch4 + Appendix B) was missed by my first dependency grep and caught only by the build's undefined-ref report — again "a clean build ≠ nothing broken" in reverse: here the build *did* catch it.

*Deviations from Prof's literal text (all deliberate, mention if asked):*
1. **$A_6$ removed from the leading-colour master list** (his sentence lists $A_{22,\mathrm{LO}},A_3,A_4,A_6$; the thesis's own Table 4.1 and Out[4] have no $A_6$ for Lead — only $\tilde A_2^2$ does).
2. **$A_6$: exact expression instead of his Mellin–Barnes sentence** (Henrique's decision, hep-ph/0507061 Eq. 5, verified); his first sentence ("The final master integral required for the $A_2^2$ antenna set is $A_6$, …") and "neither loop integration reduces directly …" are kept.
3. His "$d=4-\epsilon$" → **$d=4-2\epsilon$** (the thesis convention; his was a slip). "normalization" → "normalisation"; en dash in "Passarino–Veltman" → hyphen (house style).
4. His $A_4$ text has a blank after "The two propagators depending on $\ell$,"; **filled with the display $1/[\ell^2(p-\ell-k_1)^2]$** (taken from the definition).
5. His three "shown in Fig. X" clauses ($A_2^1$ diagram, two-loop $A_2^2$ diagrams, $N_f$ quark-loop diagrams) are **omitted** (the figures don't exist; a dangling ref would print "??"); each spot carries a `% TODO[Yours]` comment saying what to re-add.
6. His "Sec. 4.2.5" / "Sec. 4.6" → `\ref` links; "Eq. (4.19)" → `\eqref{eq:A30}` etc.
7. The $\breve A_2^2$ source-note text **replaces** the old subsection wholesale, so the old "$28C_F^2\zeta_3/(3\epsilon)$ residue" sentence is gone (his replacement doesn't have it). Henrique may want it back — [Yours] call.
8. Two small sentences of mine, not his: "Every step of the analytic integration above …" (needed once the subsubsection lost its number) and "The integrated Laurent series … apart from the exception discussed below." (the old sentence with its dead `\ref` removed; kept so the literature-agreement statement survives).
9. His PV/IBP paragraph says IBP is used "in the analytic integration"; for $A_2^2$ it is also used on the *build* side (his own §4.2.1 text says so). Consistent enough, left as he wrote it. Likewise "AntCalc retains the complete expression, which is used in the subsequent integration" — per the code the integration uses the pre-counterterm prototype payload, not the displayed public expression; left as he wrote it.

#### C. [Yours] — open (each has a `% TODO[Yours]` marker in the source where it belongs, except where noted)

- ~~**Y1 — Ch3 ~l.335 (Q2)**~~ **DONE 21 Sep, drafted by Claude at Henrique's explicit request (single-use permission, not standing).** `chapters/03-package-framework.tex` §3.x "Two-Loop Routes": the false clause ("Passarino-Veltman reduction is used on the build side to remove the two loop momenta") and the following "remarkably different" sentence replaced by: the two-loop source is *not* PV-reduced; $\ell_1,\ell_2$ are removed by LiteRed2 IBP + literature-master substitution → public result in $s_{12}$ and $\epsilon$ only; PV enters only via the $A_2^1$ UV counterterm attached to the Lead and QuarkLoop components at the public build boundary (points to `sec:buildStage`, whose existing renormalisation paragraph already describes that boundary). Also consistent with Ch3's own l.132 (PV reduction profile only for `{A,2,1}`,`{A,3,1}`), which the old l.335 had contradicted.
- ~~**Y2 — Q3**~~ **DONE 21 Sep, drafted by Claude at Henrique's explicit request (single-use).** (a) new paragraph in §4.2.1 right after the truncation note: the first displayed term is the coupling-renormalisation counterterm $=-\tfrac{11}{6\epsilon}A_2^1$ (uses $A_2^1=-\tfrac12[(3+2\epsilon)B_0+2s_{12}C_0]$, new label `subsec:A21` on §4.1.3), the only place $B_0,C_0$ enter; the omitted terms are the genuine two-loop part, IBP-reduced and master-substituted; same holds for $\hat A_2^2$ ($+\tfrac1{3\epsilon}A_2^1$). (b) Table 4.1 caption note: for $A_2^2$ and $\hat A_2^2$ the build-side $B_0,C_0$ come only from that counterterm. Decision recorded: PaVe stays in the package (needed for $A_2^1$/$A_3^1$ and the counterterm); only the *claim* that PV reduces the two-loop source was removed. The sentence about $\hat A_2^2$ in the §4.2.1 paragraph is a small addition beyond the Lead-only question Prof asked, to pre-empt the same question for §4.2.3.
- **Y3 — Q4:** decide how to describe `A22Lead` Out[4] vs Out[5]; needs a live AntCalc run (paste raw Out[4]/Out[5] for `Component->Subleading`) or `ask-codex` (not used so far).
- **Y4 — Q7:** $d\Phi_4$ prefactor `(q^2)^{3-d/2}` (marker above `eq:phaseSpaces`).
- **Y5 — Q9:** define $\bar\Gamma$ after Eq. (4.8) (marker before "which in total becomes").
- **Y6 — Q10:** $d$-dimensional $R_3$ parametrisation (marker above the three-line $R_3$ equation).
- **Y7 — Q11:** missing $J$-step (marker before "By the symmetry $y_{13}\leftrightarrow y_{12}$").
- **Y8 — figures:** representative Feynman diagrams for $A_2^1$, the two-loop $A_2^2$ set, and the $N_f$ quark loop; then re-add the three omitted "Fig. X" clauses.
- **Y9 — "PaVe" abbreviation:** gone from live Ch4 prose except two cells of the §4.6 summary table (`longtable`, narrow columns — left alone since §4.6 has no feedback yet); still present in Ch2 (4×), Ch3 (1×), Appendix B (9×, incl. the section title "PaVe Reduction" — the `\label{subsec:PaVe}` uses are fine). Decide whether Prof's "avoid it" applies thesis-wide.
- **Y10 — tell Prof:** $A_6$ now exact (Eq. 5 of hep-ph/0507061) instead of MB; $A_6$ is not a leading-colour master.
- **Y11 — pending feedback:** §4.3–4.6. Consistency items to revisit then: `+...` → `+\cdots` in the $A_3^1$/$A_4^0$/massive `Out[2]` blocks; same interpretation-comment + diagram treatment; Table 4.1 caption note.
- **Y12 — Sampaio:** if Ch4 has already gone to Prof. Sampaio, list everything in this block for him.

---

### Prof. Pires "gralhas" (typos/small errors) on Chapter 2 — 16 September 2026 — 10/10 DONE

All ten points grounded against `chapters/02-physics-background.tex` before touching anything; nine were literal or unambiguous mechanical fixes, applied and build-verified (visually spot-checked the rendered PDF pages for both equations, the table, and Figure 2.3's new caption).

**[Mine] — applied:**
- Eq. (2.32) (the NLO integrated-counterterm equation, `\int_1 d\sigma^S_\text{NLO}=...`): was one overlong line running past the margin; split into two lines with `aligned`, mirroring the identical break-point style already used for the equation immediately above it.
- Eq. (2.34) (the NNLO `d\sigma_\text{NNLO}` master equation): had a genuine extra `=` — a third line re-asserting `d\sigma_\text{NNLO} = \int_m(...)` alone, silently dropping the `\int_{m+2}(RR-S)` and `\int_{m+1}(RV-T)` terms already on the line above. Deleted that redundant final line (confirmed with Henrique's framing: this was simply wrong as printed, not an intentional second equality), kept the two-line `aligned` version, added the closing period.
- Table 2.2 caption: "matching subterm" → "matching subtraction term" (the table's own column header already said "Subtraction Term"; only the caption still had the old word).
- Figure 2.3 caption: added Prof's exact (a)/(b)/(c) → $d\sigma^{S,b}/d\sigma^{S,c}/d\sigma^{S,d}$ correspondence. Cross-checked against Table 2.2's own descriptions first ("two colour-connected" = S,b; "almost colour-unconnected" = S,c; "fully colour-unconnected" = S,d) — consistent.
- "And then the real-virtual counterterm," → "and for the real-virtual counterterm,".
- $d\sigma^{S,c}$/$d\sigma^{S,d}$/$d\sigma^{T,c}$ paragraph: replaced with Prof's literal supplied text (drops the old "their complete form is written in [Gehrmann-DeRidder:2005btv]" clause — deliberate, per his wording, not an oversight; citation is used dozens of other places in the chapter so nothing is orphaned).
- Deleted the "iterated mapping for the four-parton system... shown in Fig. 2.3" paragraph + its unlabelled two-stage-mapping equation immediately following the point above, per Prof's own reasoning (it only motivated the now-excluded $S,c/S,d$ configurations). Grounded first: `fig:colourConnectionNNLOall` already has an earlier inbound reference (line 621, before Table 2.2) so the figure isn't stranded; grepped for "iterated mapping" / the $\tilde{\tilde I}$ notation thesis-wide — used nowhere else. Equation numbers after this point shift down by one; nothing hardcodes the old numbers.
- "Following QCD vertex rules" → "Following QCD Feynman rules" (also now matches the actual section name it points to, Appendix B.1 "Feynman Rules").
- Deleted the $A_2^0$ "Born-level antenna, not a subtraction ingredient" sentence per Prof (adds nothing — $A_2^0$ is trivially 1).
- Full rebuild clean afterward: 126 pp, 0 undefined refs, 0 multiply-defined labels.

**Figure 2.2 (`fig:colourConnectionNLO`) — redrawn, resolved. Two rounds: first attempt was wrong, corrected once Henrique clarified what Prof actually meant.**

Round 1 (wrong): read Prof's 9 Sep 2026 point as "redraw like Fig. 7's left panel" and built a simplified 2-diagram version (blob with 3 legs $\to$ blob with 2 legs, vertical arrow). Henrique corrected this directly — the ask is to reproduce the *whole* factorisation figure (Fig. 3 of hep-ph/0505111, Gehrmann-De Ridder/Gehrmann/Glover, is the exact NLO match — same $i,j,k\to I,K$ structure as our own diagram, confirmed by pulling the actual paper page), including the generic spectator legs ($1,\ldots,i,j,k,\ldots,m+1$) and the square-bracket antenna-factorisation term (two smaller "cut" blob-diagrams for $X_{ijk}^0$ itself, dashed incoming line, horizontal divider), not a stripped-down version.

Round 2 (current, correct): rebuilt `figures/colour-connection-nlo.tex`'s right-hand panel as a full reproduction of the paper's Fig. 3, rotated from the paper's horizontal layout to vertical — top: full $(m+1)$-parton amplitude blob (legs $1,\ldots,i,j,k,\ldots,m+1$); vertical arrow down (Henrique's own practical call to solve the page-width problem, mirroring the arrow orientation already used by the colour-connection panel on the left); bottom: reduced $m$-parton blob ($1,\ldots,I,K,\ldots,m+1$) plus, in square brackets alongside it, the antenna function $X_{ijk}^0$ drawn as its own two-part cut diagram (mini-blob with legs $i,j,k$ / horizontal divider / mini-blob with legs $I,K$, each with a dashed incoming stub).

Implementation notes for future figure work:
- Dropped tikz-feynman for this diagram — plain TikZ `\draw`/`\fill`/`\node`, matching the paper's own plain-line style (no photon/gluon/fermion arrows in the original) and avoiding tikz-feynman's `[blob]` vertex style, which rendered invisibly at this scale (worked around earlier by manually filling a circle on top of the vertex point — same trick reused here).
- A short `dashed` stub (~0.6 local units, scale factor 0.62 applied to the whole diagram) rendered as a single tick, not a visible dash pattern — fixed with an explicit `dash pattern=on 1.2pt off 1pt` (absolute pt, unaffected by the local coordinate scale) instead of the bare `dashed` style.
- The nested `\node[right] at (x,y) {...}` bounding box is centred on content that isn't itself symmetric about local $(0,0)$ (the composite extends further below the top blob's centre than above it), so the node's vertical centring doesn't land where you'd naively expect — had to nudge the placement empirically against rendered crops, not compute it from the local coordinates.
- Iterated against actual rendered PNG crops at each step (not just "does it compile") — caught the invisible blob, the collinear gluon/photon lines, and the too-short dash pattern this way, none of which show up as compile errors.

Round 3 (Henrique's correction against the actual rendered PDF): round 2 still had real problems, caught by close comparison against a hi-res crop of the actual paper page, not just eyeballing.
- **Brackets were geometrically backwards** — the "[" was drawn with its vertical spine on the *inside* (right) and ticks pointing outward (left), i.e. it rendered as "]", and vice versa for the right bracket. Fixed by rebuilding each bracket's path so the spine sits on the outside edge with ticks pointing inward, the way a real "[ ... ]" is drawn.
- **Bracket content was drastically undersized relative to the main diagrams** — pixel-measured the paper (400dpi crop): the antenna sub-diagrams' leg lengths and blob size are close to the main diagrams', not ~40% of them as the previous attempt had it. Rebuilt with blob radii 2.0mm (main) vs 1.7mm (bracket) and leg length 1.4 vs 1.0 — comparable, not tiny.
- **Main-diagram legs shortened** (leg length 1.4, down from ~1.7–2.9) per Henrique's "reduce their size" note.
- **The "ellipsis" dots were present in the code but invisible** — radius was specified in raw pt *before* the diagram's 0.62 scale factor was applied, working out to under half a point on the page. Fixed (now 2.6pt pre-scale, visibly two distinct dots, matching the paper's placement between the "1"/"i" and "k"/"m+1" legs).
- **Internal (right-panel) horizontal alignment**: put the bottom main blob and its bracket in the same shifted `scope`, so the blob centre, the bracket's vertical centre, and the divider line share the same $y$ by construction rather than by manually matched-up numbers.
- Rebuilt each sub-diagram (top blob, bottom blob, both bracket mini-diagrams) using TikZ polar coordinates (`(angle:radius)`) throughout instead of hand-computed Cartesian points, so the angular layout (matching the paper's own: outer legs at $\pm55°$, inner legs at $\pm20$–$25°$, dot-stubs at $\pm42°$) is explicit and easy to re-tune, rather than opaque decimal coordinates.
- Widened the bracket slightly a second time after the first fix still crowded the $j$/$k$ labels against the right bracket edge.
- Iterated against rendered PNG crops at every step, per file feedback, comparing directly to a 400dpi crop of the actual paper page rather than to memory of it.

Round 4 (Henrique's second correction pass, against round 3's render): two remaining problems, both fixed and pixel-verified against the actual rendered PDF, not just eyeballed.
- **Top diagram + arrow weren't horizontally centred over the bottom row.** The bottom row is much wider than the top diagram (it carries the bracket, which the top diagram has no equivalent of), so anchoring both at the same local $x=0$ (blob-to-blob) left the top block sitting well left of the bottom row's true visual centre. Fixed by wrapping the top blob diagram and the arrow together in one shifted `scope` (`shift={(1.6,0)}`), moving them right as a unit so their combined centre lines up with the bottom row's. Verified by thresholding the rendered PNG and measuring dark-pixel column extents directly: top-block centre and bottom-row centre now agree to within 1.5px out of a ~420px-wide row — not just "looks about right."
- **Ellipsis dots**: matched against a 400dpi crop of the actual paper page pixel-by-pixel. The dots are much smaller than round 3 had them, and — the real bug — they sit at *different angles, similar radius* from the blob (tracing the angular gap between the two flanking legs), not at the *same angle, different radius* (i.e. along a single ray) as round 3 drew them; the latter reads as a "/"-diagonal smear that can look like a merged horizontal blob once dot size and blob leg spacing interact, which is what Henrique was flagging. Rebuilt as two separate small dots at $45°$/$34°$ (and mirrored $-34°$/$-45°$) at matching radius, radius reduced from 2.6pt to 1.6pt — now reads as two clearly separated dots on a shallow diagonal, matching the paper.
- Increased the overall diagram scale from 0.62 → 0.72 per Henrique's explicit go-ahead to let the TikZ picture take up more page space rather than fight for compactness; confirmed it still sits well inside the text width with margin to spare.

Round 5 (Henrique's "let it breathe" pass): purely spacing/sizing, no structural changes.
- Top/bottom main-diagram gap and the arrow between them: bottom block's shift pushed from $(0,-3.8)$ to $(0,-5.6)$ and the arrow lengthened (from $-1.55\to-2.25$ to $-1.5\to-3.3$), so there's visible clearance on both sides of the arrow instead of the two blocks sitting right on top of it.
- Bracket enlarged past the size of the reduced-blob diagram next to it, per Henrique's explicit "it may be bigger, don't be afraid of expanding" — bracket mini-diagram blob radius 1.7mm$\to$2.0mm (now equal to the main blobs), leg length 1.0$\to$1.35, bracket edges widened to $\pm2.0$ (from $\pm1.35$).
- **The actual crowding complaint** (the $k$/$I$ legs sitting right on top of the divider line): root cause was the two mini-diagrams' vertical centres only being $\pm0.65$ apart — moved to $\pm1.0$, which alone roughly doubles the clear gap between each mini-diagram's nearest leg and the divider.
- Diagram scale left at 0.72; page-width margin checked, still comfortable.

Round 6 (Henrique's fine-tune of round 5): arrow was over-corrected — the extra vertical space between the top and bottom blocks was supposed to stay empty, not stretch the arrow itself. Reverted the arrow to its original short length ($0.7$ units) and centred that short arrow within the enlarged gap (now $-2.0\to-2.7$, roughly midway between the top block and the bracket below). Also added explicit clearance the bracket didn't have: between the reduced-blob diagram and the bracket's left edge (gap $\sim\!0.7\to1.1$), between the bracket's own edges and the mini-diagrams inside it (dashed-stub start pushed out, label margin widened), and between the divider line's endpoints and the bracket sides (divider now stops short of the bracket verticals instead of nearly touching them).

Round 7 (Henrique asked directly "are you sure it's centred?" — good catch, it wasn't). Re-measured rather than assumed: the round-4 centering (shift $=1.6$) was computed against the round-3 bracket dimensions. Rounds 5–6 then enlarged the bracket and pushed its right edge out (spine $4.9\to4.65\to6.22$ over those two rounds) without ever recomputing the top block's shift — so by round 6 the top block had drifted about 45px left of the bottom row's true centre on the rendered page. Recomputed from the current geometry (bottom row span $-1.0$ to $6.22$, centre $2.61$; top diagram span $-1.0$ to $\approx\!1.65$, centre $\approx\!0.325$) and updated the shift from $1.6\to2.3$ for both the top-diagram scope and the arrow. Verified by thresholding the rendered PNG and measuring dark-pixel column extents directly (not by eye): top-block centre $\approx$1381.5px, bottom-row centre $\approx$1376.5px, a 5px difference across a $\sim$510px-wide row ($\sim$1%). Lesson for any further edits to this figure: the top block's shift is *derived* from the bottom row's geometry, not independent of it — resize the bracket, and the shift must be recomputed, not left as-is.

Full rebuild: 126 pp, clean, 0 undefined refs, 0 multiply-defined labels.

**Round 8 — post-mortem catch (Henrique, after all 7 redraw rounds): the caption still called the right panel "a schematic Feynman diagram," a leftover from the original (pre-redraw) figure.** The panel is now an amplitude-level factorisation diagram (blob + legs), not a Feynman diagram at all — genuinely stale wording, not a stylistic quibble. Fixed in `figures/colour-connection-nlo.tex`'s caption: "...accompanied also by the corresponding amplitude-level factorisation, using the momenta labels." Checked the body prose near `Fig.~\ref{fig:colourConnectionNLO}` (chapters/02-physics-background.tex:565) too — it only says "a diagram of these relationships," no stale "Feynman diagram" wording there, so the caption was the only place needing the fix. Full rebuild: 126 pp, clean.

---

### Prof. Pires review of Chapter 1 (Introduction) — 15 September 2026 (email, same batch as the
§2.1.2–2.5/Ch3 reviews) — ALL POINTS RESOLVED same day, including the one initially deferred by Prof

**[Mine] — applied, `chapters/01-introduction.tex`:**
- Opening paragraph + strong-interaction paragraph replaced with Prof's two new paragraphs: LHC/SM
  precision framing, then a proper QCD definition ($SU(3)_c$ non-Abelian gauge theory, quarks in the
  fundamental rep, gluons in the adjoint, Fig.~\ref{fig:SM} pointer).
- Fixed a real physics inaccuracy Prof flagged: the old text said $\alpha_s$ "decreases as COM energy
  increases," which isn't quite right — $\alpha_s$ depends on the renormalisation scale, typically set
  to a hard scale $Q$ of the process. Replaced with Prof's text: asymptotic freedom framing, LO→NNLO
  improving accuracy and reducing renormalisation/factorisation-scale dependence.
- Removed the Table 2.3 antenna-counting sentence entirely, per Prof's reasoning: a raw antenna count
  isn't itself a complexity measure (1000 simple integrals isn't a hard problem) — the real difficulty
  is amplitude/integral complexity at higher orders, and Ch1 shouldn't reference a Ch2 table this
  early anyway. Checked the transition into the next sentence ("Obtaining higher-order perturbative
  predictions is not simply a matter of...") still flows naturally without it — confirmed in the
  rebuilt PDF.
- "such as the hadronic $R$-ratio" → "...defined in Chapter~\ref{ch:validation}" (resolves to Chapter
  5; used the label, not a hardcoded number, consistent with house style).

**RESOLVED same day — not actually deferred.** Henrique had already replied to Prof directly (by
email) that the sentence was stale: Ch4 no longer opens with the master integrals, since he
restructured it so "each set of masters appears after the antennae." Verified this directly against
`chapters/04-worked-example.tex`'s actual section structure before touching anything (§4.2/§4.3/§4.4
each present all antennae in that multiplicity/loop set first, referencing their master integrals by
symbol throughout, then collect the actual master-integral derivations in a dedicated "Master
Integrals" subsection at the end of that section — confirmed by reading the $A_2^2$ set's content
flow directly, not just section headers). Fixed the sentence: "beginning with the master integrals
required for their integration" → "organised by final-state multiplicity, with the master integrals
required for each antenna set presented after the corresponding antennae."

Rebuilt clean (`latexmk -pdf`+`biber`, no errors/undefined refs), visually spot-checked both rendered
pages.

---

### Prof. Pires review of Chapter 3 — 15 September 2026 (email, same batch as the §2.1.2–2.5 review,
also received AFTER v1 sent to Prof. Sampaio) — ALL POINTS RESOLVED same day

**[Mine] — applied:**
- §3.3.3: "There is only, formally, one set of this kind up to NNLO, $A_2^2$." → Prof's literal
  replacement, "Within the massless $q\bar q$ final-final antennae considered up to NNLO, the only
  two-loop antenna set is $A_2^2$." (qualifies the claim to the scope actually covered).
- "The integrated antennae are denoted with calligraphic letters." → added the explicit symbol,
  "...calligraphic letters $\mathcal X_n^l$."

**[Already satisfied, no action needed]:**
- "Eq. 2.47" / "Eq. 2.65" (missing parens) — both already fixed by the thesis-wide `\ref{eq:...}` →
  `\eqref{eq:...}` sweep done during the §2.1.2–2.5 pass (58 instances, applied to every
  chapter/appendix, not just Ch2). Checked the compiled PDF directly: zero remaining bare "Eq. 2.NN"
  anywhere in the thesis.

**All 3 remaining [Yours] items RESOLVED same day (15 Sep) — Henrique supplied the content/direction
for all 3, applied directly:**
1. **[Mine, applied]** FeynCalc/LiteRed2 auto-install — Henrique confirmed the grounded finding was
   right and direct to apply. Added to the end of the sentence at
   `chapters/03-package-framework.tex:12-14`: "Both are external dependencies that must be installed
   by the user prior to loading *AntCalc*; the package's own installation procedure only installs the
   *AntCalc* paclet itself."
2. **[Mine, applied]** Two-loop ($A_2^2$) reduction method — Henrique supplied the actual mechanism
   (matches Table 3.6's `A22TwoLoopTree`/`A22OneLoopSelf` → `IBP`/`LiteRed2` entries exactly, verified
   after the fact): PaVe is used on the *build* side to remove the two loop momenta $\ell_1,\ell_2$,
   then IBP reduction is used to obtain the full $\epsilon$-pole result. Added to §3.3.3 as: "Much
   like for all other loop routes, Passarino-Veltman reduction is used on the build side to remove the
   two loop momenta $\ell_1$ and $\ell_2$, while IBP reduction is subsequently used to obtain the full
   result in terms of its $\epsilon$-poles."
3. **[Mine, applied]** Renormalisation missing from Figs. 3.2/3.5 — discussed the mention-vs-draw
   question with Henrique; agreed on both, kept minimal (append a label to the existing "public
   boundary" node group in each figure rather than adding a new box or figure). Henrique explained the
   actual architecture: renormalisation isn't a pipeline stage shared between Build and Integrate — it
   is applied independently at each call's own backend→public crossing (once for `BuildAntenna`'s
   unintegrated output, once for `IntegrateAntenna`'s integrated output). Applied:
   - Prose: one paragraph after each of Fig. 3.2 and Fig. 3.5, stating this explicitly and pointing at
     the existing boundary crossing shown in each diagram (`chapters/03-package-framework.tex`, right
     after each `\input{figures/antcalc-architecture-...}`).
   - Figures: appended "(incl. UV renorm.)" to the existing "Route \& Public Boundary" label in
     `figures/antcalc-architecture-buildstage.tex` and the existing "Public Integration Views" label in
     `figures/antcalc-architecture-intstage.tex`.

   **Follow-up fix, same day**: the initial 2→3-line label growth clipped into the divider line and
   option items below it in both figures (missed this the first time — claimed "no overlap" without
   catching it; Henrique caught it from the rendered PDF). Fixed by shifting everything from the
   enlarged label's divider line onward down by one line-height (0.4, matching the diagram's own
   item-to-item spacing unit) in both figures: the divider, its 2–3 option items, the *next* options
   group's label/divider/items, and the figures' own bottom-boundary elements (main vertical
   connector lines, Input/Output triangle markers) extended by the same amount so nothing runs past
   the existing background/red-card boundaries. Rebuilt and visually re-inspected both figures at
   150dpi this time (not just checked "does it compile") — spacing between groups now reads
   consistently with the rest of each diagram, no clipping.

**Bonus catch, same pass**: while re-checking the hat-$A_{3,\text{bare}}^1$ sentence from the earlier
§2.1.2–2.5 round (Henrique had independently refined its wording since — now reads "...carries the
on-shell gluon's momentum $p_3$, and $p_3^2=0$..."), found and fixed a typo introduced in that
refinement: "coupling-renormalisation couterterm" → "counterterm"
(`chapters/02-physics-background.tex:1159`).

Rebuilt clean after every step in this Chapter 3 pass (multiple intermediate rebuilds, all
`latexmk -pdf`+`biber`, no errors, no undefined/multiply-defined refs).

---

### Prof. Pires review of §2.1.2–2.5 — 15 September 2026 (email, received AFTER v1 sent to Prof. Sampaio) — DONE (Mine items) + 5 open [Yours]

**Important for Henrique:** this whole review arrived after the first-half draft had already gone to
Prof. Sampaio, so everything in this block needs to be listed for Sampaio separately (not assumed
already seen). Same applies to the Resumo/Abstract block right below, and to whatever Henrique adds
to this chat afterward — keep a running list.

Almost entirely literal supplied replacement text (transcription, not composition) — classified
**Mine**, executed directly in `chapters/02-physics-background.tex` (plus one thesis-wide mechanical
sweep), rebuilt clean (125 pp, `latexmk -pdf`+`biber`, no errors, no undefined/multiply-defined refs,
no `??` in the compiled text). Visually spot-checked the rendered PDF pages for every major
restructuring (§2.1, §2.2.1, §2.3, §2.4.1–2.4.5, §2.5.1).

**[Mine] — applied:**
- §2.1.1: "colour" → "colour charge"; rewrote the confusing "unlike the remaining terms..." vertex
  sentence; rewrote the gauge-fixing/ghost-Lagrangian paragraph with Prof's literal text (added a
  missing label on the gluon Lagrangian equation so the new cross-reference actually resolves);
  "λ convention" → "λ=1 choice above".
- §2.2.1: replaced the soft/collinear-limits paragraph with Prof's two new paragraphs (matches the
  9 Sep meeting's soft-quark correction — no independent single-soft-quark singularity); extended the
  NLO real-contribution sentence; rewrote the UV-divergences paragraph.
- §2.3: **Verified via `ask-codex` (1st call this session) whether Prof's biggest correctness worry
  was real** — he said he wasn't sure the integrated $X_4^0$ normalisation actually carries
  $S_\epsilon^2$ as it should. Confirmed against AntCalc's real X40 build/normalisation code
  (`src/core/profiles.wl`, `dev/check_x40_explicit_normalization.wl`) that it does carry $S_\epsilon^{-2}$,
  matching the thesis's own $S_\epsilon^k$ exponent with $k=n-2+l=2$ for four-parton tree antennae —
  so the general defining equation was already correct; Prof's complaint was about clarity/ordering,
  not a hidden bug. "ε poles" → "1/ε poles"; reordered the *k*=(n−2)+l definition to appear before
  Eq. (2.19) per Prof's exact wording; rewrote the coupling-renormalisation intro paragraph; **removed
  the orphaned N(n) equation (former eq:gehrN) entirely** — confirmed via grep it had become fully
  unreferenced once §2.4.2's opening paragraph was rewritten (see below), so this was safe, not a guess.
- §2.4 intro: deleted the vague, factually-wrong "five channels at NNLO" sentence (Prof: real NLO
  contribution doesn't have UV+IR both, this claim doesn't hold); rewrote Eq. (2.23)/(2.24) to use
  only `m` (not a mixed `n`/`m`), with explicit `|M_m^0|^2` and `J_m^m`; **wrote fully generic
  dσ^R_NLO/dσ^V_NLO/dσ^{RR,RV,VV}_NNLO equations for a generic process** (dropping the
  γ*→qq̄g-specific "channels" prose), using the exact matrix-element combinations Prof specified.
- §2.4.1 (Subtraction Terms): reworded the NLO/NNLO intro paragraphs with Prof's text; added the
  missing integrated-counterterm equation and the `∫dΦ_Xijk X_ijk^0` definition of the integrated
  antenna; renamed table column "SubTerm"→"Subtraction Term"; **removed the R-ratio-specific framing
  from both the NLO and NNLO closing paragraphs** (Prof: R-ratio belongs in Ch5, not here), replaced
  with the generic phase-space-factorisation closing Prof asked for.
- §2.4.2 (Antenna Functions): rewrote the opening/notation/antenna-set paragraphs; added `γ*→` to
  every row of Table 2.3; **moved the |M_n^l|² definitions equation to appear before the tree-level
  antenna definition** (matches Prof's requested Eq. order), added the missing explanatory sentences
  at one-loop and two-loop; rewrote the full-process `|M_{p1...pn}^l|²` paragraph with Prof's text,
  **fixed the LHS to `|M|²`** (was bare `M`, per Prof's flagged recurring typo — checked no other live
  instance of this typo remained after the M_4q^0 pass below).
- §2.4.3 (Colour Algebra): rewrote the whole subsection with Prof's SU(3)_c/Casimir-invariant text;
  **deleted the confusing planar/non-planar/quark-loop itemize block** (Prof: doesn't apply well to
  tree-level A_4^0, defers the concrete origin to §2.4.4 where it's actually shown), replaced with his
  short colour-decomposition paragraph. Left the "taking out a factor of 2C_F" sentence **untouched**
  — Prof asked a genuine question about it ("porque motivo aparece um factor de CF..."), didn't supply
  a fix, and it may be a stale leftover connected to the now-dead T-objects material. [Yours, item 5]
- §2.4.4 (Antenna Construction): reworded the NLO one-loop/NNLO tree-level/NNLO one-loop/NNLO
  two-loop/four-quark intro sentences with Prof's text; added the Ch4/Appendix A forward-pointer;
  **fixed a genuine equation error Prof caught**: the NNLO tree-level leading-colour term was written
  as a single antenna `N·A_4^0(1,3,4,2)`, corrected to the sum `N·(A_4^0(1,3,4,2)+A_4^0(1,4,3,2))`
  matching the squared-amplitude line directly above it; **fixed a missing sum over quark flavours**
  in Eq. (2.62)/M_4q^0 (needed to actually produce the N_f factor in the decomposed form); reworded
  the four-quark decoupling paragraph, using the antenna's own existing `M_{qq̄-γ}` symbol rather than
  Prof's ad-hoc shorthand `M_{B,4}^0` from the email (keeps consistency with the equations right below
  it, which already use `M_{qq̄-γ}`). **Verified via `ask-codex` (2nd call this session, 2 questions
  bundled together) against the real
  AntCalc source** — see [[ask-codex-antcalc-oracle]] — two things Prof flagged as uncertain:
  (1) `breve A_2^2` genuinely has **no** leading/subleading split in AntCalc (single `(N-1/N)` weight,
  confirmed in `src/core/profiles.wl` + `src/engines/extraction_loop.wl`) — **added a grounded sentence
  answering Prof's exam-style "why not?" concern directly**, no equation change needed; (2) the bare
  `Â_{3,bare}^1` quark-loop component is **identically zero** in AntCalc's stored result
  (`QuarkLoop -> 0`) — the nonzero *renormalised* `Â_3^1` comes entirely from the UV counterterm, not
  a genuine bare interference term. This confirms Prof's suspicion the bare equation is misleading as
  written — **left the equation untouched, flagged for Henrique** since correctly restating it needs
  his own composition. [Yours, item 2]
- §2.4.5 (Phase-Space Factorisation): full rewrite with Prof's text; **fixed the flagged `∫_1`/`Φ_jkl`
  numbering inconsistency** (an antenna phase space with two unresolved partons was being introduced
  under the single-unresolved `∫_1` label) by restructuring around the already-existing
  `eq:nloPhaseSpaceFactor` label instead of re-deriving it; replaced the confusing "two antenna types
  are exempt" paragraph with Prof's A_2^1/A_2^2 explanation. **Checked but did NOT apply** Prof's
  suggested `𝒩(ε,k)→C(ε,k)` simplification in the final integrated-antenna equation — traced actual
  live uses of `𝒩(ε,k)` (Ch4 uses it multiplying the *full* n-particle phase-space integral, not the
  antenna-only one) and confirmed swapping symbols here without also changing which phase-space
  integral is multiplied would silently introduce a `Φ_2` double-counting bug. The equation is
  correct as currently written. [Yours, item 4 — flagged so Henrique has this nuance before deciding]
- §2.5 / §2.5.1: "Monte Carlo" → "Monte Carlo (MC)"; "MI" → "Master Integrals (MI)" (both first uses);
  "ε poles" → "1/ε poles"; added an Euler Gamma function definition after Eq. (2.74)/MI-scaling
  equation; **removed the duplicated IBP/reverse-unitarity paragraphs at the end of §2.5's intro**
  (Prof: repeats §2.5.1's fuller treatment almost verbatim, including re-deriving the same
  `eq:derivativeToZero` equation) — moved that equation's label onto the fuller version inside
  §2.5.1.1 instead of duplicating it, checked all 3 cross-file references to the label
  (incl. `appendices/appendixB.tex:345`) still resolve correctly. The "add a forward-pointer to a
  worked example" request for the removed block turned out to be **already satisfied** — the fuller
  §2.5.1.2 version already has "An example... is available in Section~\ref{sec:PhiToCuts}." Laporta
  citation was **already present** (`\cite{Laporta:2000dsw}`), no action needed.
- **Root-cause fix, not a line-by-line item**: Prof's "why previous? you're still in 2.5.1" confusion
  traced to `secnumdepth` being too shallow — subsubsections (§2.5.1.1, §2.5.1.2, §2.5.1.3 etc.)
  were rendering with **no number at all**, so any `\ref` to one silently resolved to its parent
  subsection's number instead. Fixed with `\setcounter{secnumdepth}{3}` in `preamble.tex` — confirmed
  in the rebuilt PDF that subsubsections now show real numbers (2.5.1.1 etc.) and every affected
  cross-reference in the thesis benefits, not just the one sentence Prof happened to quote.
- **Thesis-wide mechanical sweep** (Prof: "aplica esta correção a todas as referências a equações"):
  converted all 58 `\ref{eq:...}` instances across every chapter and appendix to `\eqref{eq:...}`
  (renders "Eq. (2.65)" with parens instead of bare "Eq. 2.65"), confirmed `amsmath` already loaded.

**All 5 [Yours] items RESOLVED same day (15 Sep) — Henrique gave direction on all 5, all applied:**
1. **[Mine, applied]** §2.4.4 diagram sentence: Henrique's own diagnosis was correct (confirmed
   against Fig. 2.1/`eq:amplitude`) — "two processes" meant the two tree-level Feynman diagrams
   (gluon off the quark line vs. the antiquark line), both summed into the single amplitude
   $\mathcal M_3^0$ per Eq. (2.10). Rewrote the sentence to say exactly that, with explicit
   `Fig.~\ref{fig:feynDiagsEx}`/`Eq.~\eqref{eq:amplitude}` cross-references.
2. **RESOLVED same day, 15 Sep 2026 — [Henrique's own physics, Claude drafted the sentence on
   explicit request].** The bare equation
   `M_{qgq̄}^1 = ... = N A_{3,bare}^1 - (1/N)Ã_{3,bare}^1 + N_f Â_{3,bare}^1(1_q,3_g,2_q̄)` presented
   `N_f Â_{3,bare}^1` as a genuine nonzero bare term, which ask-codex had shown is actually zero
   (`QuarkLoop -> 0` in AntCalc's stored result). Henrique supplied the correct physics himself: the
   only diagram contributing to `Â_{3,bare}^1` is a massless-quark self-energy insertion on the
   external, on-shell gluon leg; since that leg has $p^2=0$, the bubble is a scaleless integral and
   vanishes identically in dimensional regularisation. The nonzero $N_f$-dependence in the
   *renormalised* `Â_3^1` two equations later comes entirely from the coupling-renormalisation
   counterterm — a genuinely separate origin, not a continuation of the (zero) bare diagram. Added a
   two-sentence note right after the bare equation stating exactly this (both the vanishing and the
   distinct origin of the renormalised piece), on Henrique's explicit request to draft it — standard
   single-use drafting permission, not a change to the general no-unsolicited-drafts rule.
3. **[Mine, applied]** Table 2.1: renamed column to "Universal IR factor", filled in all 13 rows with
   the actual antenna-specific symbols from hep-ph/0505111 (using its own eqs. 8.1–8.30, matching the
   thesis's own 1,3,4,2-style parton labelling exactly, not generic textbook formulas): $S_{132}$,
   $P_{qg\to Q}(z)$, $S_{134}$, $P_{gg\to G}(z)$, $S_{1342}$ (double soft, matches paper's own label),
   $S_{1;342}(z)P_{qg\to Q}(z)$ (soft-collinear), $P_{134\to Q}(w,z,y)$ (triple collinear),
   $P_{qg\to Q}(z)P_{\bar qg\to\bar Q}(y)$ (double single collinear), $S_{12}(3,4)$ (soft $q'\bar q'$
   pair), $P_{q\bar q\to G}(z)$ (collinear $q'\bar q'$), $P_{143\to Q}^{\text{non-ident.}}(x,y)$,
   $\tfrac12P_{234\to\bar Q}^{\text{ident.}}(w,x,y)$. Added the caption pointer to §8.1/§8.2.2. Source:
   `review/astra-verification/0505111.txt` (plain-text extraction of the same PDF).
4. **[Mine, applied]** Henrique's call: fix it in Ch2 now, track Ch4 separately. Changed
   `eq:antennaIntegral` from `𝒳_n^l = 𝒩(ε,k)∫dΦ_n X_n^l` to
   `𝒳_n^l = C(ε,k)∫dΦ_{X_ijkl...} X_n^l` (against the antenna-only phase-space integral defined one
   equation earlier, `eq:antPhaseSpaceFact`, rather than the full $n$-particle one) — mathematically
   equivalent, just cleaner. **Checked Ch4 before flagging a follow-up task**: `chapters/04-worked-example.tex`
   §sec:antIntMaths's *first* line, `𝒜_3^0 = C(ε,1)∫dΦ_ijk A_3^0`, already matches this corrected form
   exactly. Its *second* line (`= 𝒩(ε,1)(4π)^{3-2d}(q²)^{1-d/2}∫ds12 ds13 ds23 dΩ...`) is a separate,
   self-contained step converting to explicit Mandelstam-invariant integration variables — not broken
   by this fix, just a different algebraic identity for its own purpose. **No Ch4 follow-up task
   needed after all** — the initial worry doesn't apply here, though worth a final visual check once
   Ch4 gets its own revision pass.
5. **[Mine, applied]** Henrique's call: keep `eq:colourNorm` (`2C_F=N-1/N`, still referenced live
   from `chapters/03-package-framework.tex` twice) but drop the confusing "particularly useful to
   normalise the two hard quarks' colour... by taking out a factor of $2C_F$" claim, since it's really
   there to ground the (now-dead) T-objects material and $C_F$'s own definition is already given right
   above. Replaced with a neutral "follows directly from the fundamental Casimir invariant above"
   lead-in.

Rebuilt clean after all five (125 pp still, `latexmk -pdf`+`biber`, no errors, no undefined/multiply-
defined refs), visually spot-checked the table and every changed equation.

**Full re-verification pass, 15 Sep 2026 (Henrique asked "were all of Prof's Ch2 points
addressed?").** Did not trust the running summary — went back to Prof's original email and checked
every one of the ~60 distinct points individually against the live source: grepped for all 22
characteristic old phrasings he quoted (confirmed all gone) and ~28 characteristic new phrasings the
fixes should have introduced (confirmed all present, whitespace-normalised after a first pass gave
false negatives from line-wrapping). **Caught one genuine gap this way**: the "MI" → "Master
Integrals (MI)" abbreviation fix had been applied to a sentence that was *later* deleted during the
same session's §2.5 duplication cleanup, silently undoing the fix — "MI" was back to being used bare
at its actual first occurrence (`eq:intFamily`, before the `subsec:MIs` heading). Fixed by adding
"(MI)" at the true first prose use of "master integrals" instead (`chapters/02-physics-background.tex`,
the sentence introducing the master-integral reduction equation early in §2.5). Also re-confirmed the
4 thesis-wide/root-cause fixes are still intact: 0 remaining bare `\ref{eq:...}` anywhere in the
thesis, `\setcounter{secnumdepth}{3}` present, `eq:gehrN` fully gone, Laporta citation present.
Rebuilt clean once more (126 pp, one page longer from this fix + the hat-A31-bare sentence added
earlier same session).

---

### Prof. Pires review of Resumo/Abstract — 15 September 2026 (email) — DONE (Mine items) + 2 open [Yours]

Line-by-line review of `frontmatter/abstract.tex`, almost entirely literal supplied replacement
text (transcription, not composition) — classified **Mine**, executed directly, rebuilt clean
(121 pp, `latexmk -pdf`+`biber`, no errors). Two genuine open items left for Henrique.

**[Mine] — all applied, one commit-worthy diff:**
- Resumo: "experimental" inserted into opening sentence; "limite mole" → "limite \textit{soft}"
  (3 occurrences) + new sentence on unresolved radiation; loop-amplitude sentence reworded;
  antenna-formalism paragraph opener replaced with Prof's supplied text (antenna = two
  colour-connected radiators); "final-final" now explained inline; massive-extension sentence
  replaced, now names the antenna as $A_3^0(Q,g,\bar Q)$; UV/IR divergences sentence reworded;
  "polos em $\epsilon$" → "polos em $1/\epsilon$"; the $\mathcal T$-objects sentence removed
  entirely (Prof: doesn't belong in the abstract); antenna-integration paragraph opener replaced
  with Prof's more detailed supplied text; "tree-level" and "splitting-function" glosses added to
  the worked-example paragraph; R-ratio paragraph replaced with Prof's text + the $R(s)$ definition
  equation added; "A montagem" → "O cálculo"; massive-antenna sentence reworded ("presença de
  quarks massivos nesta antena"); **the entire "A principal limitação..." paragraph (computational
  cost / Python+FORM+Kira future-work) removed from the Resumo** — checked first that this content
  already lives in `chapters/06-future-work.tex` (it does: FORM/Kira/LiteRed2 replacement plan is
  there), so nothing was lost by deleting it here; closing sentence now names AntCalc explicitly.
- Abstract (English): opening paragraph replaced with Prof's LHC-focused supplied text; "virtual
  terms" → "virtual contributions"; results paragraph replaced with Prof's supplied text
  (integrated antenna functions / R-ratio validation / massive $A_3^0$ sentences).
- Verified in the rebuilt PDF: Resumo = **3 pages** (pp. 2–4), Abstract = **1 page** (p. 5) — the
  imbalance Prof flagged is still there even after these edits (deleting the limitation paragraph
  and adding the R-ratio equation to the Resumo didn't close the gap, since the Abstract also grew
  from its own literal-text additions). Doesn't resolve open item 1 below.

**Item 1 RESOLVED — 16 September 2026.** Henrique asked Claude to draft this one directly (single-use
permission, explicitly given that turn). Rewrote the opening paragraph following the positioning
plan agreed with Henrique: LHC named in the first sentence (mirroring the English Abstract's own
opening move), the old dangling "Uma parte substancial destes processos" sentence dropped entirely
rather than patched (its QCD-attribution content folded into what is now the second sentence), the
closing Feynman-diagram-complexity sentence kept unchanged as the paragraph's transition into
paragraph 2. Rebuilt clean, visually confirmed on the rendered page — reads naturally, no orphaned
references, mirrors the English structure closely as Prof originally modelled.

**[Yours] — open, needs Henrique's own composition/judgement, not drafted:**
1. **Page-count imbalance is a live, unresolved question** (renumbered from item 2) — Prof asked
   directly whether 3 pages (Resumo) vs 1 page (Abstract) follows FCUL's thesis-writing rules.
   Henrique has since confirmed the Resumo/Abstract don't count toward the 80-page main-text limit,
   which resolves the hard-limit worry, but Prof's underlying stylistic question (is this length
   asymmetry itself normal/expected, independent of any page budget) is still nominally open — no
   rules document found in this repo either way. Low priority now, not blocking anything; worth a
   quick check with the secretariat or Prof whenever convenient. Note the item-1 rewrite above made
   the Resumo slightly *longer*, not shorter, so this hasn't gotten easier to ignore.
2. *(Not raised by Prof, flagged for Henrique's own call, not acted on):* the Portuguese Resumo had
   its self-deprecating "principal limitação" paragraph removed on Prof's explicit instruction (it
   undervalues the work); the English Abstract's closing "Future work will extend AntCalc..."
   sentence carries a milder version of the same framing and Prof didn't mention it. Left as-is —
   Henrique's call whether the same logic should trim it too.

---

### Meeting with Prof. Pires — 9 September 2026 (in person)

**Nature of this pass:** a live meeting, *not* an email — same as 28 Aug. No verbatim advisor text; Henrique's notes + recollection are the record, and his correction of this map carries extra weight. Every point below grounded against the *current* `.tex` and against hep-ph/0505111 on 9 Sep 2026. **Page numbers he gave (fig 2.2, pg 24, pg 25) match the current `main.pdf` (117 pp):** Fig. 2.2 is on printed p.24, the NNLO subterm block + Table 2.2 on printed p.25.

**Deadline context:** Henrique told Sampaio (in the corridor, 9 Sep) he'd send **the rest of the first half by Monday 15 September at the latest**. That's the primary objective — finishing Ch1–Ch3, i.e. Task Q completion + the Ch2 items in this block + Task X. Ch4/Ch5 items here (points 7, 8) are **not** on the Monday critical path; they can wait for Phase 3.

Most points fold into existing tasks rather than opening new ones:

**1. Introduction — add an LHC / Standard-Model primer, with figures, *before* the QCD material. [Yours]. Folds into Task X.**
Ch1 currently opens on colliders/the LHC (Task A) but is compact, figure-free, and has no SM overview. Prof wants: what the LHC does, the SM particle content (with a diagram/chart), photos/schematics — set the experimental scene before perturbative QCD. This is separable from §2.4 (new front-matter, doesn't depend on the §2.4 rewrite) so the primer half of Task X can be drafted in parallel with finishing Q. **Figures:** an LHC/detector schematic + an SM particle chart — must be CC-licensed (CERN Document Server, Wikimedia) or redrawn; flag licensing before dropping any image in.
**Scope correction (11 Sep 2026, Henrique):** Prof's actual ask, restated plainly, is small — **a short primer paragraph plus maybe one figure/graphic**, not a deep restructuring. Don't conflate this with the separate, larger "Ch1 deep rewrite" idea (NNLO-frontier context, subtraction-scheme landscape, automation gap, lineage — noted 30 Aug as Henrique's own ambition, not Prof's instruction). Task X as executed now = this scoped primer addition; the bigger rewrite, if it still happens, is a distinct, later decision.
**Target length (11 Sep, Prof directly):** whole chapter to **~2.5–3 pages** (currently ~1.2pp) — so this addition is a real ~1.3–1.8pp of new/tightened material, not a token paragraph.

**CONSOLIDATED WRITING PLAN (12 Sep 2026) — write against this, not from scratch.** Structure is rising-specificity, matching Henrique's own conference-talk arc (general field → subfield's version of the problem → this thesis's contribution). Beats 1/3/5/6 already exist and are Prof-approved (25 Aug) or drafted — touch lightly, don't rewrite. Beats 2 and 4 are the actual new writing.

1. **[EXISTING, untouched]** Opening accelerator/precision sentence (`chapters/01-introduction.tex:3-8`). Ends on "...processes governed by **the strong interaction**" — that's the deliberate handoff into beat 2, don't rewrite it looking for a better pivot; the pivot already works (proven by beat 2 draft below picking it up cleanly).
2. **[NEW — Block A, SM/strong-interaction primer. Drafted, 3rd pass, two line-fixes given 12 Sep, not yet finalised.]** Content, in order: strong interaction = 1 of 4 fundamental interactions → its charge is colour charge → mediated by gluons (gauge bosons) vs. carried by quarks (fermions, colour-charged matter, six flavours, two possible electric charges: +2/3e up-type, −1/3e down-type) → point at `fig:SM` (Standard Model particle chart) → name QCD as the theory of *the strong interactions among these particles specifically* (not a blanket "how they interact" claim — QCD ≠ electroweak). **Open decision:** does "what makes QCD special" (gluon self-interaction / non-Abelian structure — gluons, unlike the photon, themselves carry colour charge; confinement; asymptotic freedom) belong inside this same block, or is block 2 already complete on its own with that content living in the beat-3 bridge / beat-4 opening instead? Not resolved — decide before finalising beat 2.
3. **[EXISTING, untouched]** "To be able to use these measurements fully... QCD permits... a perturbative expansion in powers of $\alpha_s$" (current `:9-13`) — closes beat 2, resumes the kept opening. Henrique's own addition worth keeping: append "...as its value decreases as the centre-of-mass energy $q$ increases" (asymptotic freedom, correct, gives a second figure option — running-coupling plot — if wanted instead of/alongside the SM chart).
4. **[NEW — Block B, the precision problem / phenomenology's "orphan" position. NOT YET DRAFTED — this is the real remaining work.]** Content decided across this conversation, in rough order:
   - The core claim, kept at your talk's altitude, not narrowed to a scheme-by-scheme comparison: collider-data precision is outpacing theoretical predictions; this leaves phenomenology stretched between abstract theory and experiment; state plainly why pheno matters as a field in itself. **Don't** let this drift into naming/comparing specific subtraction schemes (Catani-Seymour/FKS/etc.) — that's the separate, larger "Ch1 deep rewrite" idea explicitly set aside above.
   - **Best complexity-gap illustration found this session: the ff/if/ii antenna taxonomy**, sharper and more defensible than any single example — "realistic LHC processes don't just need bigger members of the ff family this thesis computes; many are if or ii, bringing in initial-state factorisation (PDFs) and colour structures this work doesn't touch." This does double duty: it's also the still-unplaced **scope statement** from Task Q's PARKED list #4 (final-final $q\bar q$ now; $qg$/$gg$ + initial-state = future work) — land it here instead of writing it twice.
   - **Optional, 1-2 sentences max, conservatively phrased:** the LHC Higgs-production complexity-gap anecdote (ggF via HEFT giving an $H\to gg$ antenna, genuinely ff, directly parallel to this thesis's $\gamma^*\to q\bar q$ — correct and citable-in-spirit) and/or the jet-clustering-precision point (grounded in Henrique's own prior work with Prof. Apolinário on jet quenching/clustering, so more defensible than an inference from someone else's talk). **Do not** state the specific "could backtrack jets with much better precision" causal mechanism as fact — too specific a claim to defend cold if pushed at viva; if used, state only that more complete antennae would improve achievable precision, not a specific mechanism.
   - **Needs an explicit narrowing sentence somewhere in this block:** the LHC (pp, hadron collider) framing at the top vs. this thesis's actual validation observable, the $R$-ratio ($e^+e^-\to$hadrons, historically PETRA/PEP/LEP) — legitimate and standard in this literature, but the chapter needs to say so explicitly rather than let the LHC-opening imply direct LHC applicability that hasn't been shown.
5. **[EXISTING, tighten only]** antennae/infinities/complexity-growth-with-order material — current paras 2–3 (`:9-31`). Henrique's talk phrasing of this ("we use antenna functions to handle infinities... every time we increase order, the number of antennae grows and they get more complex") is tighter than what's on the page now — sharpen, don't rewrite from scratch.
6. **[EXISTING, tighten only]** AntCalc payoff, current para 4 (`:32-41`). Same treatment — talk phrasing ("my project succeeds at automating... lays the groundwork for further antennae... reproducible") is a sharper version of what's already there.
7. **[EXISTING, one-line fix]** Chapter-outline opening line (`:42-`) currently assumes the chapter starts on "High-energy collider experiments" — no longer literally true once beats 2/4 exist ahead of it structurally-late reference; needs a one-line adjustment, not a rewrite.

**Figures, decided/leaning:** a **redrawn SM particle chart** for beat 2 (favoured over pulling a CDS/Wikimedia image — no licence-chasing, matches house figure style) — still open which tool/style to match (check what the rest of the thesis's figures use before building it). A **running-coupling plot** is a live option for beat 3's asymptotic-freedom line. For beat 4, a **redrawn ff-vs-$gg\to H$ complexity-contrast sketch** was suggested as a cheaper, more pointed alternative to a real detector-data plot (real data-vs-theory content already has its proper home in Ch5's Task Y, not here).

**Status 12 Sep 2026:** beat 2 is close (3 drafting passes, two corrected sentences given, one open structural decision above); beat 4 has a full content plan but zero prose; beats 1/3/5/6/7 need no new composition, only the noted light touches. Henrique found live sentence-by-sentence chat review unproductive ("I'm just riffing") — next attempt should draft a fuller pass of a whole block away from turn-by-turn review, then bring it back complete for one review pass, rather than iterating line-by-line live.

**2. `tab:limits` (§2.2.1, from Task R2) — replace the "Vanishing invariant(s)" column with the actual singular factor. [Yours]. Folds into Task R2 + Task R.**
Each row should show the **eikonal factor** for soft limits ($S_{ij,k}=2s_{ik}/(s_{ij}s_{jk})$-type) and the **Altarelli–Parisi splitting function** $P(z)$ for collinear limits, à la hep-ph/0505111 §5 (single-unresolved factors) / §8.1.1. Consistent notation already exists in Ch4 §4.2.2 ("Soft Limit and the Eikonal Factor" `:971`, "Collinear Limit and the Altarelli--Parisi Splitting Function" `:1012`) — reuse it. Keep consistent with the corrected soft-quark story (Task U): no soft-quark row.

**3. "Check out fig 2.2" — redraw `fig:colourConnectionNLO`. [Yours]. Folds into the Task Q figure plan.**
Clarified by Henrique: **Prof did not like the Feynman-like diagram half.** He wants something "very similar to hep-ph/0505111's Fig. 7" — i.e. the *factorisation-as-division* illustration style (blobs with parton lines connected by `=`, the square bracket standing for antenna function $\times$ antenna phase space; their Figs 3 and 7 are the NLO and NNLO versions). Action: drop the schematic Feynman diagram from Fig. 2.2; keep/clean the colour-connection + parent-daughter ($i,j,k\to I,K$) illustration (0505111 Figs 1–2 style). **Note the overlap:** `fig:antennaFactorisation` already exists (built under Task E as a deliberately concrete, non-copy version of 0505111 Fig. 3, because Henrique found the paper's abstract Fig. 3 unreadable). Prof's ask may already be substantially met by that figure — decide whether Fig. 2.2 becomes a plain colour-connection diagram with the division handled by `fig:antennaFactorisation`, or whether `fig:antennaFactorisation` itself gets revised toward the 0505111 Fig. 7 look. Do **not** just copy the paper's figure — same reasoning as Task E.

**4 + 5. "pg 24, see $\sigma_E$, for large angles" / "see pg 25" — make the wide-angle soft (eikonal) factor explicit in §2.4.1. [Yours]. Folds into Task Q.**
Confirmed with Henrique: pg 25 point is "probably the $\sigma_E$ term", same thing as pg 24. Printed p.24–25 is Eq. 2.28 ($d\sigma^S_\text{NLO}$ with an abstract $X^0_{ijk}$), Fig. 2.2, and the NNLO subterm block. The ask: show the **process-independent soft/eikonal factor** — the wide-angle ("large angle") soft-gluon radiation pattern $2s_{ik}/(s_{ij}s_{jk})$ that $X^0_{ijk}$ reproduces — rather than leaving $X^0_{ijk}$ abstract. This is **already a Task Q bullet** ("$S_j$ is process-independent — use the exact 0505111 formula, not 'universal singular factor for this process'"); the meeting just reinforces it and ties it to points 2 and 3. No new work item, but make sure the Q rewrite lands it visibly.

**6. Antenna table — show the originating process $\gamma^*\to q\bar q + X$. [Yours, small]. Folds into Task Q3.**
`tab:antFamilies`: every family comes from $\gamma^*\to q\bar q$ plus extra radiation $X$. Make that explicit — the "Final state" column shown as the process, or a header/annotation. Merge with Q3's existing "defining partons of each antenna, $A_3^0(q,g,\bar q)$ style" addition; one edit.

**7. Conference R-ratio plots → Chapter 5. New Task Y. [mixed — figure scaffold Mine, captions/prose Yours].**
Plots are in `../r_ratio_table/` (sibling of `thesis/`). Per its `NOTES.md` the finalised slide figures are:
- `rratio_formal_q_curve.pdf` / `rratio_formal_q_curve_thesis.pdf` — LO charge-counting staircase + AntCalc NNLO $R(Q)$ over the full PDG range, terminating at the two-loop Landau pole ($Q=0.391$ GeV); the "scope of validity" figure. The `--thesis` variant has the fuller annotation + long y-axis label — use that one.
- `rratio_continuum_validation.pdf` — two panels, continuum only (2–50 GeV, $c\bar c$/$b\bar b$ excluded): $R$ vs $Q$ (top) and $R_\text{data}/R_\text{NNLO}$ (bottom, sits at 1.00 to a few %); the quantitative-agreement figure.
Theory provenance (from `NOTES.md`): fresh `BuildRRatio[SMQCD]` run 8 Sep 2026, AntCalc 0.3.1β1, Wolfram 15.0, `ResultOrigin -> ComputedFromIntegratedIngredients`, `ReferenceAgreementQ -> True`. Data = PEP/PETRA Table 1 of `phep-2004-006.pdf` + PDG 2018 compilation.
Destination: Ch5 is short and pre-rewrite — these belong in a new "Comparison with experimental data" section (after §5.2). Overlaps the TODO's "Expand Chapter 5" item (§4 below) and **Task W**. **Mine:** copy the two PDFs into `figures/`, scaffold the `figure` environments. **Yours:** captions, the data-provenance paragraph, placement, and the surrounding validation narrative. Not on the Monday critical path.

**8. Chapter 4 — reorganise around the worked example; masters at the end; PaVe scalar functions to the appendix. Expands Task V. [Yours] — structural + new per-antenna content + connective prose.**
Current: §4.1 defines *all* master integrals up front → §4.2 NLO ($A_3^0$ worked example, $A_2^1$) → §4.3 NNLO → §4.4 summary → §4.5 massive. Prof wants:
- **§4.1 = the $A_3^0$ worked example**, introducing $R_3$ inline (it already does — `eq:A30intermsofR3` + `eq:R3` are in `subsec:masterresults`... actually in `sec:antIntMaths`, `:752`/`:758`).
- **Then the other antennae** ($A_2^1$, $A_2^2$ set, $A_3^1$ set, $A_4^0/\tilde A_4^0$, $B_4^0$, $C_4^0$), each presenting its **MasterCombination form** (antenna $=\sum_i c_i R_i$, before master substitution) — this is the substance of Task V ("antenna results reduced to the quoted masters").
- **Master-integral definitions collected at the end** of the chapter ("we use the MI $R_4$, $R_6$, etc. — here they are at the end").
- **PaVe scalar functions $B_0,C_0,D_0$ → the appendix**, next to the PaVe reduction method (already in Appendix B.2 from Task G).
Cross-ref check (done 9 Sep): only Ch2 §2.5.2 (`subsec:MIs`, `:1885`) forward-refs `sec:masterresults`; `appendixA` refs Ch2's `subsec:MIs`, not Ch4. Fairly self-contained across chapters, but **many intra-Ch4 `\ref`s** to `subsubsec:R8a` / `subsubsec:V8` / `subsubsec:A4` / `subsubsec:A6` / `subsubsec:B0` / `subsubsec:D0` to retarget. Merges the existing §4 items "one representative IBP reduction term" (already done, `subsubsec:a30IBPExample` `:762`) and "replace 'agrees with the literature' prose with one validation table" into Task V. **Claude produces the move-map + full cross-ref audit; Henrique writes connective prose and pulls MasterCombination forms from AntCalc.** Phase 3, not Monday.

**Not discussed / explicitly deferred:** nothing new retired. Points 2–6 are all Ch2 and belong in the Task Q / R2 / R / Q3 work already sequenced for Phase 2; points 7–8 are Ch4/Ch5 Phase 3.

---

### Meeting with Prof. Pires — 28 August 2026 (in person)

**Nature of this pass:** a live meeting, *not* an email. There is no verbatim advisor text — Henrique's notes plus his recollection are the source of record, so his correction of this map carries more weight than usual. Every point below was grounded against the *current* `.tex` and against hep-ph/0505111 on 28 Aug 2026. Compiled section numbers (from `main.toc`), for disambiguation: §2.1.2 = "From the QCD Lagrangian to Feynman Diagrams" (p16); §2.2.1 = "Infrared Divergences" (p17); §2.3 = "Dimensional Regularisation" (p19); §2.4.1 = "NLO and NNLO Subtraction Terms" (p21); §2.4.2 = "Antenna Functions" (p23); §2.4.3 = "Colour Algebra and the Antenna Colour Decomposition" (p25, and it currently contains a `\subsubsection{$T$-objects}` at `:793`); §2.4.4 = "Antenna Families" (p27); §2.4.5 = "Phase-Space Factorisation" (p28).

**Overall verdict from Prof:**
- Ch2 is still too dense. Take notation *and* diagrams from hep-ph/0505111 much more directly. Read the chapter as an examiner would.
- What he is happy with: the integration material (§2.5 — keep **one** IBP example), and §2.1.1 Lagrangian / §2.2 divergences / §2.3 dim-reg. **The problems are concentrated in §2.4 (subtraction).** Converges with the "Chapter 2 — cross-cutting concern" already in this file.

**STRUCTURAL DECISION — RESOLVED 28 Aug 2026 (Henrique): §2.4.1 gets a CLEAN REWRITE against hep-ph/0505111 §2.1, not a patch.** It moves from the thesis's own abstracted notation (`eq:subTerms`: `d\sigma^X = 2C_F\sum_{a(,s)}f_a(N,N_f)X^l_{ij,(a,s)}d\sigma^B`) to 0505111's *exact* eq. (2.5) structure (`d\sigma^S = N\sum_{m+1}\int d\Phi_{m+1}\frac{1}{S_{m+1}}\sum_j X^0_{ijk}\,|M_m(\ldots\tilde p_I,\tilde p_K\ldots)|^2\,J^{(m)}_m`). This **supersedes most of Task D** (T-object content in Ch2, `tab:sigmaByOrder`, the abstracted-sum equations, the `\sim`→exact work — all committed in `a572aa8`/`785a2fd`/`9744c15`). Task D's roadmap note "keep `subsec:antennaFuncs` mostly as-is" is also overridden — Prof wants §2.4.2 redone (see below). **Prof numbers §2.4.x by the current compiled draft** (confirmed 28 Aug): 2.4.2 = Antenna Functions, 2.4.3 = Colour Algebra.

#### Chapter 2

**§2.4.1 CLEAN REWRITE — supersedes the §2.4.1 half of Task D. [Yours] — one commit. Task Q — IN PROGRESS (30 Aug 2026).**

**PROGRESS (30 Aug 2026) — the intro + subtraction-terms part is drafted and committed.** Henrique wrote it; Claude reviewed structure only (no mistakes pass yet, at his request). Old §2.4 content left in place inside `\iffalse … \fi` blocks, to be deleted after the *whole* §2.4 rewrite is done.
- **`sec:antSubForm` intro (unnumbered, `:441–478`):** mirrors 0505111 §2 opening — sum over partonic channels (1 LO / 2 NLO / 5 NNLO), UV via renorm + IR via KLN, then the LO (`eq:bornLevel`, `d\sigma^B` in the `\sum_n d\Phi_n (1/S_n)|M_n|^2 J` form + explicit `d\Phi_n`), NLO (`d\sigma^R + d\sigma^V`), and NNLO (`d\sigma^{RR}+d\sigma^{RV}+d\sigma^{VV}`, five channels) *naive* decompositions.
- **`\subsection{Subtraction Terms}` (§2.4.1) with unnumbered `NLO` / `NNLO` heads** (kept unnumbered deliberately — Prof's fragmentation complaint; no 2.4.1.1/2.4.1.2).
  - **NLO — content-complete.** naive→subtracted cross section; requirements list (all four: every singular limit / analytically integrable / local / no spurious); KLN + the loop *closed* ("after analytic integration … explicit `\epsilon`-poles which cancel … pole-by-pole"); `d\sigma^S_\text{NLO}` in the 0505111 (2.5) antenna-sum form; mapped momenta `\tilde p_I,\tilde p_K` + parent/daughter + colour connection + `fig:colourConnectionNLO`; `R`-ratio → `J=1` → phase-space factorisation `d\Phi_{m+1}=d\Phi_m\,d\Phi_{X_{ijk}}` → `d\Phi_3 = P_2\,d\Phi_{X_{ijk}}`.
  - **NNLO — structurally complete.** naive (2.14-form) → subtracted; `d\sigma^S = S,a+S,b+S,c+S,d`, `d\sigma^T = T,a+T,b+T,c`; `tab:counterterms` (config → subterm, all seven rows); the takeaway sentence ("at this order four-parton `X^0_{ijkl}` and one-loop three-parton `X^1_{ijk}` antennae are necessary"); **`S,a`, `S,b`, `T,a`, `T,b` in full** (the ones that apply to a two-hard-radiator process; `S,b`'s `X^0_{ijkl}-X^0_{ijk}X^0_{IKl}-…` and `T,b`'s `X^1\to X^1+(\beta_0/\epsilon)(…)X^0` shift kept as bare formulas — deliberately *not* explained here, that's §2.4.2's job when `B_4^0`/`C_4^0`/`A_3^1` come up); **`S,c`, `S,d`, `T,c` cut to one sentence + `\cite{Gehrmann-DeRidder:2005btv}`**; colour-connection notation + three NNLO figures; `R`-ratio → `J=1` → `d\Phi_4 = P_2\,d\Phi_{X_{ijkl}}`.

**PARKED — pick up when resuming §2.4 (do NOT lose these):**
1. **Dangling refs.** `eq:sigmaSDef` now lives inside `\iffalse`; downstream text still `\ref`s it at `02-physics-background.tex:720` and `:1144`. Fix after the *complete* §2.4 rewrite (the downstream subsections `subsec:antennaFuncs` onward will be rewritten anyway — Q2).
2. **"higher-multiplicity processes" (`:615`) → "processes with three or more hard partons."** Physics-accuracy wording, not polish — `S,c/S,d/T,c` are about hard-radiator count, not final-state multiplicity (`q\bar qgg` is higher multiplicity but still two hard radiators). Don't let this get lost in the typo pass.
3. **`R`-ratio → Ch5 pointer.** The NNLO-R-ratio-via-antennae result is Henrique's headline selling point (Prof, 28 Aug meeting: "not something everybody is able to do", cited Joana Reis's thesis leaving it undone). It is currently mentioned *nowhere* in §2.4. Decide deliberately where it lands — end of the NNLO subtraction part, §2.4.2, or the later results discussion — stated as a contribution + forward-ref, not as a boast.
4. **Scope statement** ("this work treats final-final `q\bar q` antennae; qg/gg families + initial-state = future work"): does NOT live in §2.4 (Henrique's call, agreed). Homes: Ch1 intro (already partly there) + a **new Ch2 opening paragraph** (Ch2 currently jumps straight from `\chapter{Theoretical Foundations}` into `\section{QCD…}` with no chapter intro / roadmap — add one). §2.4.4/`tab:antFamilies` gets at most a one-clause "qg/gg follow the same construction, see Ch6".
5. Full **mistakes pass** on the new §2.4.1 (the `\sum_n d\Phi_n` missing `\int`, `tab:counterterms` cosmetics, typos) — deliberately deferred at Henrique's request.

**Still open in Task Q proper (not started):** §2.4.2 "Antenna Functions" rewrite (Q2), `tab:antFamilies` additions (Q3), the paired Ch3 T-primer, deleting the Ch2 `\subsubsection{$T$-objects}` (now at `02-physics-background.tex:897`), the non-antennae explanation.

---

**Original Task Q spec (unchanged):**
Ground the whole subsection on hep-ph/0505111 §2.1 (eqs. 2.1, 2.3, 2.4, 2.5, 2.6, 2.8; Figures 1–3). Specific asks, all pulled toward "match the paper exactly":
- Write `d\sigma^S` in the exact form of **0505111 eq. (2.5)**, not the abstracted `2C_F\sum f_a X d\sigma^B`. Prof said more than once he is "not a fan of the `d\sigma` expressions" (`eq:subTerms`, `eq:nnloCross` tail, the `\sigma^\text{NLO}/\sigma^\text{NNLO}` block at `:531-539`).
- **Drop the `2C_F f_a(N,N_f)` prefactor bookkeeping.** (Henrique confirmed: "2C_F f_a" = the colour-prefactor terms carried with the antennae in `eq:subTerms`.) 0505111's notation folds all of this into the normalisation `N` and the colour-ordered `|M_m|^2`; adopt that.
- **Use matrix elements, not antennae, in the schematic** (Henrique confirmed — meaning: keep `|M_m(\ldots)|^2` explicit and multiply by the tree antenna `X^0_{ijk}`, exactly as eq. 2.5/2.6 do; don't collapse the reduced matrix element into a `d\sigma^B` blob).
- **The Born / reduced matrix element must be written in terms of the mapped `I, K` momenta** — `|M_m(p_1,\ldots,\tilde p_I,\tilde p_K,\ldots)|^2`, per eq. (2.6). (Henrique confirmed reading.)
- **`S_j` (the single-unresolved / soft factor) does not depend on the process — use the exact formula from 0505111**, not the "universal singular factor for this process" phrasing at `:422-428`. For a soft gluon `j` between radiators `i,k` the paper gives `|M_{m+1}|^2 \to (2s_{ik}/s_{ij}s_{jk})|M_m|^2` explicitly (p6–7, just after eq. 2.6), with the colour factor modification `T^a_{ikj}\leftrightarrow\delta_{iI}\delta_{IK}\delta_{Kk}`.
- **Jet / measurement function: state `J = F_n` explicitly, and `= 1` for the R-ratio** (Henrique confirmed). Thesis already says `F_n=1` for every `n` (`:454-457`); the ask is to identify it with 0505111's `J^{(n)}_m` and use that notation.
- **`d\sigma_\text{NLO}` and `d\sigma_\text{NNLO}` must be explicitly defined**; explain **why both NLO and NNLO subtraction are needed**; explain **why the analytic integration is needed** and **why there are `\epsilon` poles**. (Some of this exists at `:459-482`, `eq:nnloCross` — needs to be foregrounded and tightened, not buried after the equation.)
- **Mention the non-antennae `A^2_2`, `A^2_1`** (and `\tilde A^2_2` etc.): state plainly that they are *not* antenna functions **by definition** but *are* required ingredients, and why. Prof wants this as a short **addendum near `tab:antFamilies`** (see the tab:antFamilies item below) as well as in the §2.4.1 narrative.
- **All colour-independent objects → "colour-stripped"** (not "colour-independent"); state that colour is handled inside FeynCalc/FeynArts. (Same wording fix as Task J item (e) in the Ch3 review — apply consistently.)
- **`tab:sigmaByOrder` (`:545`, "dσ in terms of antennae") is badly placed** — reposition (or fold into the rewrite).
- **T-objects — REMOVE the Ch2 `\subsubsection{$T$-objects}` entirely** (`02-physics-background.tex:793-828`, from commit `8292d57`). Confirmed 28 Aug (Henrique): the whole subsubsection goes, not a trim. 0505111 uses no T-objects. Ch5's `subsec:Tobjects` + `eq:integratedT` are a **separate matter** — they stay (Task D's topology-grouping argument + Task W both need them); this removal is Ch2-only. Check inbound refs to the removed block before cutting (`eq:genTSum`/`list:A31Comps` are referenced nearby — verify what points at the T-object equations specifically).
- **Framing to use instead of T-object bookkeeping:** "every antenna carries all colour components; the ones a given antenna doesn't have are simply zero." (Henrique's note, confirmed.)
- Diagrams to bring in from 0505111: the **colour-connection chain** (their Fig. 1), the **parent/daughter `i,j,k \to I,K`** picture (their Fig. 2), and the **mapping/factorisation diagram showing the division** (their Fig. 3 — the `[...]` bracket = antenna function × antenna phase space). Thesis already has `fig:antennaFactorisation` (Task E) — Prof wants the *division* to read more clearly, Fig. 3 style. This is the "digram on the mapping should have the division" + "colour coupling diagrams" notes.

**§2.4.2 "Antenna Functions" — redo. [Yours]. Task Q2 (fold into Q or keep adjacent — same commit series).**
Prof: "redo 2.4.2" (= Antenna Functions, confirmed his numbering matches the compiled draft). "2.4.3 stays" (= Colour Algebra body — but the T-objects subsubsection currently inside it is removed, see above). This overrides Task D's roadmap line "keep `subsec:antennaFuncs` mostly as-is." Scope to settle with Henrique: `eq:bornNorm` density (already a Task E follow-up), the antenna-definition presentation, and whether the per-antenna-type Feynman-diagram figures (already noted under Task L) land here.

**§2.4 — `tab:antFamilies` (was "Table 1 of 0505111"). Layout is GOOD (confirmed 28 Aug). Two additions. [Yours, small]. Task Q3.**
- **Show the defining/relative partons of each antenna as a reference** — e.g. `A^0_3(q,g,\bar q)`, `A^0_4(q,g,g,\bar q)`, `B^0_4(q,q',\bar q',\bar q)`, … the way 0505111 Table 1 gives the explicit argument list. Add as a column or an inline annotation.
- **Addendum on the non-antennae**: a short note at/under the table stating that the `q\bar q`-row entries (`A^1_2`, `A^2_2`, `\tilde A^2_2`, `\hat A^2_2`, `\breve A^2_2`) are **not antenna functions by definition** (an antenna requires at least one unresolved parton radiated between two hard radiators), but are needed as assembly ingredients. Same content as the §2.4.1 "mention the non-antennae" bullet — write once, reference from both places.

**§2.4 — AP and eikonal checks on `A^0_4`. [Yours]. Provisional Task R.**
Add explicit Altarelli–Parisi (collinear) and eikonal (soft) limit checks for the `A^0_4` antenna, demonstrating it reproduces the known singular factors. 0505111 §8.2.2 has the target limits. Related to the existing "local eikonal/AP-splitting validation" mention in Ch1.

**§2.4 — PaVe pointer / move.** Already **Task G (DONE, `9744c15`-era)** — `subsec:PaVe` moved to Appendix B.2, all three refs retargeted. Prof's note ("point to PaVe in the appendix, maybe transfer them there") is now satisfied. Treat as closed; just confirm he's seen it.

**§2.2.1 — soft-quark explanation is wrong. Reopens the closed "Task B" item. [Yours]. Task U — DONE (30 Aug 2026).**
Current text (`:218-229`): "if only one of the quarks is soft, then the pole formed is the same as in the collinear limit, and requires no separate dedicated soft-quark counterterm." **Prof says this is physically impossible as stated** — for `\gamma^*\to q(k_1)g(k_3)\bar q(k_2)`, a soft `q(k_1)` would require an underlying Born process `\gamma^*\to g\bar q`, which does not exist (colour / fermion-number). So there is no soft-single-quark limit to describe for this process at all. (Henrique: "he didn't like the current logic, he said it's physically impossible; there should be information about it in 0505111.") This **overturns the refinement committed under the old Task B** (`56e2311`). Grounding needed against 0505111 §8 (single unresolved factors — soft quark appears only where a `q\bar q` pair can factor onto a genuine lower process, e.g. `E`/`G`-type antennae), before rewriting. Cross-check the §2.2.1 `\cite{Ellis:1996mzs}` claim.
- **DONE:** grounded against 0505111 §8.2 — `A^0_3` has exactly two single-unresolved limits (soft gluon eq. 8.14, `q\|g` collinear eq. 8.15), no soft-quark entry anywhere in the whole §8 catalogue; §8.1.1's single-unresolved factor list is soft-gluon eikonal + three AP splittings only. The wrong worked-example paragraph was deleted; Henrique wrote a replacement (`:215-220`): a soft limit exists only where removing the parton leaves a physical lower-multiplicity process, so `\gamma^*\to qg\bar q` admits a soft gluon but not a soft quark (no `\gamma^*\to g\bar q`); a quark can only go unresolved collinearly; a `q\bar q` pair can go soft as a unit at NNLO. `\cite{Ellis:1996mzs}` dropped from the deleted claim (the `:202` kinematics cite stays). Build clean.

**§2.4 (or new) — table/list of the limits considered. [Yours, small]. Task R2 — DONE (30 Aug 2026).**
Explicit list or table of the unresolved limits actually relevant here: soft gluon, collinear, double-soft gluon, soft `q\bar q` pair, etc. — with which invariant each singularity sits on and which antenna handles it. Must be consistent with the corrected soft-quark story (Task U). The retired §2.2.1 "motivation paragraph" bullet noted a table like this would do that motivational work.
- **DONE:** `tab:limits` added to §2.2.1 (`02-physics-background.tex:227-`) as an inline `longtable` (not a float — it was orphaning a page), caption below. Organised by final-state configuration (`(1_q,3_g,2_{\bar q})` NLO; `(1_q,3_g,4_g,2_{\bar q})`, `(1_q,3_{q'},4_{\bar q'},2_{\bar q})`, `(1_q,3_q,4_{\bar q},2_{\bar q})` NNLO), each row: limit / kinematic condition (`p_3\to0`, `1_q \parallel 3_g`, …) / vanishing invariants. All 14 rows verified against 0505111 §8.2 eqs. 8.14–8.30. **No antenna column** — Task Q adds the antenna→limit mapping later so R2 isn't reworked. Referenced from `:227`. Build clean.

**§2.1.2 — Feynman rules. [Yours-light]. Task S — DONE (30 Aug 2026).**
~~move Feynman rules elsewhere, maybe an appendix~~ — **re-scoped 30 Aug (Henrique's meeting recollection): NOT a relocation and NOT a trim of §2.1.2.** Prof *liked* §2.1.2 as it is; his point was that "how the `\mathcal M` amplitudes appear" is left unanswered — he wants the Feynman rules *explained somewhere*. So Task S is purely **additive**: §2.1.2 unchanged except a one-line pointer.
- **DONE:** new `\section{Feynman Rules}` in Appendix B (`appendices/appendixB.tex`, `sec:FeynmanRules`, B.1): propagator table (quark/gluon/ghost, massless + massive-quark parenthetical, graphical reps), external-state completeness relations (`\sum u\bar u=\slashed p`, `\sum \varepsilon_\mu\varepsilon_\nu^*=-g_{\mu\nu}+\text{gauge}`), vertex table (`q\bar qg`, `ggg`, `gggg`, `c\bar cg`, `\gamma^*q\bar q` with graphical reps). All expressions checked against Peskin/ESW conventions as a consistent set (`ggg`/ghost carry no explicit `i` in that convention; `gggg` sign `-ig_s^2` and colour `f^{abe}f^{cde}` fixed in review). Cited `\cite{Ellis:1996mzs,Peskin:1995ev}` on both tables. §2.1.2 (`:185-186`) adds "An amplitude … is computed from a Feynman diagram using Feynman rules. This ruleset is given in Section~\ref{sec:FeynmanRules}." B.1 points back to Ch4's full application (`subsec:masslessA30`). No full worked example in B.1 — Ch4 has it. Build clean, 119 pp.
- Loose end (minor): B.1's `\ref{subsec:masslessA30}` prints "Subsection" but the target is a `\subsubsection` — same label-type mismatch family the 27-Aug coherence pass fixed 7 of.

**Normalisations — it's a PRESENTATION problem, not a section. [Yours]. Task T — §2.3 HALF DONE (30 Aug); structural `G_k`/`Λ_l`/`𝒩` unification with §3.5.3 still bundled with Task M.** Entangled with Task M (Ch3 §3.5.3).
- **DONE 30 Aug (finish of the §2.3 half):** the `S_ε` / `G_k` / `C(ε,k)` / `𝒩` apparatus collapsed to one short paragraph + `eq:loopNorm` (`G_k=(8π²)^{n-2}Λ_l`) + `eq:normN` (`𝒩=C/Φ_2`, `C=G_kS_ε`), with the hedge compressed to a one-line pointer to `tab:GkNorm` (§3.5.3). `eq:alphaRescaling` prose tail cut from ~6 lines to 2 (`μ_0`, `β_0`, `N=3`, `N_f` glossed). Both deleted labels (`eq:loopNorm`, `eq:normN`) re-added — Ch3 `:318`/`:630` resolve again. `S_ε` cite switched `Bardeen:1978yd`→`Gehrmann-DeRidder:2005btv`. Build clean. This went a bit past the "two quick wins" scope but stopped short of the shared-symbol unification with §3.5.3, which stays in Task M.
- Tiny nit not yet fixed: "the renormalisation scale **is scaled as** `μ²=q²`" → "is set to".
Clarified 28 Aug (Henrique): Prof was not pointing at one section. His complaint is the **overall presentation** of the normalisation factors — too much prose, it breaks the narrative flow. The apparatus (`S_\epsilon`, `G_k`, `\Lambda_l`, `C(\epsilon,k)`, `\Phi_2`, `\mathcal N(\epsilon,k)`) sits in **§2.3** (`:338-405`) and is bridged again in **Ch3 §3.5.3** ("Normalisations and Convention Bridge", `tab:GkNorm`).
- **Done 28–29 Aug (§2.3 half, `02-physics-background.tex:338-405`):** cut the "why 4π/γ_E/Γ(1−ε) appear from d-dimensional spherical coordinates" textbook paragraph (pure over-explanation); tightened the `S_ε` sentence (MS̄ + `\cite{Bardeen:1978yd}` retained, "using CDR" clumsiness removed); kept a one-clause `α_s/(2π)` justification (load-bearing — `β_0=(11N−2N_f)/6` is only right in that convention, and `(8π²)^k` needs it); moved `k=(n-2)+l` into the `C(ε,k)` equation line and killed the resulting dangling "and is, itself defined as," fragment; de-fragmented the `Λ_l` sentence; `n`/`l` now defined right after `eq:loopNorm`. Rebuilt clean, all 7 inbound labels (`eq:loopNorm`/`eq:normN`/`eq:alphaRescaling` from Ch3/Ch4) resolve. **Assessed honestly as only ~20% of Prof's density complaint** — cut ~4 of ~50 lines. The passage still hits 4 displayed normalisation equations + 5 symbols back to back.
- **Still open, deferred to the Task M joint pass:** `eq:alphaRescaling` prose tail → one sentence (or move the whole coupling-renorm eq to §2.2.2 where UV renorm is discussed); the `G_k→C(ε,k)→𝒩` three-step chain → collapse to `𝒩(ε,k) = (8π²)^k(4π)^{−ε}e^{εγ_E}/Φ_2` + pointer; defer `Λ_l` introduction to Ch3; hedge paragraph (`:386-390`) → one line. Can't finish in §2.3 alone — the symbols are shared with §3.5.3, which Prof separately suggested cutting; do both files at once.
- "Simplify equations and review normalisations" (his §2.4 note) is the same theme. The unverified-`C(\epsilon,k)` caveat is still flagged in §2.3 and Task D.

#### Chapter 3

**§3.6 "Higher-Level Functions" — condense to a list/table.** Already **Task P**. Prof repeated it verbatim in the meeting ("going on too much on the inner workings, just list them or show a table"). Confirms Task P; no change, still blocked on the Task D T-object destination.

#### Chapter 4

**Rename `\mathcal S_3 \to s_{123}`, `\mathcal S_4 \to s_{1234}`; do NOT touch `d\mathcal S_3`. Mech-22 — DONE 28 Aug 2026 by Henrique himself.** Not reviewed by Claude; if a diff review is wanted later, check the `:138-281` block plus any prose mentions.

**Show antenna results reduced to the quoted master integrals. [Yours]. Provisional Task V.**
For each antenna result in Ch4, show the reduction to the stated master-integral basis before the master substitution. Overlaps with the existing §4 items "Expand the `A_3^0` worked example with one representative IBP reduction term" and "Replace repeated 'agrees with the literature' claims with one evidence-dense summary table." Treat as one task.

#### Chapter 5

**Show the integrated T-objects with their `\epsilon` poles, à la hep-ph/0403057. [Yours]. Provisional Task W.**
`subsec:Tobjects` (`chapters/05-validation-r-ratio.tex:53`), `eq:integratedT` (`:137`). Prof wants each integrated `\mathcal T`-object shown with its explicit Laurent expansion so the pole cancellation is visible and the subtraction reads as clearly complete — the way `Gehrmann-DeRidder:2004ttg` (hep-ph/0403057, "Infrared structure of e+e- -> 2 jets at NNLO") lays it out. Directly overlaps the existing §5 item "Add a compact table of the pole cancellation by perturbative order and NNLO colour/flavour channel." One task.

#### Considered and deferred — NOT a task

**Numerical validation with NNLOJET.** Prof raised it as "a further point that might be interesting," explicitly **not** a "do it." Henrique's own view: it's a stretch to claim NNLOJET validates AntCalc when both use the same underlying antenna expressions. Sketch, if ever pursued: NNLOJET 4.1, the e⁺e⁻ setup from arXiv:2503.22804, `\sqrt{s}` = 900 and 365 GeV, `\mu=\sqrt{s}`, three runs (LO/NLO/NNLO), observable TBD (`p_T^\text{jet}`, invariant mass, …), likely a case-studies chapter. Leave parked here; do not letter it. (Related to the existing §4 item "State in the conclusion that an independent numerical MC check remains future work" — that stays.)

### Open questions — RESOLVED with Henrique 28 Aug 2026

1. **§2.4.1 rewrite depth** → **clean rewrite** against 0505111 §2.1.
2. **"§2.1 normalisations"** → not section-specific; it's the overall *presentation* (too much text, breaks flow). Task T reframed accordingly, spanning §2.3 + Ch3 §3.5.3.
3. **T-objects in Ch2** → **remove the `\subsubsection{$T$-objects}` entirely** (`:793-828`). Ch5 T-objects untouched (Task W needs them).
4. **Prof's §2.4 sub-numbering** → he uses the **current compiled draft numbers** (2.4.2 = Antenna Functions, 2.4.3 = Colour Algebra).
5. **`tab:antFamilies`** → layout is **good as-is**; add (a) the defining partons of each antenna as a reference, (b) an addendum that the non-antennae aren't antennae by definition. → Task Q3.
6. **Task lettering** → convention confirmed; Q, Q2, Q3, R, R2, S, T, U, V, W + Mech-22 stand. Reletter to strict one-letter-per-commit when sequencing the work.

**Still to settle at execution time (not blocking):** whether Q2/Q3 fold into Q's commit or sit adjacent; how much of §2.1.2's Feynman-rules content moves vs. stays as a pointer (Task S); whether removing the Ch2 T-objects subsubsection leaves Ch3 (`03-package-framework.tex:~358`, `T_\text{Lead}` etc.) with no upstream definition — the Ch3 T-primer was already an open Task-D-extension item, re-confirm it covers this.

### Execution plan (walked through with Henrique 28–29 Aug, to start in full 30 Aug)

**Core rule that prevents rework:** *nothing fine-grained inside §2.4 happens before Task Q.* Everything that can safely precede Q is outside §2.4. No polishing `tab:sigmaByOrder`, `eq:subTerms`, the T-objects subsubsection, `eq:bornNorm`, or `fig:antennaFactorisation` before Q/Q2 — all of it gets rewritten.

**Phase 1 — outside §2.4 (no rework risk, any order) — COMPLETE (30 Aug 2026).**
- **U — soft-quark fix, §2.2.1. DONE.** Grounded vs. 0505111 §8.2 (no soft-quark limit anywhere in the antenna catalogue), wrong paragraph deleted, Henrique wrote the replacement, `Ellis:1996mzs` dropped from the bad claim.
- **R2 — limits list/table. DONE.** `tab:limits` in §2.2.1, inline `longtable`, by-configuration (limit / kinematic condition / vanishing invariants), 14 rows checked vs. eqs. 8.14–8.30, no antenna column (Q adds it).
- **S — Feynman rules. DONE (re-scoped — additive, not a relocation).** Appendix B.1 `\section{Feynman Rules}`: propagator + vertex tables + external-state relations, Peskin/ESW-consistent, cited; §2.1.2 gets a one-line pointer, unchanged otherwise.
- **T (§2.3 half). DONE.** Apparatus collapsed to one paragraph + `eq:loopNorm` + `eq:normN`; `eq:alphaRescaling` tail trimmed; deleted labels restored; hedge → one-line pointer to `tab:GkNorm`. Structural `G_k`/`Λ_l`/`𝒩` unification with §3.5.3 still in Task M.

**Phase 2 — the §2.4 rewrite (strict order):**
- **Q — rewrite `sec:antSubForm` intro + §2.4.1 against 0505111 §2.1.** Sets the whole §2.4 narrative, notation, and figure plan (0505111 Figs 1/2/3). Deletes the T-objects subsubsection (`:793-828`). Writes the non-antennae explanation **once**. *(IN PROGRESS 30 Aug — intro + `\subsection{Subtraction Terms}` (NLO + NNLO) drafted and committed, old content in `\iffalse`; see the full progress + PARKED list under the "§2.4.1 CLEAN REWRITE … Task Q" entry above.)*
- **Q — paired: add the Ch3 T-primer.** Same logical change as deleting the Ch2 T-block — otherwise `03-package-framework.tex:358` etc. (`T_\text{Lead}`/`T_\text{Sublead}`/`T_\text{QL}`) point at nothing. Own small commit right after Q, no gap between them.
- **Q2 — redo §2.4.2 "Antenna Functions."** Built to serve the narrative Q just fixed. Folds in: the `eq:bornNorm` explicit-`l=1`/`l=2`-cases follow-up (do *not* do separately), the figure execution, and Task L per-antenna Feynman diagrams if they land here.
- **Q3 — `tab:antFamilies`.** Add the defining-partons reference (`A^0_3(q,g,\bar q)` style) + the non-antennae addendum (references the explanation written in Q).
- **R — AP + eikonal checks on `A^0_4`.** Needs the antenna definitions in final form → after Q2. Ties to the Ch1 "local eikonal/AP-splitting validation" mention.

**Phase 3 — Ch3, then Ch4/5:**
- **Task M + T (structural half)** — joint §2.3 / §3.5.3 normalisation collapse, both files open at once. Defer `Λ_l` introduction to Ch3.
- **Ch3 T-object extension** (§3.3.2 / §3.3.3 / §3.5.4 / §3.6 → Ch5 `subsec:Tobjects` outright vs. short pointer) **+ Task P** (condense §3.6 to the itemised one-liners / table).
- Remaining Ch3 review tasks (H, I, J, K, L, N, O) as they fit.
- **V — Ch4 antenna results reduced to the quoted masters.** Merge the existing §4 "one representative IBP term" + "replace 'agrees with literature' prose with a validation table" items.
- **W — Ch5 integrated T-objects with their ε poles**, à la hep-ph/0403057. Merge the existing §5 "compact pole-cancellation table" item.

**Done outside this order:** Mech-22 (Ch4 `\mathcal S_3 → s_{123}`) — completed by Henrique 28 Aug, not Claude-reviewed.

---

### Chapter 1 — Introduction — DONE (25 Aug 2026) [Mine — all Prof-supplied text or minimal-diff edits]
- [x] Opening sentence: name the LHC/CERN explicitly as the motivating example of "high-energy collider experiments," not just an unspecified reference.
- [x] "Achieving higher orders of perturbation is not simply..." → "Obtaining higher-order perturbative predictions is not simply a matter of...".
- [x] Remove "(in trajectory)" after "collinear" — not a meaningful qualifier for collinear partons.
- [x] Rewrite "these individual contributions are divergent and must be arranged before they can be evaluated reliably" — "arranged" is vague. Replace with explicit language: IR singularities must be extracted and cancelled before numerical integration; for IR-safe observables (e.g. the hadronic $R$-ratio) the cancellation is between real and virtual contributions, leaving a finite result. Prof supplied exact replacement text.
- [x] Reword the following "antenna subtraction formalism provides the framework for this rearrangement" sentence to match (framework for *extracting and cancelling* these singularities).
- [x] "Their systematic construction and integration are therefore central ingredients in higher-order QCD calculations" → qualify as central *within the antenna subtraction framework* specifically. Other subtraction schemes (Catani–Seymour, FKS, sector-improved, ...) don't use antenna functions at all — the unqualified claim overclaims generality.
- [x] Define the $R$-ratio in one sentence at first mention (currently used before being defined) and set it in italics (\textit{$R$-ratio}); tighten the sentence on local eikonal/AP-splitting validation. Prof supplied phrasing.
- [x] Rewrite the chapter-outline paragraph: it currently uses $A_2^1$, $B_4^0$, $A_3^0$, and "$T$-objects" before that notation has been introduced anywhere — confirmed against `chapters/01-introduction.tex:47-55`. Prof's rewrite avoids the problem by naming only "the $A_3^0$ antenna" in prose and deferring all other notation to where it's actually defined (§2.4.3); adopt that structure. Note: paragraph 2's mention of the $R$-ratio still technically precedes paragraph 4's fuller definition — flagged to Henrique as a minor ordering nuance, left as is by his choice.

### Chapter 2 — cross-cutting concern
- [ ] **Grade-relevant, not just stylistic:** the chapter is currently too long and too technical in a way that dilutes the thesis's own motivation and objectives. In places it's oversimplified to the point of losing rigor (soft/collinear power counting, loop-momentum divergence description — see below, both are actual errors, not just clarity issues). In other places (§2.4/§2.4.1/§2.4.2, Eq. 2.27, three footnotes on one page) it's over-indexed to the point of being hard to follow. Prof is explicit that this can pull the final grade down even with correct physics and a strong defence. General fix: replace index-heavy taxonomy prose with tables/lists/figures. Model the antenna-subtraction exposition on Gehrmann-De Ridder et al., hep-ph/0505111 — §2.1 explains NLO antenna subtraction in a couple of pages using a figure; §3/Table 1 gives a compact antenna taxonomy by letter (A/B/C/D) and particle content (q,g,q̄ / q,q̄,q,q̄ / ...). This converges with the "rebalance for readability" theme already in §5 below — two independent reviews flagging the same density problem, treat as confirmed and high priority.

### §2.1.1 (QCD Lagrangian) — DONE (25 Aug 2026)
- [x] Gauge-fixing and ghost content moved from the old footnote into the main text: names the convention (t'Hooft–Feynman, $\lambda=1$), shows $\mathcal L_\text{gf}$ and the resulting propagator, shows $\mathcal L_\text{ghost}$, and states explicitly that ghosts leave only the two transverse gluon polarisations in physical amplitudes (Pires's specific requested "why"). $\lambda=1$ confirmed directly via `Options[CreateFeynAmp]` in Henrique's own Mathematica session (not just FeynArts' documented default) and cross-checked against the `ExcludeParticles` lists in AntCalc's own build-stage code, which exclude the electroweak ghosts (`U[1]`-`U[4]`) but never the QCD ghost (`U[5]`) — confirming the gluon ghost is genuinely present in the $A_3^1$ one-loop generation, not assumed. Citation swapped from the wrong source (`FeynArts1Manual`, the 1991 v1.0 manual) to the correct one (`Hahn:2000kx`, the 2001 FeynArts 3 paper). The `ExcludeParticles`-level implementation detail was deliberately kept out of Ch2 and left as optional Ch3 (`subsec:oneLoopRoutes`) material if Henrique wants to document it later.

### §2.1.2 — DONE [Mine]
- [x] Rewrite "the interaction terms in $\mathcal L_q$ and $\mathcal L_g$ generate Feynman rules..." — interaction terms fix the vertices; vertices + propagators together are the Feynman rules. Add that a Feynman diagram is one term in the amplitude's perturbative expansion, and the full amplitude sums over all contributing diagrams at a given order. Prof supplied text.
- [x] Eq. (2.9): define what the $D$ and $D'$ summation indices represent — currently unexplained.

### §2.2.1 (Infrared divergences) — DONE, one item retired [mixed Mine/Yours]
- [x] Open the section by defining real vs. virtual corrections before the soft/collinear limits. **[Mine]**
- [x] **Physics correctness issue, formerly "Task B".** **[Yours]** — resolved via a worked example using $\gamma^*\to q(k_1)g(k_3)\bar q(k_2)$ (already the process in Fig. 2.1): collinear → single pole $1/s_{13}$; soft gluon → double pole $1/(s_{13}s_{23})$; soft quark → same pole as the collinear limit, no separate counterterm needed (refined beyond Prof's original blunter framing, verified computationally against Henrique's own `BuildAntenna[A,3,0]` output); NNLO soft-$q\bar q$-pair case added as a genuine double pole in the pair's own invariant, sourced to Gehrmann-De Ridder–Gehrmann–Glover eq. 8.4 (verified by reading the paper directly). Cites `\cite{Ellis:1996mzs}` for the general framework, `\cite{Gehrmann-DeRidder:2005btv}` for the NNLO claim.
- [x] Footnote on $p_k^2$ — missing "$=0$". **[Mine]** Duplicated the same fix into §2 "Specific source corrections" below, since it's the identical underlying footnote.
- [x] Rewrite the "experimentally indistinguishable" / KLN paragraph for rigor. **[Mine]**
- [x] Expand the real/virtual subtraction-structure explanation and label `eq:subScheme` as the *NLO* schematic only. **[Mine]**
- [ ] ~~Add an explicit thesis-motivation paragraph... central objective of this thesis...~~ **RETIRED — do not do separately.** Superseded by the explicit $d\sigma^S$/$d\sigma^T$ antenna-sum equations planned in the §2.4 restructuring below (**Yours**) — that item does the same motivational work more concretely, from an equation rather than prose assertion. Writing both would be redundant.

### §2.2.2 (Ultraviolet divergences) — DONE [Mine]
- [x] **Correctness/clarity issue**: "The soft and collinear limits on $l$ behave exactly as described above for the outgoing momenta" was not accurate as a blanket statement. Rewritten: loop-momentum regions soft/collinear to an *external massless* momentum generate the IR poles contributing to $d\sigma^V$'s singular structure; large-$|l|\to\infty$ is a separate, UV, divergence.
- [x] Rewrote the UV power-counting paragraph and the renormalisation paragraph; added a forward-reference to `Subsection~\ref{subsec:oneLoopRoutes}` (Ch3's one-loop routes, where $A_3^1$ renormalisation is actually made explicit — checked the label and its content exist before citing it).

### §2.3 (Dimensional regularisation) — mostly done, one item still open [mixed]
- [x] Rewrite the opening paragraph to explicitly connect the subtraction split → numerical finiteness in 4D → analytic integration in $d=4-2\epsilon$ → pole cancellation via KLN. **[Mine]**
- [x] Terminology: "dimensional regularisation methods" → "dimensional regularisation **schemes**". **[Mine]**
- [x] State explicitly which scheme is used throughout — **Conventional Dimensional Regularisation (CDR)**. Named HV and FDH as the alternatives, with citations. **[Mine]**
- [x] Rewrote the paragraph that conflated CDR, $\overline{\text{MS}}$, and the antenna normalisation convention into the separated $S_\epsilon$ / $G_k$ / $C(\epsilon,k)$ structure. **[Mine]** — the **verify against Ch4/Ch5 conventions** caveat from the original note still stands and hasn't been separately re-checked; worth a pass before final submission.
- [x] **Task C — DONE (26 Aug 2026). [Yours]** Resolved as both a convention citation *and* a forward-reference to the existing derivation, rather than either alone — checked first that `subsec:phaseSpaceFactor` (§2.4.4) already derives the recursive factorisation ($d\Phi_3=d\Phi_2\,d\Phi_{ijk}$, $d\Phi_4=d\Phi_2\,d\Phi_{ijkl}$, cited to `Catani:1996vz`), so this was a forward-reference gap, not a missing derivation. Final text: "...as is convention in the antenna-subtraction literature \cite{Gehrmann-DeRidder:2005btv}. The factorisation method used is discussed in Subsection~\ref{subsec:phaseSpaceFactor}, and written in Eq.~\ref{eq:phi2Factor}." Also combined the two previously separate $d\Phi_3$/$d\Phi_4$ equations into one `\split` block under a single new label `eq:phi2Factor` so both could be referenced together. Verified by rendering the compiled PDF (pp. 21–22, 27): citation resolves to `[4]`, both cross-references resolve correctly (Subsection 2.4.4, Eq. 2.45), combined equation renders as one numbered pair. One grammar slip caught in review (a stray "in" — "the factorisation method used **in** discussed in..." ) and fixed.
- [x] Added the coupling-renormalisation paragraph (bare $\alpha_s^0$ vs. renormalised $\alpha_s(\mu)$, $\mu_0$ vs. $\mu$, $\beta_0$, $N=3$, $N_f$); added a citation for $\beta_0$ (`\cite{Ellis:1996mzs,Peskin:1995ev}`). **[Mine]**

### §2.4 (full section) — expanded 25 Aug 2026 after Prof's follow-up note on this section specifically; re-reviewed and split into Task D / Task E on 25 Aug 2026

Prof confirmed §2.4 needs an actual rewrite: confusing, needs more rigour in places, and the subsection order is wrong. Re-checked every item below against the current source on 25 Aug 2026 — all confirmed genuinely non-mechanical, so **all of §2.4 is [Yours]**, none of it automatic. Split into two tasks: **Task D** is the core rewrite, where the sub-items are entangled around one underlying decision (how much T-object content survives in Ch2 vs. moves to Ch5, and how the antenna-sum equations get written once that's settled) — treat as one unit of work so it lands as one commit. **Task E** is the separable readability cleanup, confirmed in the original pass as *unaffected* by the Task D entanglement — it can be done independently, though doing it after Task D means the taxonomy table/figure can reflect the finished §2.4 structure rather than the current one.

**Task D — core §2.4 rewrite (entangled, one commit):**
- [x] **Reorder subsections.** Done earlier in this session (before the transcript segment this file's later entries describe). Verified 26 Aug 2026 against current source, since the checkbox had gone stale: `chapters/02-physics-background.tex:435`, "NLO and NNLO Subtraction Terms," is now the first subsection of §2.4, immediately after `sec:antSubForm` opens at `:415` — matches the planned reorder exactly.
- [x] **Write the general dσ^S (and dσ^T) equations as explicit antenna sums.** Done earlier in this session, same stale-checkbox situation as above. `eq:subTerms` (in the new 2.4.1) gives the abstracted $2C_F\sum_{a(,s)}f_a(N,N_f)X_{ij,(a(,s))}^l\,d\sigma^B$ form for all five terms ($d\sigma^V_\text{NLO}$, $d\sigma^S_\text{NLO}$, $d\sigma^{VV}_\text{NNLO}$, $d\sigma^T_\text{NNLO}$, $d\sigma^S_\text{NNLO}$) — an abstracted alternative to the reference paper's fully-expanded eq. 2.5, chosen because it matches this thesis's actual scope (assembling known antenna combinations, not deriving numerical MC subtraction machinery) — see the session's earlier back-and-forth on this before it landed.
- [x] **Add a momentum-mappings discussion.** Done earlier in this session, same stale-checkbox situation. Verified 26 Aug 2026: present at `chapters/02-physics-background.tex:514-522`, right after the antenna-sum equations, covering the reduced on-shell momenta $\tilde p_I,\tilde p_K$ and explicitly noting the pure-virtual terms ($d\sigma^V_\text{NLO}$, $d\sigma^{VV}_\text{NNLO}$) need no mapping.
- [x] **Move `subsec:antFamilies` ("Antenna Families and T-Objects") to Chapter 5.** Done 26 Aug 2026. `chapters/02-physics-background.tex`'s `subsec:antFamilies` retitled to "Antenna Families" and trimmed to the taxonomy + $B_4^0$/$C_4^0$ definitions only (`eq:cAntTheory` unaffected, stays put). The $T$-object machinery (general $T$-object definition/footnote, `eq:ATypeTObj`, the $A_3^1$/$\breve A_2^2$ NNLO-extension itemize, and the $B_4^0$/$C_4^0$-specific $T$-object equations) moved into `chapters/05-validation-r-ratio.tex`'s §5.2 "Assembly from Integrated Antennae", which now carries a new label `subsec:Tobjects` and opens with the moved definitional content before the pre-existing $R$-ratio assembly equations that actually use it. `chapters/03-package-framework.tex:744` (the `TObject`-build-route reference) retargeted from `\ref{subsec:antFamilies}` to `\ref{subsec:Tobjects}`/Ch5 — see resolution note below. Full clean rebuild confirmed no undefined/duplicate references. **Still open:** the new Ch5 prose is a mechanical transplant, not yet reviewed for flow/continuity in its new home — see the new Ch5-review item added below, to run after the logical-coherence pass on Ch2.
- [x] **Replace the `\sim` equations with exact ones.** Done 26 Aug 2026. Went to the primary literature (Gehrmann-De Ridder, Gehrmann, Glover, hep-ph/0505111, eqs. 2.8/2.10/2.11/2.18/2.19/2.23/2.35) rather than the paraphrase. First equation (`\int_1 d\sigma^S\sim\int d\Phi_{ijkl...}\,X_n^l`): the Born/jet-function piece factors out exactly by the phase-space factorisation, so `eq:sigmaSDef`'s $\sum_\text{unresolved pairs}$ structure carries straight through — now reads `\int_1 d\sigma^S=\sum_\text{unresolved pairs}\left(\int d\Phi_{ijkl...}\,X_n^l\right)d\sigma^B`. Second equation (`eq:antPhaseSpaceFact`): found it was doing two things at once — the $\int d\Phi_{ijkl...}X_n^l=\frac1{\Phi_2}\int d\Phi_n\,X_n^l$ half was already exact (follows directly from `eq:phi2Factor` divided by the constant $\Phi_2$), only the leading "$\mathcal X_n^l\sim$" was inexact, and redundant besides — `eq:antennaIntegral`, two equations later, already states $\mathcal X_n^l$ exactly with the correct $\mathcal N(\epsilon,k)$ prefactor. Dropped "$\mathcal X_n^l\sim$" from the equation and softened the lead-in sentence ("is then rewritten as," not "is then computed as,") to match. One near-miss caught in review: an intermediate edit briefly left `eq:antPhaseSpaceFact` claiming `$\mathcal X_n^l = \frac1{\Phi_2}\int d\Phi_n X_n^l$` outright — contradicting `eq:antennaIntegral`'s `$\mathcal N(\epsilon,k)$` four lines later by a factor of `$C(\epsilon,k)$`. Caught and fixed before it reached the build. Also surfaced, unprompted: `C(\epsilon,k)=(8\pi^2)^k(4\pi)^{-\epsilon}e^{\epsilon\gamma_E}` (`:378`) doesn't actually match the reference paper's own usage at $k=2$ — their NNLO 4-particle case (eq. 2.23) squares the *entire* prefactor including $S_\epsilon$, while their one-loop 3-particle case (eq. 2.35, also nominally $k=2$ under the thesis's own $k=(n-2)+l$) does *not* square $S_\epsilon$ at all, suggesting the correct power might track $(n-2)$ alone, not $k$. Left unresolved and un-fixed — this is the same "unverified 'matches Ch4/Ch5 conventions'" caveat already flagged on `C(\epsilon,k)` in §2.3, now with a concrete literature discrepancy attached, not something to silently patch. Full clean rebuild confirmed after each step.

**Roadmap for Task D (drafted 26 Aug 2026, for execution ~27 Aug) — a proposed shape, not a final decision.** Went through all five current subsections of §2.4 line by line, plus the reference paper's actual §2.1/Table 1 (not just the paraphrase) and every cross-chapter dependency, to ground this. Henrique reacts/adjusts before writing.

*§2.4 intro + `subsec:antennaFuncs` (`chapters/02-physics-background.tex:415-515`) — KEEP, mostly as-is.* The singular-factor factorisation, tree-level antenna definition (`eq:antennaTree`), generalisation to loop antennae, and the $k$-index are all foundational and not T-object content. `eq:bornNorm` (`:503-509`) is the index-heavy equation (Task E, not D). Candidate spot for the reference paper's colour-connection figure (their Fig. 2) — nothing here currently illustrates the $i,j,k\to I,K$ parent/daughter relationship pictorially, it's prose-only.

*`subsec:colourAlg` (`:517-593`) — KEEP as-is.* Colour algebra, Casimir invariants, the leading/subleading/fermion-loop ($N$/$1/N$/$N_f$, i.e. tilde/hat) taxonomy, the four-quark Fierz identity. Not T-object content, no restructuring needed — just the 2 footnotes here that are part of Task E's "three footnotes" trim (`:454` and `:586`; the third is `:476`, also in `subsec:antennaFuncs`).

*`subsec:antFamilies` (`:594-738`) — SPLIT, this is the crux.* Confirmed it bundles two genuinely different things:
  - **(a) Antenna-family letter taxonomy + $B_4^0$/$C_4^0$ definitions** (`:596-664`, including `eq:cAntTheory` at `:641`) — KEEP in Ch2, compress into the Task E taxonomy table (this *is* the content Table 1 in the reference paper compresses). Not what Prof's T-object complaint was about.
  - **(b) The $T$-object machinery itself** (`:666-738`: definitions, the $A_3^1$/$\breve A_2^2$ NNLO extensions, the $B_4^0$/$C_4^0$-specific $T$-object equations `eq:cAntTheory`-derived) — CUT from Ch2, MOVE to Ch5.
  - This split resolves `chapters/03-package-framework.tex:288` (cites `eq:cAntTheory`) for free, since that equation stays in part (a). `chapters/03-package-framework.tex:744` (cites the subsection for the `TObject` build-route explanation) — **resolved 26 Aug 2026:** retargeted to `\ref{subsec:Tobjects}` in Ch5 rather than rephrased away. Accepted as a legitimate forward reference: Ch3 describes the `TObject` function's build route (assembling Ch2 antennae per the method now formally defined in Ch5), and that assembly method genuinely isn't defined in prose until Ch5, for the same reason Prof gave for moving it out of Ch2 in the first place. Left unrephrased rather than softened, since the earlier concern (Ch2 pointing forward to Ch5) doesn't reapply the same way here — Ch3 explaining an API call whose full definition comes later is ordinary technical-writing practice, not the "reads backwards" problem Prof flagged.

*`subsec:phaseSpaceFactor` (`:739-841`) — KEEP the antenna phase-space factorisation and `eq:antennaIntegral`/`eq:phi2Factor` machinery.* The closing "Implications on the $T$-objects" subsubsection (`eq:integratedT`) — **done 26 Aug 2026**, cut from Ch2, landed in Ch5's `subsec:Tobjects` (see the destination-history entry below). This is also the natural place for the new momentum-mappings material (Task D item 3) — still open.

*`subsec:nloAndNnloSubtraction` (`:842-941`) — DISSOLVES as a standalone subsection.* Front two-thirds (`:842-909`: why $d\sigma^S$/$d\sigma^T$ are needed, the NLO/NNLO cross-section splits, the exactness argument specific to this process) is genuine motivation — becomes the new, un-subsectioned **opening of §2.4** (mirroring the reference paper's own §2.1, which isn't subdivided either). Needs real rewriting, not cut-and-paste: it currently opens "So far... we have discussed what antenna functions are..." — a recap framing that assumes antenna functions were already introduced. Once this opens the section instead, that framing inverts to a preview ("we will build...", not "we have built..."). This is also where the new explicit $d\sigma^S$/$d\sigma^T$ antenna-sum equations (Task D item 2) land, right where "NLO only needs $X_3^0$-type, NNLO needs $X_4^0$/$X_3^1$-type" gets to follow visibly from the sum, exactly as Prof asked. Back third (`:910-941`: the $T$-object-heavy rewriting of $\sigma^\text{NLO}$/$\sigma^\text{NNLO}$, `eq:nnloCross`) — **resolved for free, checked 26 Aug 2026**: this was never carried over verbatim during drafting. The current `eq:nnloCross` (`chapters/02-physics-background.tex:460`) is already the clean, antenna-based form written for Task D item 2, not the old $T$-object version. Nothing to cut here.

*Destination history, 26 Aug 2026 — tried Appendix B, reverted the same day.* Ch2's own $T$-object content (`tab:sigmaByOrder`'s column, the "Implications" subsubsection) was cut and, together with the `subsec:Tobjects` block already sitting in Ch5 from the earlier `antFamilies` move, relocated wholesale to a new `appendices/appendixB.tex` section, "The $\mathcal T$-Object Notation." Reasoning at the time: $T$-objects are a derived notation, not independent physics, and Ch5's own text already reaches the correct final $R$-ratio result via the antenna-only form, so nothing physical is lost keeping them out of the main narrative anywhere.

**Reverted after further discussion with Henrique, same day — his point, on reflection correct.** The $T$-object form of the $R$-ratio equation groups terms by *final-state topology* ($\mathcal T_{q\bar q}^4$, $\mathcal T_{q\bar qg}^4$, $\mathcal T_{q\bar qq\bar q}^6$, ...), which is exactly what motivates "why these particular terms, at this order" — the antenna form groups by colour structure instead, essential for computation but not as direct an answer to that question. Since the $T$-object *equation* genuinely earns its place in Ch5's main argument, splitting its *definition* into an appendix would recreate the same "notation used before it's defined" problem this whole exercise exists to fix, just one level down (Ch5 using $T$-objects with the definition sitting in an appendix, instead of Ch2 using them with no definition at all). Also a more literal reading of Pires's own conditional after all — his "if useful, move to Ch5" test comes back *positive*, not negative as first assessed, once the topology-grouping argument is accounted for.

**Net result: back to the state documented in the `Move subsec:antFamilies` entry above, plus `eq:integratedT` joining it.** Ch2 stays fully clean (that part of the appendix attempt was correct and stands). The full $T$-object narrative — general definition, `eq:ATypeTObj`, the two NNLO extensions, the $B_4^0$/$C_4^0$ $T$-objects, and now also `eq:integratedT` — lives in `chapters/05-validation-r-ratio.tex`'s `subsec:Tobjects`. `chapters/03-package-framework.tex:804`'s reference points there again. `appendices/appendixB.tex` shows no net diff from before the appendix attempt.

**Correction, 26 Aug 2026 — the revert above was initially incomplete, caught by Henrique asking "did you make prose changes?" rather than by any build check.** The first revert pass restored Ch5 to its state right after the *original* `antFamilies` move (before `eq:integratedT` joined it), then deleted the appendix section outright — silently dropping `eq:integratedT` and its explanatory paragraph from the thesis entirely, with no dangling reference to catch it, since the one citation to it (`tab:sigmaByOrder`'s caption) had already been removed during the earlier Ch2 cut. A clean build is therefore not sufficient evidence that a content move preserved everything — it only proves references resolve, not that content wasn't dropped between edits with no reference pointing at it. Fixed same session: `eq:integratedT` and its explanation re-added to `chapters/05-validation-r-ratio.tex` (`:135-146`ish), full clean rebuild reconfirmed.

**Still open:**
- `chapters/05-validation-r-ratio.tex` — the pacing/coherence review flagged when the `antFamilies` split first landed (see the `Item added 26 Aug 2026` entry below) still applies, now covering a slightly larger block since `eq:integratedT` joined it on this second landing.
- Chapter 3's own T-object-heavy sections (§3.3.2, §3.3.3, §3.5.4, §3.6 — see the scope-extension note below) — untouched. Same kind of call as before: how much moves to Ch5 outright vs. gets trimmed to a light pointer, still entangled with Tasks O and P. Destination is Ch5 again, not the appendix.

*Net effect on length:* §2.4 goes from 5 subsections to roughly 4 (intro+`antennaFuncs`, `colourAlg`, compressed `antFamilies`, `phaseSpaceFactor`), loses ~180 lines of T-object content to Ch5, gains the new explicit-sum equations and mapping discussion (a few paragraphs) and, separately via Task E, the taxonomy table replacing prose. Should net out similar length or shorter than today, not longer — consistent with Prof's own "couple of pages" reference-paper anchor.

*Final item, added 27 Aug 2026, deliberately deferred to the very end of Task D (and Task E) — a systematic logical-coherence and cross-reference pass — DONE 27 Aug 2026.* Scope ended up much wider than originally planned: Henrique asked for a full pass across all of Chapter 2, the moved material in Ch5/Appendix B, and cross-references elsewhere, run twice independently (Claude's own pass, plus a second pass through ChatGPT with a purpose-written review prompt) and then compared. Neither pass was trusted blind — every finding from both was checked against the actual current source/PDF before acting on it.

**The one serious substantive finding, from ChatGPT's pass, confirmed real: a genuine definition-order regression in Chapter 3, caused by this session's own `T`-object move (Task D).** `chapters/03-package-framework.tex:358` uses $T_\text{Lead}$/$T_\text{Sublead}$/$T_\text{QL}$ with zero local definition. Before the move, this was fine — the general `T`-object definition lived in Ch2, read before Ch3. Now it's in Ch5, read after Ch3. **Not yet fixed** — needs a compact, *locally motivated* `T`-notation primer in Ch3 (not Ch2 — reintroducing it there would recreate Prof's original complaint) right before this first use, plus a forward-pointer to `subsec:Tobjects`. Also needs the terminology split ChatGPT flagged: "$T$-terms"/"interference components" for the raw pieces ($T_\text{Lead}$ etc.) vs. "$T$-object" reserved for $T_\text{topology}^{2(k+1)}$ itself.

**A second substantive finding, also confirmed, also not yet fixed:** `tab:sigmaByOrder` in Ch2 shows $\mathcal A_2^1\mathcal A_3^0$ product terms with zero derivation — the only explanation (the $A_3^1$ two-diagram-type / iterated-subtraction story) now lives in Ch5's `subsec:Tobjects` itemize, three chapters away. Fix agreed: move (compress, don't duplicate) that explanation into Ch2 near the table — it's genuinely physics content about how $A_3^1$ is built, not something specific to the $T$-object or R-ratio story, so it belongs where the reader first needs it. Ch3's `eq:a31Norm` and Ch5's itemize then both get backward-pointers instead of each re-deriving it. Not yet drafted.

**The "exact coincidence" claim (§2.4.1, `:479-487`) — resolved.** ChatGPT initially objected that "the subtraction terms coincide identically... everywhere, not merely in the unresolved limit" was stronger than the argument supplied. On request, it went back to hep-ph/0505111 and found the claim is *literally stated* in that paper's §2.5 as a special property of two-jet production specifically — verified directly against the extracted paper text (exact quote match), not taken on ChatGPT's word. Henrique rewrote the paragraph to ground it in the paper's actual construction-based justification instead of the thesis's own weaker derivation, citing `Gehrmann-DeRidder:2005btv` directly in place of the general `Catani:1996vz` citation (kept only where the general limiting-behaviour contrast is stated). One round of review needed to catch a missing citation and an `$F_n=1$` "order" vs. "multiplicity" slip before it was solid.

**Everything else — confirmed mechanical, executed directly, full clean rebuild after each batch:**
- Fixed all 7 Section/Subsection/Subsubsection word-vs-label-type mismatches thesis-wide (built an actual label→heading-type map from every `\label{}` and cross-checked every reference against it — not spot-checking). 3 in Ch2 (`:333`, `:661`→now fixed, `:1192`), 3 in Ch4, 1 in Ch6. Re-ran the same automated audit after fixing: 0 remaining.
- "Passarino-Veltmann" → "Passarino-Veltman" (missing-n misspelling), 2 instances (Ch2, Ch4) — cross-checked against the 9 correct instances elsewhere. Re-verified after: 0 remaining.
- Ch2 terminology/grammar: "Fundamental Casimir" → lowercase; "$N$ for the leading order" → "leading colour" (real terminology confusion, not just capitalisation); $g^2$ → $g_s^2$; "make"/"makes" subject-verb agreement; $\eta^B$ → $\eta^b$ (ghost Lagrangian index mismatch); the `\alpha_0`-"as shown in Eq. 2.1" claim (that equation doesn't actually contain $\alpha_0$) corrected to "implicit in the covariant derivative"; "bare" redefined consistently as "before UV renormalisation" (was conflated with "without normalisation," which contradicts `eq:antennaTree` already including $\mathcal N_X$); several comma-splice/run-on sentences split (`subsec:phaseSpaceFactor`'s phase-space definition, the IBP scalar-products derivation); duplicate "relevant sectors" definition removed (kept the more complete one); $F_n$'s inconsistent triple description (quantity/observable/distribution) collapsed to one; "respectively subtract" tightened to state which counterterm subtracts which piece explicitly; "inadvertently" → "introduces" (the complexity is inherent, not accidental); $B_4^0$/$C_4^0$ topic-sentence fragments fixed; missing noun after "Born-level" fixed; subject-verb "does not exist" → "do not exist" (D–H families paragraph); ambiguous $\delta$-function reference in reverse-unitarity disambiguated ($\delta^{(d)}$ = momentum conservation, $\delta^+$ = on-shell condition); "$n_u=0$" note now explicitly excludes $A_2^0$ with its own stated reason (Born normalisation, not an integrated subtraction ingredient) rather than silently omitting it.
- `tab:antFamilies`'s final-state ordering vs. the surrounding prose's ordering — genuinely different conventions (table follows the reference paper's radiator-position ordering; prose lists by flavour pairing), not an arbitrary inconsistency. Added a one-line caption clarification rather than picking a "winner," since changing the table would break its deliberate fidelity to hep-ph/0505111's own Table 1.
- Citation-placement gaps: $B_4^0$'s definition (previously uncited while $C_4^0$'s wasn't) now cites `Gehrmann-DeRidder:2005btv`; `eq:antPhaseSpaceFact` (now an exact equality, was uncited) now cites the same; `eq:ATypeTObj` in Ch5 (previously only cited via an unrelated notation footnote) now cites `Gehrmann-DeRidder:2005btv` directly.
- Ch5: the $T$/$\mathcal T$ direction was reversed on first introduction ("These are the $\mathcal T$-objects... defined, before integration... notated by $T$") — fixed to state unintegrated/integrated forms in the correct order; `eq:integratedT`'s $T_{q\bar q}$ was missing its `^2` superscript present in every neighbouring equation (confirmed real, predates this session, not introduced by the T-object move) — fixed; several informal phrasings tightened ("kinematics- and colour-conscious" → "carrying both kinematic and colour information," "creates a very elegant structure... It goes like," → "takes a simple form," "construct back" → "reconstruct," "not singular to" → "not unique to," "notated" → "denoted," "blindly integrating" → "integrating the composite... directly"); ambiguous "As mentioned before Eq. X" comma fix; repeated "using... using" and missing articles in the chapter opening; closing sentence "exact expression... at NNLO" (misleadingly implies all-orders exactness) → "exact... coefficient through NNLO."
- Appendix B: "the vast majority of the present work's ensuing chapters will focus on [the massless] regime" — now sits after every main chapter, so there are no ensuing chapters; reworded retrospectively. "the massive phase-scale is not equal to" clarified to "the characteristic phase-space scale." "the phase-space in non-trivial" typo fixed. "With general massive denominator defined as" → plural "denominators" (two are being defined) and trimmed "takes the form that follows" redundancy. Conversational phrasing in the rank-one bubble worked example tightened ("we are able to write that" → "Lorentz covariance gives," "which once contracted... makes" → "Contracting with $p_\mu$ yields," "where we consider $r_1=p$" → "setting $r_1=p$").
- All three Ch2/Ch3 references to the PaVe material retargeted from the whole "Appendix B" to the specific "Section B.2" (`\ref{subsec:PaVe}`), now that PaVe and Massive Extension are two distinct numbered sections there.

**Explicitly not done, flagged rather than silently applied:**
- `subsec:PaVe`'s label still reads `subsec:` despite now being a `\section` — cosmetic only, left alone to avoid unnecessary churn across every reference to it.
- Appendix B's two backward references to Ch2 subsections (`:31`, `:95`) still omit the "of Chapter~\ref{ch:physicsbackground}" qualifier that the Ch5 case got — much lower priority since the chapter context is already set a few lines earlier in the appendix's own opening, unlike the Ch5 case.
- `subsec:PaVe`'s rank-one-example's $r_i$-vs-$p_i$ relationship (ChatGPT's finding) — genuinely two different, both-legitimate PaVe quantities (propagator shifts vs. independent tensor-decomposition basis vectors), already bridged explicitly in the worked example (`$r_1=p$`) but not in the general $N$-point discussion. Flagged, not fixed — didn't want to risk mis-stating the general relationship without more certainty.
- ChatGPT's #50 (systematic comma-vs-colon before displayed equations, recurring across `Eqs. 2.1–2.9, 2.11, 2.14, 2.27, 2.37, B.15` and others) — lower-value, very broad stylistic preference touching a large number of individually tiny edits; not applied this pass, left as an optional future polish.

Full clean rebuild confirmed after every batch (0 errors, 0 undefined refs/citations throughout); two automated audits (Section/Subsection label-type map, Veltman spelling) re-run at the end and both confirm zero remaining instances.

*Item added 26 Aug 2026, once the `antFamilies` split landed — review the Ch5 $T$-object content and run a logical-coherence/pacing check on Chapter 5.* The `subsec:Tobjects` block (`chapters/05-validation-r-ratio.tex`, now also including `eq:integratedT`) is a mechanical transplant from Ch2, not yet reviewed as Ch5 prose: it still opens with "As described in Subsection~\ref{subsec:colourAlg}..." — phrasing written for a reader arriving straight from `subsec:colourAlg` in Ch2, not one arriving at Ch5 after three intervening chapters — and the whole block was written to *introduce* antenna machinery, whereas by Ch5 the reader has long since met it, so tone/pacing may now be off. Also check how it sits against the *existing* Ch5 material immediately after it (the $R = \dots$ assembly equations) — there may be overlap or a register jump between the transplanted definitional prose and the pre-existing assembly walkthrough. **Sequencing unchanged: run this only after the systematic logical-coherence and cross-reference pass on Chapter 2 (the item directly above) is done** — Ch2's pass may still trigger further edits upstream of this block.

**Task E — §2.4 readability cleanup (separable from Task D, can be done independently) — CLOSED 27 Aug 2026, all five items done.** Two related items surfaced during this task that were never formal Task E items and remain open, tracked separately: `eq:bornNorm`'s further readability step (showing the $l=1$/$l=2$ cases explicitly instead of the "..." expansion) and the $D$–$E$–$F$–$G$–$H$ paragraph trim, which Henrique drafted himself and is now done too (`chapters/02-physics-background.tex:857-864`) — final version restores the split citations (`GehrmannDeRidder2005:neutralino` with the SUSY claim specifically, `Glover2010` with the $H\to gg$ EFT claim) after a review round caught them dropped, and fixes "Ch." to "Chapter" to match the thesis-wide convention. Full clean rebuild confirmed (0 errors, 0 undefined refs/citations).
- [x] Two of the three original mechanical items already done in the §2.5/§2.6 pass above: the empty `$ $` placeholder and the self-citing footnote at `subsec:phaseSpaceFactor`.
- [x] Eq. `eq:bornNorm` is index-heavy (the likely "Eq. 2.27") — simplify. **Fixed 26 Aug 2026, and it turned out to be a correctness bug, not just a readability one.** The sum bound `\min(l, k-1)` is only correct for $n\ge3$ (real-emission) antennae; for the $n=2$ purely-virtual antennae ($A_2^1$, $A_2^2$), where $k=(n-2)+l=l$, the bound evaluates to $l-1$ — one short. Concretely, $A_2^1$ ($l=1$) got a bound of $0$ (empty sum, i.e. the formula claimed the NLO virtual antenna needs *no* coupling-renormalisation counterterm at all), and $A_2^2$ ($l=2$) got a bound of $1$ instead of $2$ (silently dropping the double-counterterm term). Verified against standard coupling-renormalisation structure (an $l$-loop bare amplitude always needs exactly $l$ counterterms down to tree level, regardless of $n$) and cross-checked the general shape against the reference paper's own NNLO pole structure (eq. 9.56: renormalised content built from $\beta_0$-counterterms times the one-order-lower amplitude). Fix: bound changed from `\min(l,k-1)` to plain `l` — since $n\ge2$ always in this problem, $k\ge l$ always, so `\min(l,k)=l` unconditionally; the min-wrapper was only ever needed because the bound was off by one. This is also the readability win: one index cleaner, no `k`/min-comparison in the bound. Left the further readability step (showing the $l=1$/$l=2$ cases explicitly instead of the "..." expansion, since $l\le2$ throughout this thesis) as a follow-up, not done in this pass.
- [x] Three footnotes within ~130 lines in `subsec:antennaFuncs`/`subsec:colourAlg` — trim/merge. Done 26-27 Aug 2026, split across two sessions since one turned out to be a real terminology fix, not a style call:
  - `:581` ("other antenna types... different normalisations") and `:713` ("colour Fierz identity") — genuinely mechanical, inlined verbatim as parentheticals, no content change.
  - `:603` (colour charge vs. strong charge) — **not mechanical, and not actually a synonym choice either.** The footnote defined two different *kinds* of object (colour charge = a conserved quantum number; "strong charge" = the coupling constant $g_s$) as if they were alternate names for the same thing. But the main sentence attributes their conservation to the Ward-Takahashi and Slavnov-Taylor identities — WI enforces U(1) electric charge conservation, STI is its non-abelian generalisation enforcing SU(3) *colour* charge conservation; neither identity says anything about a coupling constant being "conserved" (couplings run with scale, they aren't Noether charges). So "strong charge," as the footnote defined it, wasn't the right concept for this sentence at all. Fix: main text now reads "the electric and colour charges"; footnote deleted outright, since there's no second unfamiliar term left needing definition.
  Full clean rebuild confirmed after all three (via manual `pdflatex`→`biber`→`pdflatex`×2, since `latexmk` hit a transient Dropbox-sync-related `.bcf` corruption a couple of times this session — not caused by the edits, resolved by the manual sequence each time).
- [x] Build the compact antenna-taxonomy table modelled on hep-ph/0505111 Table 1. Done 27 Aug 2026. Verified directly against Prof's original note (`PROF_PIRES_COMP_REVIEWS.md:154`): he names Table 1 explicitly by number and describes its exact structure (letter + defining final-state particles), so this isn't a style choice, it's a direct instruction, matched precisely. `tab:antFamilies`, new at `chapters/02-physics-background.tex:718` — final state / tree level / one loop / two loop columns, covering $A_2^0$ through $C_4^0$; replaced the enumeration prose at the old `:715-729` (kept the B-vs-C explanation and the $B_4^0$/$C_4^0$ derivation right after it, since a table can't replace *why*, only *what*). Henrique asked for the two-loop column to be added afterward, with $A_2^2$/$\tilde A_2^2$/$\hat A_2^2$/$\breve A_2^2$ in the $q\bar q$ row (matching Ch3's `tab:antennaTiers` convention) and `---` elsewhere, since it's genuinely the only two-loop entry in the thesis's scope.

  **Build-tooling note, 27 Aug 2026, unrelated to content correctness but worth recording:** while rebuilding after this edit, hit a real `main.out` (hyperref bookmarks file, pure build output) corruption — 8192 null bytes baked into a 12288-byte file — that made every subsequent `pdflatex` pass fail immediately with "invalid character" errors, cascading through several failed diagnostic attempts (briefly suspected Dropbox sync, then VS Code's LaTeX Workshop auto-build racing the manual rebuilds) before isolating the actual corrupted file. Fixed by deleting `main.out` (safe — it's regenerated automatically, no authored content) and rebuilding. Source `.tex` files were never at risk — checked brace/environment balance and confirmed zero null bytes in all of them before chasing the build issue. If a `pdflatex` run ever fails immediately with a wall of "Text line contains an invalid character" errors again, check `main.out`, `main.aux`, and other build artifacts for null-byte corruption first (`python3 -c "print(open(f,'rb').read().count(b'\x00'))"`) before assuming the source content is wrong.
- [x] **Un-softened 27 Aug 2026 — this is a direct instruction, not an optional "consider" — and now done.** Re-checked Prof's original note (`PROF_PIRES_COMP_REVIEWS.md:154`): he names hep-ph/0505111's Figure 3 explicitly, in the same sentence as the Table 1 instruction, both aimed at simplifying §2.4/2.4.1/2.4.2. Built `fig:antennaFactorisation` in `chapters/02-physics-background.tex`, right after `eq:antennaTree` in `subsec:antennaFuncs`. Deliberately *not* a copy of the paper's own Fig. 3 — Henrique found that one unreadable on first read ("doesn't come across as a division"), since it's two disconnected before/after snapshots with generic $1,\ldots,i,j,k,\ldots,m+1$ labels. This version instead draws it as three TikZ panels connected by literal "$=$" and "$\times$" nodes, so the factorisation reads as the multiplication it algebraically is; and it reuses the thesis's own concrete $\gamma^*\to q(i)g(j)\bar q(k)$ process (same one as `fig:feynDiagsEx`, one page earlier) instead of the paper's abstract $(m+1)$-parton indices, so there's no generic bookkeeping to translate. One layout bug caught and fixed during visual verification (rendered the actual PDF page to PNG and inspected it, not just checked for build errors): the "fixed $\tilde p_I+\tilde p_K$" label initially overlapped "$q(i)$" in the third panel — repositioned higher, reverified. Full clean rebuild confirmed (0 errors, 0 undefined refs/citations) after the fix.

  **Separate build-tooling note, 27 Aug 2026:** while building this figure, isolated the actual root cause of the `.aux`/`.bcf` corruption flagged in the taxonomy-table entry above — it was neither Dropbox sync nor VS Code's LaTeX Workshop racing (both suspected in turn), it was a corrupted `main.out` (hyperref bookmarks, pure build output) with 8192 null bytes baked into it from an earlier interrupted run, causing every subsequent `pdflatex` pass to fail immediately. Deleting `main.out` and rebuilding fixed it for good — hasn't recurred since. Updating the earlier note's diagnosis for the record: check `main.out` specifically, not just `main.aux`/`main.bcf`, if this happens again.

### §2.5 / §2.6 (Phase-Space Integration & IBP Reduction; The Massive Extension) — new, 25 Aug 2026; executed 25 Aug 2026

Prof's first paragraph in this batch (§2.4 reorder/exact-equations/mappings/T-objects) is a verbatim repeat of the note already captured above — no new action there.

**Done:**
- [x] §2.5 opening two paragraphs rewritten (Prof's text, adapted to use `\ref{eq:antennaIntegral}` instead of a hardcoded "(2.45)"). The old footnote explaining that "unintegrated" already refers to phase-space integration was removed — its content is now stated directly in the rewritten prose, so it was redundant.
- [x] Sentence fixes applied: "fixed mapped hard-radiator momenta" → "hard-radiator momenta"; "integrated through $\Phi_X$" → "integrated over $\Phi_X$"; "propagator or invariant denominators" → "propagator or Mandelstam invariants $s_{ij}$'s"; "integrand of a single amplitude" → "integrand of an antenna function". Also found and fixed a second instance of the "hard-radiator momenta... fixed" error later in the section ("$\tilde p_\text{hard}$ remains fixed as the hard-radiator combined momenta" → "...is the hard-radiator combined momenta"), confirmed by grep — a third-looking hit ("momenta held fixed during the loop integration...") was checked and left alone, since it's a different, legitimate usage (external momenta being constant with respect to the loop-integration variable, not a claim about hard-radiator momenta being fixed/mapped in general).
- [x] Paragraph below the master-integral-reduction equation rewritten (Prof's text), with the original's "positive vs. non-positive $a_j$" clarification kept as an added final sentence rather than dropped, since it wasn't in Prof's replacement but is genuine content not stated anywhere else.
- [x] Mechanical fixes: the empty `$ $` math-mode placeholder now reads "$SU(3)_c$ generators"; the self-citing footnote (`subsec:phaseSpaceFactor` citing itself) removed outright, since it added no information a reader in that same subsection didn't already have.
- [x] §2.6 "The Massive Extension" relocated wholesale to `appendices/appendixB.tex` ("Supporting Derivations," previously just an empty chapter header) as a new `\section`. Checked first: no `\label` existed anywhere in this section and no other chapter references its content by name or number (grepped Ch3/Ch4/Ch6) — genuinely self-contained, safe to move mechanically. One internal cross-reference inside the moved text ("throughout this chapter") was updated to `Chapter~\ref{ch:physicsbackground}` since it now lives outside that chapter.
- [x] Closing paragraph added at the true end of Chapter 2 (Prof's text, using `\ref{ch:packageframework}`/`\ref{ch:workedexample}` instead of hardcoded chapter numbers), now landing naturally right after §2.5.3 "Master Integrals" since §2.6 no longer follows it.
- [x] **Correction to an earlier claim in this file:** while doing this relocation, an Edit initially clipped one sentence short and left an orphaned fragment at the end of the chapter, which briefly looked like the chapter had always ended mid-sentence. That was wrong — checked against `git show HEAD` and the original text was complete. Both files are now clean; flagging this only so nobody trusts a stray "the file just stops" claim from earlier in this session.

**Still open — re-reviewed and relettered 25 Aug 2026:**
- [x] Remove footnote 17. **→ DONE via the Mechanical Pile, Mech-1.** See that entry — the earlier guess at which footnote this meant was wrong; resolved against `PROF_PIRES_COMP_REVIEWS.md` and fixed for real.
- [x] **Task F — DONE 27 Aug 2026.** The "2.54"/"2.60" labels in Prof's note were stale (equation numbers had shifted a lot this session) — relocated by content instead: the actual `\sim` equation was `sec:integration`'s opening ($\mathcal X_n^l\sim\int(\ldots)N_s(\ell_r,p_a)/\prod D_j^{a_j}(\ell_r,p_a)\ldots$, unlabeled), and the actual $D_j$-definition companion equation ("2.60") was in `subsec:ibp`, not `subsec:PaVe` as first guessed (checked: PaVe's own denominator definition is single-loop only, a special case, and PaVe is a separate technique applied after antenna integration — doesn't fit "clarify external-particle treatment in the antenna integration process" the way `subsec:ibp`'s general multi-loop $D_j$ definition does). Fix: recognised the `\sim` equation was a redundant, looser restatement of `eq:intFamily`, which sits a few lines later and is already exact — dropped the separate $N_s/D_j^{a_j}(\ell_r,p_a)$ notation entirely rather than patching it. Relocated `subsec:ibp`'s $D_j$ definition up to this earlier spot (new label `eq:denomDef`), added one new sentence — "for the massless, on-shell external partons considered throughout this work, $D_j$ reduces directly to a combination of Mandelstam invariants $s_{ij}$" — which is what actually resolves the $s_{ij}$-vs-$p_a$ complaint. `subsec:ibp` now references `eq:denomDef` back instead of re-deriving it. Visually verified via rendered PDF pages (not just build-clean), full clean rebuild confirmed. Also established, while investigating: Task F and Task G (`subsec:PaVe` move) don't actually conflict, since the relocated equation was never in `subsec:PaVe` to begin with.
- [x] **Task G — DONE 27 Aug 2026.** Re-verified scope before executing (the TODO's own protocol) — found it covered more than tracked: **three** references to `subsec:PaVe`, not the one originally noted (`chapters/03-package-framework.tex`'s line had also shifted, 296→313). The other two were internal to Ch2 itself: `:1022`, a forward pointer ("described in detail in Subsection~\ref{subsec:PaVe}"), and `:1216`, in `subsec:ibp`, which actively said "the *previous* Subsection~\ref{subsec:PaVe}" — this one would have become factually wrong once PaVe left, not just stale. All three resolved as mechanical reference-swaps to `Appendix~\ref{ap:supportingDerivations}` (the Ch3 case follows the same precedent already accepted for the `TObject` move — Ch3 describing an API whose full technical treatment lives elsewhere is normal, not the "reads backwards" problem). Moved the ~110-line block (including the rank-1 bubble worked example) to `appendices/appendixB.tex` as a new `\section` (promoted from `\subsection`; the internal "Rank-One Bubble Example" `\subsubsection` promoted to `\subsection` to match), landing as B.2 after "The Massive Extension." One more internal wording fix caught: a footnote inside the moved block said "throughout this subsection" — no longer accurate once promoted to a `\section`, fixed to "this section." Confirmed via `grep` that none of the moved block's internal equation labels (`eq:paveDens`, `eq:paveRankOne`, `eq:ellToDens`, `eq:gramInversion`, `eq:bubbleResult`) are referenced from outside the block — clean, self-contained move. Executed via a small Python script rather than the Edit tool after an exact-string-match failure on the ~110-line block (not a content issue — just safer for a block that size). Visually verified all three reference seams and the new appendix landing page via rendered PDF, not just a clean build. Full clean rebuild confirmed (0 errors, 0 undefined refs/citations).
- [x] **DONE — duplicate, already completed above.** Original text: "Remove or relocate §2.6, 'The Massive Extension'... to an appendix." This is the same action as the §2.6 relocation already logged as done earlier in this file — confirmed against the current source: `grep`-ing for "Massive Extension" across `chapters/` and `appendices/` finds it only in `appendices/appendixB.tex`, never in Chapter 2. Already done; this entry had just never been marked off.
- [x] **DONE — duplicate, already completed above.** Original text: "Add a closing paragraph at the end of Chapter 2... forward-references Ch3... and closes on the $\epsilon$-pole structure..." This is the same closing paragraph already logged as done earlier in this file — confirmed against the current source: Chapter 2's final paragraph already forward-references Ch3/Ch4 and closes on exactly the $\epsilon$-pole-structure point described here. Already done; this entry had just never been marked off.

### Still pending
- [ ] Prof said corrections for the remainder of Chapter 2 (beyond §2.4.2) are coming later this week — re-open this section when they arrive.

## Chapter 3 review — Prof. Pires, received 26 Aug 2026

Full-chapter review of `chapters/03-package-framework.tex`, grounded against the current source before
classifying (line numbers below refer to that file as of this pass). One point is a **repeat with
extended scope**: the "move T-objects out" note already tracked as Task D (Ch2) turns out to apply to
Chapter 3 too — see the Task D extension below, not a new task. Section numbers in Pires's email drift
slightly from the compiled numbering in places; items below are matched by content, not by his stated
number.

**[Mine] — DONE (26 Aug 2026).** All items below executed in one pass directly on
`chapters/03-package-framework.tex`, `references.bib`. Full clean rebuild confirmed afterward
(`latexmk -pdf -gg`, 4 pdflatex passes + biber): no undefined references, no undefined citations,
no LaTeX errors — every new `\ref{}` added below (to `eq:antennaIntegral`, `sec:dimreg`,
`eq:expansionOrderDiag`, `tab:twoLoopProdModes`, `ch:workedexample`) resolves correctly, and the
two new bibliography entries (`PackageX`, `FeynHelpers`) compile and cite correctly. Individual
items:
- [x] Opening 3 paragraphs (`:1-27`): footnote the GitHub link, state AntCalc was built from scratch
  for this thesis (supplied text); swap the FeynCalc/LiteRed2 sentence for the tighter supplied version;
  swap the "Experimental routes" sentence for the supplied version.
- [x] §3.1 opening objective sentence (`:40-41`) — supplied replacement (names "antenna subtraction
  method" instead of vague "antenna calculations").
- [x] §3.1 PaVe-reduced example sentence (`:45-46`) — supplied replacement describing $X_3^1$ via
  Passarino-Veltman reduction into scalar one-loop functions.
- [x] §3.1 `IntegrateAntenna` default-truncation sentence (`:47-51`) — supplied replacement naming the
  NLO/NNLO antennae explicitly.
- [x] §3.1 "Using route profiles..." grammar fix (`:61-63`) — supplied replacement.
- [x] §3.1 tier itemize (`:69-83`) → table, per Pires's sketch. Populate the full antenna list
  (already present in the current itemize) into the table columns he specified
  (Perturbative Order / Antenna / Status), plus his connecting sentences before and after.
- [x] §3.2 route-key example (`:96-103`) — supplied replacement naming "leading-colour"/"subleading-colour"
  explicitly instead of bare "leading"/"subleading".
- [x] §3.2 renormalisation cross-reference: add a pointer to `\ref{sec:dimreg}` (Section 2.3) alongside
  the existing `\ref{eq:alphaRescaling}` citation, in both the $A_3^1$ (`:333`) and $A_2^2$ (`:422`)
  UV-renormalisation paragraphs — label already exists, this is a straightforward addition, not a new
  derivation.
- [x] §3.3 build-stage opening paragraph (`:171-177`) — supplied replacement.
- [x] §3.3 loop-order-dispatcher sentence (`:192-196`) — supplied replacement (fixes the physics: tensor
  reduction of loop-momentum dependence, not "reduction of the loop momenta").
- [x] §3.3.2 "These antenna functions also differ..." grammar fix (`:303`) — supplied replacement.
- [x] §3.4 full section: rename to "From the Build Stage to Integration", replace the whole paragraph
  (`:448-460`) with the supplied text.
- [x] §3.5 opening paragraph (`:464-469`) — supplied replacement; resolve the placeholder
  "equation (X.YY)" to `\ref{eq:antennaIntegral}` (Ch2, the antenna phase-space integral definition).
- [x] §3.5.1 $A_2^1$-exception sentence (`:507-509`) — supplied replacement (Passarino-Veltman wording).
- [x] §3.5.1 Table `tab:intModes` "Current Route" column (`:518-523`): these are *integrated* routes,
  so the antenna symbols should be calligraphic ($\mathcal A_2^1$, etc.), matching the convention used
  everywhere else once an antenna is integrated — currently plain-letter, inconsistent with its own
  table's subject.
- [x] §3.5.1 closing paragraph (`:530-536`) — supplied $B_4^0$-example replacement, plus the new
  forward-reference to Chapter 4 for the master-integral basis.
- [x] §3.5.2 heading "PaVe Route" → "Passarino-Veltman Route"; opening paragraph (`:542-550`) — supplied
  replacement. Needs a real citation for Package-X/FeynHelpers where the text says "(add reference)" —
  not currently in `references.bib` (checked); source and add the correct entries rather than leave a
  placeholder (likely Patel 1503.01469 for Package-X, Shtabovenko 1611.06793 for FeynHelpers — verify
  before citing, don't transcribe from memory).
- [x] §3.5.2 IBP Route paragraph (`:554-567`) — supplied replacement, keeps the existing
  `\ref{sec:integration}` citation.
- [x] §3.5.3.1 "Expansion Order" subsection (`:612-619`) — supplied replacement.
- [x] §3.5.3.1 comment below `eq:expansionOrderDiag` (`:628-632`) — supplied replacement (fixes the
  apparent self-contradiction about literature validation); use `\ref{eq:expansionOrderDiag}` instead
  of the hardcoded "(3.15)".
- [x] §3.5.4 title → "Reconstruction of Colour Components"; first paragraph (`:634-647`) — supplied
  replacement. **Stop there** — the rest of this subsection (`:649-673`, the $\mathcal T_\text{Lead}$
  etc. content) is T-object material now in scope for the Task D extension below, not this pass.
- [x] §3.5.5 opening paragraph (`:676-679`) — supplied replacement; resolve "Table 3.4" to
  `\ref{tab:twoLoopProdModes}` (the two-loop contributions table in §3.3.3 — not `tab:intModes`, which
  is a different table also numbered 3.4 in his draft numbering).
- [x] §3.6 opening sentence (`:694`): name the two canonical functions explicitly —
  `\texttt{BuildAntenna}` and `\texttt{IntegrateAntenna}` — instead of "the two canonical functions".

**New Mechanical Pile items (sweep across the whole thesis, not just Ch3):**
- [x] **Mech-19 — DONE for Chapter 3 (26 Aug 2026).** "PaVe" → "Passarino-Veltman" wherever it named
  the reduction method in running prose: fixed all 5 remaining prose instances in Ch3 (the
  `AntennaReductionProfile`/`ReductionProfile` walkthrough at `:128,175,313`, the `tab:intModes`
  backend-name cell, and the `IntegrateAntenna[...]` workflow pseudocode's "IBP/PaVe reduction" line).
  Left the `subsec:PaVe` LaTeX label itself untouched, correctly — it's an internal identifier, not
  rendered text. **Not yet done in Chapter 2** — `subsec:PaVe`'s own prose still says "PaVe" throughout;
  deliberately left alone since that whole subsection is mid-relocation per Task G and would need the
  same fix regardless of where it ends up — do it once, as part of executing Task G, not twice.
- [x] **Mech-20 — DONE for Chapter 3 (26 Aug 2026).** "leading"/"subleading" (unqualified) →
  "leading-colour"/"subleading-colour" in running prose: fixed the `{A,3,1}` route-key example and the
  $A_3^1$-vs-$A_4^0$ comparison sentence in §3.3.2. Left alone, correctly: the literal code/option
  identifiers `Leading`/`SubLead`/`Subleading` (`Component->Subleading`, the `\texttt{}` profile-field
  names, the $X_\text{Lead}$/$X_\text{SubLead}$ subscripts) — these name actual package internals, not
  prose, and renaming them would misrepresent the code; and the generic "the subleading component" in
  §3.6, which reads unambiguously in its own context. **§3.3.1 (Tree-Level Routes) not swept** — that
  whole subsection is Task J territory (entangled terminology rewrite), left for that pass. **Ch2 not
  checked yet** — flagged, not done.
- [x] **Mech-21 — DONE (26 Aug 2026).** Added `PackageX` (Patel, Comput. Phys. Commun. 197 (2015)
  276-290, arXiv:1503.01469) and `FeynHelpers` (Shtabovenko, Comput. Phys. Commun. 218 (2017) 48-65,
  arXiv:1611.06793) to `references.bib` — verified via live web search against arXiv/ADS before adding,
  not transcribed from memory, since neither existed in the bibliography yet. Cited from the new
  Passarino-Veltman Route paragraph in Ch3. Confirmed resolving correctly in a full rebuild.

**New Yours tasks (continuing the letter sequence from Task G):**
- [ ] **Task H — §3.1, "Higher-order terms in the ϵ-expansion can be obtained through the options
  described above" (`:52-53`).** Pires flagged this as unclear — no specific option was actually named
  above it. Decide: cut the sentence, or point to the real option (`ExpansionOrder`, introduced later
  in §3.5.3.1) — either needs a judgment call about what this paragraph should promise this early.
- [ ] **Task I — Add an equation to the `AntennaProfile[{A,2,1}]` discussion (`:149-167`)** clarifying
  what `TreeAmplitude`/`BornInterference` actually compute for $A_2^1$ — Pires's suggestion, not
  supplied text; needs deciding which equation belongs here (likely `eq:treeLoopInterf`, already defined
  later at `:325-328`, or a purpose-built normalised version) and how it interacts with that later
  definition.
- [ ] **Task J — Full rewrite of §3.3.1 "Tree-Level Routes" (`:209-290`), entangled, several linked
  issues:** (a) "self-interference"/`BornInterference` terminology is confusing at tree level, since
  there's no actual interference except for $C_4^0$ — Pires wants "squared matrix element" in prose
  while the code field itself stays named `BornInterference` (can't rename the code); (b) the $B_0$
  letter clashes with the $B_4^0$ antenna — Pires suggests replacing the prose definition with the
  actual normalisation equation from hep-ph/0505111 eq. (3.2) directly (ground against
  `thesis_docs/pdfs/0505111v3.pdf` before writing); (c) "Leading"/"Subleading" needs the physical
  explanation — that $A_4^0$'s two-quark-two-gluon process generates exactly two colour terms ($N$,
  $1/N$) from its own colour algebra, not just named coefficients; (d) "profile-defined sectors" is
  internal package language and needs an actual physical definition of what a sector is; (e)
  "colour-independent" → "colour-stripped" (Pires: current wording is physically wrong, not just
  informal); (f) the $B_4^0$ description ("primary-current sector has been self-interfered") needs to
  state plainly whether this is an interference or a squared amplitude. One commit, not split — the
  paragraphs share the same terminology decisions throughout.
- [ ] **Task K — One-loop routes (§3.3.2, `:292-364`):** clarify "the components extracted" (`:302`,
  which components, and when there are none vs. three); add the missing antenna-definition equation
  (hep-ph/0505111 eq. 3.3-style) before the $T_\text{Lead}$/$T_\text{Sublead}$/$T_\text{QL}$ discussion
  so the later decomposition is motivated rather than dropped in; and the "using PaVe... as described in
  the appendix" cross-reference (`:296`) is **blocked on Task G** (the `subsec:PaVe`→appendix move isn't
  executed yet).
- [ ] **Task L — Two-loop $A_2^2$ (§3.3.3, `:366-434`): explain physically why $A_2^2$ has four
  components** (the two-loop quark form factor for $\gamma^*\to q\bar q$, split by the two distinct
  interference contributions) — currently just asserted, not derived or motivated. Pires separately
  raised, here and previously in conversation, that **Feynman-diagram figures for the antenna types
  themselves** ($A_2^2$, $A_2^1$, $C_4^0$, $A_3^1$, ...) would make the physical process each symbol
  represents legible to a reader who doesn't already know the antenna-subtraction literature — this is
  a bigger scope item, ties into the existing "expand the worked example" bullets in Section 4 below;
  treat as one open item rather than duplicating it there.
- [ ] **Task M — §3.5.3 "Normalisations and Convention Bridge" (`:571-608`), including `tab:GkNorm`.**
  Pires: this section doesn't explain why some routes need extra multiplicative factors and others
  don't, and an examiner won't care about the mechanics of an $8\pi^2\cdot e^{\epsilon\gamma}$-type
  factor — he explicitly suggested cutting it if a clear rewrite isn't achievable. Genuine editorial
  judgment call: rewrite around simple equations, or remove and fold anything essential elsewhere.
- [ ] **Task N — §3.5.1 "The $A_2^2$ exception" sentence (`:506`).** Needs one explanatory clause tying
  back to the two-source-contribution split already established in §3.3.3 (Task L) — smaller than the
  others, but still requires composing the connective explanation, not a supplied swap.
- [ ] **Task O — §3.7 "Validation and Development Diagnostics" (`:785-930`) — move to Chapter 5, per
  Pires.** Looked mechanical at first (same shape as the Massive Extension move), but checking cross-
  chapter references first (per the established red-flag rule — see
  `memory/mechanical-vs-yours-workflow.md`) found `chapters/06-future-work.tex:43` citing
  `\ref{subsec:wardAndRelease}` directly — the Ward-identity validation subsection inside this block is
  referenced forward from Ch6. **Reclassified Yours** for that reason alone, same pattern as Task D
  (`antFamilies`) and Task G (`subsec:PaVe`): needs a judgment call on whether to rephrase Ch6's
  reference or keep the forward pointer into Ch5.
- [ ] **Task P — §3.6 "Higher-Level Functions" (`:692-783`) — condense.** Pires: replace the long prose
  walkthrough of each of the five functions with the 1-2 line description already itemised, using his
  supplied one-liners as a starting point. Entangled with the Task D extension below, since the
  `TObject`/`BuildRRatio` descriptions here overlap with T-object content moving to Ch5 — do after that
  lands so the compressed description doesn't have to be rewritten twice.

**Task D — scope extension (repeat of the "T-objects belong in Ch5" note, now covering Chapter 3 too):**
Pires repeated the instruction explicitly for Ch3: the whole $\mathcal T$-object description belongs in
Chapter 5, not Chapter 3 either. This is the same underlying decision as the Ch2 T-object move already
tracked as Task D, not a new task — but it now also covers: §3.3.2's $T_\text{Lead}$/$T_\text{Sublead}$/
$T_\text{QL}$ interference-decomposition content (`:330-360`); §3.3.3's $T_{q\bar q}^6$ assembly
(`:436-446`); §3.5.4's $\mathcal T$-object equations, the "why already UV renormalised" question, and
the `ReturnTTerms` option (`:649-673`, everything after the first paragraph already scheduled as Mine
above); and §3.6's `TObject` function description and `tab:TObject` (`:742-773`, feeding into Task P).
Resolve as one decision alongside the rest of Task D's Ch2 roadmap — don't draft the Ch3 side of this
independently, since how much of this Ch3 content is a light forward-reference versus needs to move
wholesale depends on where the destination ends up.

**Update, 26 Aug 2026: the destination is Chapter 5** (`subsec:Tobjects` in
`chapters/05-validation-r-ratio.tex`) — briefly moved to `appendices/appendixB.tex` the same day, then
reverted (see Task D's "Destination history" entry above): the $T$-object form of the $R$-ratio equation
genuinely earns its place in Ch5's main argument, so its definition belongs there with it, not in an
appendix. That still resolves the "don't draft independently" blocker above: the Ch3 side can now be
scoped against a concrete, stable target. Still genuinely open, still Henrique's call — how much of
§3.3.2 (`:330-360`), §3.3.3 (`:436-446`), §3.5.4 (`:649-673`), and §3.6 (`:742-773`, `tab:TObject`) moves
to Ch5 outright versus gets trimmed to a short pointer at `\ref{subsec:Tobjects}`. Not executed this
session — flagged, not touched.

## Mechanical Pile — [Mine], reassigned 26 Aug 2026

Full sweep of the entire checklist (not just Section 0) on 26 Aug 2026, pulling out every item confirmed genuinely mechanical — no physics judgement, no composition, no editorial call Henrique needs to own — and renumbering them **Mech-1** through **Mech-18** in roughly the order they'll get executed. Everything left behind in its original section (Sections 1–6 below, plus Tasks C/D/E/F/G above) stays **[Yours]** or **verify**, confirmed deliberately rather than by default. Two items outside either pile — confirming title-page details with the supervisors, and getting a subject-matter reader's sign-off — need another person, not either of us, so they're left where they were.

- [x] **Mech-1 — DONE (26 Aug 2026).** Originally logged as "remove footnote 17," and originally misidentified: the compiled PDF at the time showed a footnote numbered 15 ("assumes generic kinematics with $p^2\neq0$...") that content-matched a footnote-17-shaped gap, but no reasoning for removing *that one* survived anywhere in this file's history, and its content read as a genuine caveat rather than cruft — so execution was paused pending confirmation. Henrique then added `PROF_PIRES_COMP_REVIEWS.md`, Prof's original notes in full. Cross-checked against a `pdftotext` dump of the *pre-revision* `main.pdf` (via `git show fb22fdb^:main.pdf`) and found the real footnote 17 two footnotes earlier than the one first suspected: "The inversion in Eq.~2.71 assumes a non-singular Gram matrix. Exceptional kinematics may require a limiting procedure or an alternative reduction method," attached to the Lorentz-decomposition-coefficients sentence in the PaVe rank-one-bubble derivation. Prof's note confirms the removal directly. Removed the `\footnote{...}` at what is now `chapters/02-physics-background.tex:1120` (in the pre-edit numbering; kept the sentence grammatical — "...by taking," now leads straight into `eq:gramInversion`). The footnote 15/"assumes generic kinematics" one that was originally suspected was never actually asked for removal (it's footnote 18 in Prof's own copy) and was correctly left alone.
- [x] **Mech-2 — DONE (26 Aug 2026).** "Twice ... plus one" conflicted with the displayed exponent $2(k+1)=2k+2$ — checked against $T^2/T^4/T^6$ at LO/NLO/NNLO ($k=0,1,2$) to confirm $2(k+1)$ is the correct reading. Reworded the footnote at `chapters/02-physics-background.tex:669` to "our previous $k$-index... incremented by one and then doubled, $2(k+1)$."
- [x] **Mech-3 — DONE.** `c\sigma^{RV}`→`d\sigma^{RV}` was already fixed in an earlier pass (confirmed via git history — predates this review cycle). The "As an example" half was real: `chapters/02-physics-background.tex:908` literally began mid-word, "an example, $\sigma^\text{NNLO}$..." — added the missing "As ".
- [x] **Mech-4 — Already satisfied, no change needed.** No `>>`/`<<` remain anywhere in Chapter 2 math mode (already fixed in an earlier pass). The one remaining `<<` in the whole thesis (`chapters/03-package-framework.tex`) is `<<AntCalc\`` — genuine Wolfram Language package-load syntax inside a `\texttt{}` code listing, not a math-mode inequality — correctly left alone.
- [x] **Mech-5 — DONE.** Confirmed against the thesis's own convention (`chapters/02-physics-background.tex:948` explicitly defines "loop-level antennae ($l\geq1$)"): the footnote at line 809 said "$l\geq0$... which happens for all loop antennae by definition" right after a sentence excluding $l=0$ — inconsistent with itself. Corrected to $l\geq1$.
- [x] **Mech-6 — Stale, not executed; premise didn't match the source.** The actual source at `chapters/04-worked-example.tex:1121` (the $\hat A_2^2$ finite term) reads `72\zeta_3`, not `72\zeta_2` — and $\zeta_3$ appears consistently across all four bracket terms in that same result (standard NNLO transcendental, not a one-off). No `zeta_2` exists anywhere in the thesis. This was evidently already fixed independently before this review cycle; touching it on the stale "72 zeta_2" premise would have risked introducing an actual error into a real physics result.
- [x] **Mech-7 — DONE.** Removed the duplicate `\cite{TM_JoanaReis}` right after its own footnote at `chapters/04-worked-example.tex:9-12` (cited once inside the footnote, then redundantly again immediately outside it). Corrected "Källen" → "Källén" at line 153.
- [x] **Mech-8 — Already satisfied, no source change needed; verified via full rebuild.** `preamble.tex`'s `\hypersetup` already had `colorlinks=true`, all link/cite/url colors set to black, and `pdfborder={0 0 0}` — functionally equivalent to `hidelinks`. Ran a full `latexmk`+`biber` rebuild and visually checked the Table of Contents (rendered page, all links) — no colour boxes anywhere.
- [ ] **Mech-9 — Remove "Versão Provisória" from the title page.** **Deferred — do not execute yet.** Only do this when actually preparing the final submitted version; running it now would be premature.
- [x] **Mech-10 — Already satisfied, no change needed; confirmed via rebuild.** Checked `chapters/03-package-framework.aux` after a full rebuild: `fig:higher-level` resolves to `{3.9}{55}`, exactly as expected — not pointing at the Future Work figure.
- [x] **Mech-11 — DONE.** Standardised to the canonical forms the item itself specifies. Fixed: 3× missing `~` in `Eq.\ref{}` (ch2:908, ch3:217, ch3:628 — the last two are exactly the "expansion-order"/`eq:expansionOrderDiag` area flagged); 1× missing `~` in `Fig.\ref{}` (ch3:644, the "component-extraction" area, also flagged specifically); 2× `Figure~\ref{}`→`Fig.~\ref{}` (ch3:29, ch4:564); 1× `Sec.~\ref{}`→`Section~\ref{}` (ch4:862). Appendices were already clean.
- [x] **Mech-12 — Already satisfied, no change needed.** Grepped for any `\cite{}\cite{}` (adjacent, any spacing/punctuation) across every chapter and appendix — zero matches. Every multi-source citation already uses the combined `\cite{a,b}` form.
- [x] **Mech-13 — Already satisfied, no change needed.** No `\textcolor{red}{...}` instances remain anywhere in the thesis.
- [x] **Mech-14 — DONE.** Found 10 instances of the manual slash hack `/\!\!\!X` across `chapters/04-worked-example.tex` (lines 618, 630–631, 648–651, 697, 871, 873 — the $A_3^0$/$A_2^0$ amplitude derivations), none of which the earlier search caught due to a regex-escaping miss. Converted all to `\slashed{X}` (`\slashed{k_1}`, `\slashed{\varepsilon}^*(k_3)`, etc.). Rebuilt and visually confirmed the Feynman-slash strokes render correctly (p. 70–71).
- [x] **Mech-15 — DONE, scope extended.** Fixed the 1 literal tilde-scope instance, plus 12 more of the *identical* bug under `\hat{}` and `\breve{}` (same root cause — the accent macro wrapping the whole `\mathcal A_n^l` including sub/superscripts instead of just the base letter) across `chapters/02-physics-background.tex`, `03-package-framework.tex`, and `05-validation-r-ratio.tex` — flagging this extension explicitly since the item as written only named "tilde." All now read `\tilde{\mathcal A}_3^1` / `\hat{\mathcal A}_2^2` / `\breve{\mathcal A}_2^2` style.
- [x] **Mech-16 — Narrowed after investigation; one real fix, two false positives.** "Wolfram Language" (1 instance, ch3 opening sentence, "an active research Wolfram Language package") is technically correct as written — Wolfram's own terminology distinguishes the language from the Mathematica front-end — left alone rather than forced to match the more casual majority usage. "build-side" vs. "Build Stage/build stage" turned out not to be a real inconsistency either: "build-side" is used only as an adjective, "Build Stage" only as the section-title noun phrase (matching the equivalent "Integration Stage" pattern) — genuinely different grammatical roles, not free variation. "master integral" vs. "MI" is the standard define-then-abbreviate pattern, not an inconsistency. The one real finding: 5 instances of "Build stage" (capital B) in running prose in `chapters/03-package-framework.tex`, inconsistent with the lowercase body-text convention used everywhere else (including the one already-correct "build stage" instance and every "integration stage" instance) — fixed all 5 to lowercase, left the actual `\section{The Build Stage}` title untouched.
- [x] **Mech-17 — Already satisfied, no change needed.** Both citations already exist in `references.bib` with exactly the bibliographic details requested (Alwall et al. JHEP 07(2014)079/arXiv:1405.0301 for MadGraph; Huss et al. SciPost Phys. Codebases 69 (2026) for NNLOJET — the published-venue form, which the item explicitly allowed for), already cited at `chapters/06-future-work.tex:49,51`, and the surrounding prose already frames both as proposed future connections ("could allow us to... enable...") rather than implying either is currently part of AntCalc.
- [ ] **Mech-18 — Final build & QA sweep (do last, near submission).** Consolidates three near-duplicate QA bullets originally scattered across Sections 1/3/6: run a clean build from scratch with Biber and resolve every warning; confirm the final compile has no undefined references or citations, visually checking Chapter 5 and all front matter; check the table of contents, lists of figures/tables, page numbering, captions, bibliography, and PDF metadata; do a final end-to-end visual pass (link borders absent, no empty reference boxes, code blocks/long equations/tables/workflow diagrams readable at normal print size).

## 1. Submission blockers

- [ ] Write the Portuguese **Resumo** and English **Abstract**.  Both are currently placeholders.
- [ ] Fill the four appendices, or temporarily remove their headings from the compiled thesis.  Their bodies currently contain placeholders.
- [x] ~~Remove visible PDF link borders...~~ **→ DONE (already satisfied) via the Mechanical Pile, Mech-8.**
- [ ] ~~Complete a final compile with no undefined references or citations...~~ **→ folded into Mech-18 in the Mechanical Pile.**
- [ ] ~~Remove "Versão Provisória" from the title page...~~ **→ moved to the Mechanical Pile as Mech-9 (deferred).**

## 2. Correctness and internal consistency

### Must verify before submission

- [ ] **A6 master-integral expansion:** reconcile the incompatible expansions in `chapters/04-worked-example.tex` around lines 389 and 513.  They have different `zeta_3` and `pi^4` coefficients (`23`/`103 pi^4/180` versus `27`/`23 pi^4/36`) and differ in the scale convention.  Establish whether a convention/analytic-continuation factor explains this; otherwise correct one expression and state the convention.
- [ ] **NNLO self-interference normalisation:** check the relative factor multiplying `breve A_2^2` between the Chapter 2 NNLO cross-section expression and Chapter 5 Eq. (5.5).  The review identified a possible `C_F` versus `2 C_F = N - 1/N` mismatch.  Verify against the cited R-ratio reference, not by visual pattern matching.
- [ ] **R-ratio expansion convention:** Chapter 5 first introduces coefficients in powers of `alpha_s/pi`, then writes the AntCalc result in `alpha_s/(2 pi)`.  Add one explicit sentence relating the two coefficient conventions (`c_NLO = r_NLO/2`, `c_NNLO = r_NNLO/4`) or use one convention throughout.
- [ ] Check the apparent loss of the `e_q^2 g_s^2 (N^2-1)` prefactor in the final line of the squared-amplitude derivation near Eq. (4.64).  If it is intentionally factored out, say so; otherwise restore it.

### Specific source corrections

- [x] **DONE** (fixed via the §2.2.1 pass) Chapter 2, footnote after the massless Mandelstam definition: change "as `p_k^2` for any `k`" to "as `p_k^2 = 0` for any `k`".
- [x] **DONE** (fixed via the §2.4 mechanical pass) Chapter 2 colour-algebra introduction: fill the missing group name in "governed by the $ $ generators `T^a`" — now reads "$SU(3)_c$ generators".
- [x] **DONE** — grepped the current source, no remaining "AndCalc" or "framework establish" instances anywhere in `chapters/02-physics-background.tex`.
- [x] ~~Chapter 2, footnote on `T^{2(k+1)}`...~~ **→ DONE via the Mechanical Pile, Mech-2.**
- [x] ~~Chapter 2: correct `c sigma^{RV}` to `d sigma^{RV}`...~~ **→ DONE via the Mechanical Pile, Mech-3.**
- [x] ~~Chapter 2: replace `>>` and `<<`...~~ **→ DONE (already satisfied) via the Mechanical Pile, Mech-4.**
- [x] ~~Chapter 2, footnote concerning loop antennae...~~ **→ DONE via the Mechanical Pile, Mech-5.**
- [ ] ~~Chapter 4: reduce `72 zeta_2`...~~ **→ found STALE via the Mechanical Pile, Mech-6 — source already reads `72\zeta_3`, not `zeta_2`; not touched. Left unchecked deliberately, see Mech-6.**
- [x] ~~Chapter 4: remove the duplicate citation... "Källen" → "Källén".~~ **→ DONE via the Mechanical Pile, Mech-7.**

## 3. References, labels, and typesetting

- [x] ~~Fix the higher-level-functions figure reference in Chapter 3...~~ **→ DONE (already satisfied, confirmed via rebuild) via the Mechanical Pile, Mech-10.**
- [x] ~~Standardise reference prose...~~ **→ DONE via the Mechanical Pile, Mech-11.**
- [x] ~~Standardise citation calls...~~ **→ DONE (already satisfied) via the Mechanical Pile, Mech-12.**
- [x] ~~Replace remaining red `\textcolor{red}{AntCalc}`...~~ **→ DONE (already satisfied) via the Mechanical Pile, Mech-13.**
- [x] ~~Use `\slashed{}` consistently...~~ **→ DONE via the Mechanical Pile, Mech-14.**
- [x] ~~Correct tilde scope...~~ **→ DONE (scope extended to \hat{}/\breve{} too) via the Mechanical Pile, Mech-15.**
- [ ] ~~Make an end-to-end visual pass after the final build...~~ **→ folded into Mech-18 in the Mechanical Pile.**

## 4. Strengthen the scientific argument

- [ ] In the introduction and conclusion, explicitly frame the contribution as **automation and validation of known antenna results**, plus an exploratory massive extension—not a claim of new massless QCD results.  This makes the novelty precise and defensible.
- [ ] Expand the `A_3^0` worked example with one **representative IBP reduction term**.  After introducing the cut-integral family, choose a term from the antenna integrand, show its denominator/index representation, state the reduction identity or LiteRed2 reduction output, and show how it reduces to the `R_3` master before the master substitution.  Keep it to one compact example: its job is to make the reduction bridge auditable, not to reproduce the complete IBP system.
- [ ] Expand Chapter 5, the thesis's main global validation.  Add a compact table of the pole cancellation by perturbative order and NNLO colour/flavour channel, followed by the final finite R-ratio comparison with the literature.
- [ ] Replace repeated "the result agrees with the literature" claims in Chapter 4 with one evidence-dense summary table.  Suggested columns: antenna route/components, build reduction, integration basis/master integrals, comparison target, Laurent orders checked, and validation status.
- [ ] Add a concise reproducibility paragraph, preferably in Chapter 3 or an appendix: AntCalc version/commit, Mathematica, FeynCalc/FeynArts/LiteRed2 versions, hardware/RAM, representative runtimes, and what tests are automated.
- [x] ~~Cite **MadGraph5_aMC@NLO** and **NNLOJET**...~~ **→ DONE (already satisfied) via the Mechanical Pile, Mech-17.**
- [ ] Add a short positioning paragraph on related subtraction/antenna automation tools.  State clearly what AntCalc automates, what it does not yet do, and how its role differs from a numerical event-generator validation such as NNLOJET.
- [ ] State in the conclusion that the present validation is analytic/symbolic and that an independent numerical Monte Carlo check remains future work.

## 5. Improve the thesis as a document

- [ ] Rebalance Chapter 3.  Keep the architectural rationale and the strong diagrams, but move the full profile association dump and other API-level detail to a user guide or appendix if page budget permits.  The main text should foreground design decisions, extensibility, and scientific consequences.
- [ ] Rework the longest raw Mathematica `In[]/Out[]` transcripts in Chapter 4.  Retain the compact examples that demonstrate the API, but replace truncated large outputs with a result statement, a reference to the complete expression, and a validation table.
- [ ] Enlarge or split the densest workflow figures and code blocks if they are not comfortably readable when printed.  Figure 3.9 is a likely candidate.
- [ ] Do a style pass to remove informal or weak framing: "simple", "simply", "usual", "as expected", and "we may now".  Prefer a direct statement of what is computed and why it establishes the claim.
- [ ] Reduce repeated descriptions of the build/integrate workflow, especially across Chapter 4 case studies; use the freed space for interpretation and validation evidence.
- [x] ~~Standardise terms and capitalisation...~~ **→ DONE (narrowed after investigation) via the Mechanical Pile, Mech-16.**

## 6. Final delivery checklist

- [ ] Confirm title-page names, degree wording, academic year, and institutional template requirements with the supervisors/department.
- [ ] ~~Run a clean build from scratch with Biber...~~ **→ folded into Mech-18 in the Mechanical Pile.**
- [ ] ~~Check the table of contents, lists of figures/tables...~~ **→ folded into Mech-18 in the Mechanical Pile.**
- [ ] Ask one subject-matter reader to sign off specifically on the two marked **verify** physics/normalisation items before submission.
